# دليل تغليف توفير للمتاجر (PWABuilder + TWA + Google Play / Huawei / Apple)

> ⚠️ هذا توثيق مكتوب بالكامل — لا تفتح pwabuilder، لا تولّد مفاتيح، لا ترفع شيئاً.
> هذا الدليل ككتاب تعليمات: اقرأه واتبع خطواته يدوياً على جهازك.

---

## أ) شرح التقنيات بإيجاز

### 1. ما هو PWA؟
**Progressive Web App** — موقع ويب يتصرف كتطبيق أصيل:
- يُثبّت على الشاشة الرئيسية بأيقونة مستقلة
- يفتح **بدون شريط عنوان المتصفح** (standalone)
- يعمل **أوفلاين** (Service Worker يخزّن البيانات)
- يتلقى **تحديثات فورية** عبر الويب (بلا متاجر — يكفي نشر نسخة جديدة على Vercel)

توفير **مبني كـ PWA أصلاً**: manifest ديناميكي + Service Worker + زر تثبيت + شاشة أوفلاين. يعمل مباشرة على https://tawfir.giize.com.

### 2. لماذا PWA كخيار أول؟
- **تحديث فوري بلا متاجر**: تنشر على Vercel → كل المستخدمين يرون التحديث فوراً (بعد رسالة «يتوفر تحديث»).
- **لا مراجعة متجر**: لا انتظار Google Play (أيام-أسبوع).
- **كود واحد**: نفس كود الويب يخدم أندرويد + iOS + سطح المكتب.
- **حجم صغير**: أقل من 5MB (مقارنة بـ APKs كبيرة).

### 3. ما هو PWABuilder؟
**أداة Microsoft مجانية** (https://www.pwabuilder.com) تحوّل PWA جاهزاً إلى حزم متاجر:
- يفحص جاهزية PWA (manifest, SW, icons, HTTPS)
- يولّد **APK + AAB** لأندرويد (عبر Trusted Web Activity)
- يولّد حزم iOS و Windows (اختياري)
- لا يحتاج خبرة أندرويد — كل شيء آلي

### 4. ما هو TWA؟
**Trusted Web Activity** — تقنية أندرويد تجعل تطبيق Play يفتح محتوى الويب **بلا شريط عنوان** (كأنه تطبيق أصيل):
- التطبيق = غلاف رفيع حول PWA
- يستخدم Chrome على الجهاز لعرض الموقع (لا WebView منفصل)
- يتطلب **assetlinks.json** للتحقق من الملكية (بدونه يظهر شريط عنوان)
- **ميزة**: تحديثات المحتوى تصل عبر الويب بلا مراجعة متجر — الحزم تُحدّث فقط عند تغيير الأيقونة/الاسم

---

## ب) خطوات التغليف عبر pwabuilder.com (مرقمة للمشرف)

> ابدأ بعد نشر توفير على Vercel (https://tawfir.giize.com يعمل + https://facility.tawfir.giize.com يعمل).

### الخطوة 1: افتح pwabuilder.com
- على متصفح سطح المكتب، اذهب إلى https://www.pwabuilder.com
- لا تحتاج حساب (مجاني تماماً)

### الخطوة 2: تطبيق العميل (tawfir.giize.com)
1. الصق في المربع: **`https://tawfir.giize.com`**
2. اضغط **Start** → يحلّل الموقع (قد يستغرق 30 ثانية)
3. راجع **النتيجة**: ستظهر بطاقات (Manifest, Service Worker, Installability, Security)
   - **يجب أن تكون كلها خضراء** للنشر الناجح
4. **إن رأيت بطاقة حمراء**:
   - **Manifest**: تأكد أن /manifest.webmanifest يُرجع JSON صحيح (تحقق: افتح الرابط مباشرة). لو ناقص icons/shortcuts، عادل `src/app/manifest.webmanifest/route.ts` وأعد النشر.
   - **Service Worker**: تأكد أن /sw.js يُرجع JS صحيح (افتح الرابط). لو غير مسجّل، تحقق من ServiceWorkerRegistrar.
   - **Installability**: يتطلب HTTPS (Vercel تلقائي) + manifest + SW.
   - **Security**: يجب HTTPS — Vercel يوفّره.
5. عند اخضرار الكل: اضغط **Package for stores**

### الخطوة 3: إعدادات حزمة أندرويد (العميل)
في نافذة التغليف:
- **Package ID**: `sa.giize.tawfir`
- **App name**: `توفير — بطاقة الخصومات`
- **Short name**: `توفير`
- **Version**: `1.0.0`
- **Version code**: `1`
- **Launcher name**: `توفير`
- **Status bar color**: `#005B82`
- **Theme color**: `#005B82`
- **Orientation**: `Portrait`
- **Fullscreen**: `false` (standalone أفضل)
- **Signing key**: اِستخدم مفتاحك (أنظر القسم ج)

### الخطوة 4: حمّل الحزم
- **APK** (للتوزيع المباشر/اختبار): حمّله — يُثبّت مباشرة على أندرويد
- **AAB** (لـ Google Play): حمّله — ارفعه على Play Console

### الخطوة 5: كرّر لتطبيق المالك (facility.tawfir.giize.com)
1. ارجع لـ pwabuilder.com
2. الصق: **`https://facility.tawfir.giize.com`**
3. اضغط Start → تحقق من الاخضرار
4. **Package for stores** → أندرويد:
   - **Package ID**: `sa.giize.tawfir.owner` (⚠️ **إلزامي مختلف** عن تطبيق العميل — لا يمكن أن يتطابقا)
   - **App name**: `توفير — بوابة المنشآت`
   - **Short name**: `توفير مالك`
   - **Version**: `1.0.0`
   - **Version code**: `1`
   - **Theme color**: `#003B55` (أغمق للمالك)
5. حمّل APK + AAB

---

## ج) مفتاح التوقيع (الأهم — أمان حرج)

> ⚠️ **تحذير صارخ**: فقدان مفتاح التوقيع = **استحالة تحديث التطبيق للأبد على Google Play**.
> احفظه في **3 نسخ آمنة** (قرص خارجي مشفّر + خزانة سحابية + تخزين مادي).

### 1. ولّد مفتاح التوقيع (مرة واحدة — للعميل والمالك)

افتح سطر الأوامر (Terminal) على جهازك واطلب أمر `keytool` (يأتي مع JDK):

```bash
keytool -genkeypair -v \
  -keystore tawfir-release.keystore \
  -alias tawfir \
  -keyalg RSA -keysize 2048 \
  -validity 10000 \
  -storepass YOUR_STORE_PASSWORD \
  -keypass YOUR_KEY_PASSWORD \
  -dname "CN=Tawfir, OU=Operations, O=Giize, L=Sanaa, ST=Sanaa, C=YE"
```

**تفسير البارامترات**:
- `-keystore`: اسم ملف المخزن (يُنشأ محلياً)
- `-alias`: اسم مستعار للمفتاح داخل المخزن (استخدم `tawfir`)
- `-keyalg RSA -keysize 2048`: خوارزمية RSA بمفتاح 2048-bit (معيار Play)
- `-validity 10000`: صالح 10000 يوم (~27 سنة — أكثر من كافٍ)
- `-storepass`: كلمة مرور المخزن (احفظها!)
- `-keypass`: كلمة مرور المفتاح (يمكن أن تطابق storepass)
- `-dname`: معلومات الموقّع (CN=الاسم، O=المنظمة، C=الدولة YE لليمن)

سيُنشأ ملف `tawfir-release.keystore` في مجلدك الحالي. **احفظه في 3 أماكن آمنة**.

### 2. استخرج بصمة SHA-256 (لملف assetlinks.json)

```bash
keytool -list -v \
  -keystore tawfir-release.keystore \
  -alias tawfir \
  -storepass YOUR_STORE_PASSWORD
```

سيطبع معلومات المفتاح. ابحث عن السطر:
```
SHA-256: XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX
```
انسخ هذه السلسلة (64 حرف سداسي عشري مفصولة بنقطتين `:`).

### 3. املأ ملف assetlinks.json

#### تطبيق العميل (`tawfir.giize.com`)
افتح `public/.well-known/assetlinks.json` في مستودع الكود واملأه:

```json
[
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "sa.giize.tawfir",
      "sha256_cert_fingerprints": [
        "XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX"
      ]
    }
  }
]
```

الموقع النهائي للملف على الإنتاج: `https://tawfir.giize.com/.well-known/assetlinks.json`

#### تطبيق المالك (`facility.tawfir.giize.com`)
أنشئ نفس الملف لكن بتغيير `package_name` إلى `sa.giize.tawfir.owner`:
```json
[
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "sa.giize.tawfir.owner",
      "sha256_cert_fingerprints": [
        "XX:XX:XX:XX:...السلسلة نفسها..."
      ]
    }
  }
]
```

يمكنك استخدام **نفس المفتاح** لكلا التطبيقين (موقّع واحد، package_name مختلف).

### 4. أعد النشر + تحقق
1. اِرفع تحديث `assetlinks.json` إلى Vercel (Git push → نشر تلقائي)
2. تحقق أن الملف يُخدم: افتح `https://tawfir.giize.com/.well-known/assetlinks.json`
   - يجب أن يُرجع 200 + Content-Type: `application/vnd.android.package.archive+json` (مُعد في next.config.ts)
3. تحقق بأداة Google الرقمية: **Google Digital Asset Links Tool** (ابحث في Google عن "Digital Asset Links" → الصق package_name + بصمة SHA-256 → يجب أن تقول "Connected" / "associated")
4. **أعد توليد الحزم** على pwabuilder.com (الآن بمفتاح التوقيع + assetlinks جاهز)
5. الحزم الجديدة ستفتح **بلا شريط عنوان** (تجربة TWA أصيلة)

> دون assetlinks صحيح: يظهر شريط عنوان أخضر فوق التطبيق — تجربة غير أصيلة.

---

## د) النشر على كل متجر

### 1. Google Play (الأهم)

#### المتطلبات:
- حساب **Google Play Console** ($25 مرة واحدة): https://play.google.com/console
- ملف **AAB** (وليس APK) من PWABuilder
- بيانات المتجر (وصف عربي + لقطات شاشة + تصنيف محتوى + رابط /privacy)

#### خطوات الرفع:
1. ادخل Play Console → **Create app**
2. اسم التطبيق: `توفير — بطاقة الخصومات` (للعميل)
3. اللغة الافتراضية: العربية
4. **App type**: Application
5. **Paid/Free**: Free
6. اضغط **Create app**
7. **Dashboard → Set up your app** → أكمل كل خطوات القائمة:
   - **App access**: All functionality available without restrictions
   - **Ads**: No
   - **Content rating**: املأ الاستبيان (aptitude for food/lifestyle) → احصل على تصنيف
   - **Target audience**: 13+ (أو حسب المحتوى)
   - **News app**: No
   - **Data safety**: املأ (لا نجمع بيانات حساسة — اقرأ /privacy)
   - **Government apps**: No
   - **App category**: Shopping أو Lifestyle
   - **Use of COVID-19 contact tracing**: No
8. **Store listing**:
   - **App name**: توفير — بطاقة الخصومات
   - **Short description** (80 حرف): منصة يمنية لطلب الوجبات وخصم 30% للأعضاء
   - **Full description** (4500 حرف): اشرح الميزات (تصفّح وجبات يمنية، طلب، عضوية، أوفلاين)
   - **App icon**: 512x512 PNG (من public/icons/icon-512.png)
   - **Feature graphic**: 1024x500 PNG
   - **Phone screenshots**: من public/screenshots/customer-*.png (3-8 لقطات)
   - **Privacy Policy URL**: `https://tawfir.giize.com/privacy`
9. **Releases → Production → Create release**:
   - ارفع ملف **AAB** (وليس APK)
   - release name: `1.0.0`
   - release notes (عربي): أول إصدار
10. اضغط **Review release** → **Start rollout to Production**
11. **المراجعة**: تستغرق أيام-أسبوع. ستصلك رسائل على بريد Console.

#### تكرار لتطبيق المالك:
- نفس الخطوات لكن: اسم `توفير — بوابة المنشآت`، package_name `sa.giize.tawfir.owner`، لقطات owner-*.

#### ⭐ ميزة TWA المهمة:
تحديثات **المحتوى** تصل عبر الويب بلا مراجعة متجر — تنشر على Vercel → المستخدمون يرون التحديث فوراً. **الحزم تُحدّث فقط** عند تغيير: الأيقونة، الاسم، package_name، أو مفتاح التوقيع.

### 2. Huawei AppGallery

#### المتطلبات:
- حساب **Huawei Developer**: https://developer.huawei.com
- ملف **APK** (وليس ABB — AppGallery يقبل APK)
- نفس الأصول

#### خطوات:
1. ادخل Huawei AppGallery Connect → **My Apps → Create**
2. اسم التطبيق: `توفير — بطاقة الخصومات`
3. **App type**: Application
4. ارفع APK (وليس AAB)
5. بيانات المتجر (نفس Google Play: وصف عربي + لقطات + أيقونة)
6. أضف رابط سياسة الخصوصية
7. اِرفع للمراجعة

### 3. Apple App Store (تقييم صادق)

> ⚠️ **تقييم صادق**: احتمال **رفض** تطبيقات الويب المغلفة (PWA/TWA). Apple apply قاعدة **4.2 Minimum Functionality** التي ترفض التطبيقات التي «مجرد موقع مغلّف».

#### الاحتمالات:
- **قبول**: لو تمكنت من إقناع المراجع بأن التطبيق يقدم قيمة أصيلة (إشعارات، تكامل جهاز، تجربة offline حقيقية). توفير يقدم: عضوية، طلبات، تتبّب، أوفلاين — قد يكون كافياً.
- **رفض**: الأرجح للـ PWA المغلفة مباشرة.

#### خطة Capacitor (حل جذري إن رُفضت):
1. ثبّت `@capacitor/core` + `@capacitor/cli` + `@capacitor/android` (لا iOS بعد)
2. بُنِ Capacitor wrapper حول نفس كود Next.js (static export)
3. أضف Native Plugins إن لزم (Camera, Geolocation, Push)
4. قدّم كتطبيق **Hybrid أصيل** (ليس مجرد WebView)
5. هذا يتطلب جهداً إضافياً — اتركه خطة لاحقة إن رُفضت

#### لا تدع آبل تعطل إطلاق أندرويد
- **ابدأ بـ Google Play + Huawei** أولاً (أيسر قبولاً)
- اِعرض نسخة PWA على https://tawfir.giize.com (تعمل على iOS Safari مباشرة — «إضافة إلى الشاشة الرئيسية»)
- Apple App Store كخطة تكميلية لاحقاً

---

## هـ) checklist ما قبل النشر (قائمة تحقق نصية فقط)

- [ ] APK عميل مثبّت على جهاز أندرويد → يفتح **standalone بلا شريط عنوان**
- [ ] APK مالك مثبّت بجانبه → أيقونة **مختلفة** (لون أغمق) → يفتح على بوابة المالك (/owner/login)
- [ ] **الطيران مفعل** على الجهاز → كلا التطبيقين يفتح → عميل يعرض آخر وجبات شوهدت (SWR)
- [ ] محاولة عملية بلا اتصال: محاولة طلب → رسالة **«يتطلب هذا الإجراء اتصالاً بالإنترنت»** (عربية)
- [ ] عودة الاتصال → **تحديث تلقائي** للبيانات
- [ ] نشر نسخة جديدة برقم إصدار أعلى على Vercel → رسالة **«يتوفر تحديث لتطبيق توفير»** + زر «تحديث الآن» → التطبيق يتحدث
- [ ] https://tawfir.giize.com/.well-known/assetlinks.json يُرجع 200 + JSON صحيح بالبصمة
- [ ] https://facility.tawfir.giize.com/.well-known/assetlinks.json يُرجع 200 + package_name المالك
- [ ] أداة Google Digital Asset Links تؤكد «Connected» لكلا التطبيقين
- [ ] سياسة الخصوصية (https://tawfir.giize.com/privacy) تعمل وربطها في بيانات المتجر
- [ ] اللقطات (3-8) من public/screenshots جاهزة للرفع

---

## و) ملاحظات ختامية

- **نفس الكود** يخدم: PWA على المتصفح + TWA على Play + نسخة Huawei — لا كود مكرر.
- **assetlinks.json** هو السر الذي يجعل TWA أصيلاً — بدونه يظهر شريط عنوان.
- **مفتاح التوقيع** = هوية التطبيق على Play. لا تفقده أبداً (3 نسخ آمنة).
- **تحديثات المحتوى** تصل فوراً عبر Vercel — لا انتظار مراجعة المتجر.
- **iOS**: لا beforeinstallprompt → اعتمد على إرشاد المستخدم. إن رُفضت نسخة TWA، استخدم PWA على Safari مباشرة + خطة Capacitor لاحقاً.
- **ابدأ بأندرويد**: Google Play + Huawei أيسر قبولاً. Apple لاحقاً.
