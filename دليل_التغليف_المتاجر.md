<div dir="rtl">

# 📦 دليل تغليف توفير للمتاجر — الجولة 24 (محدّث بالكامل)

> ⚠️ **هذا الإصدار محدّث بعد كل جولات التحسين (22-24)**: appId الجديد، الإصدار 1.2.0،
> ألوان الهوية الصحيحة، **مفتاح التوقيع الحقيقي المنشأ فعلاً** (tawfeer-release.keystore)،
> لقطات الشاشة الحقيقية الـ20، وإجابات نموذج PWABuilder حقل-حقل.
> **أي قيمة قديمة (sa.giize.tawfir / #005B82 / 1.0.0) — تجاهلها نهائياً؛ هذا الدليل هو المرجع الوحيد.**

---

## أ) الخلاصة السريعة (لو تقرأ سطراً واحداً)

| البند | القيمة الصحيحة |
|---|---|
| تطبيق العميل | `com.tawfir.ye.app` — **توفير** |
| تطبيق المالك | `com.tawfir.ye.owner` — **توفير مالك** |
| الإصدار الحالي | **1.2.0** (متزامن آلياً من package.json) |
| لون الثيم | **`#0A1A2F`** (كحلي الهوية — وليس #005B82) |
| الخلفية | **`#F7F7F7`** |
| مفتاح التوقيع | **`tawfeer-release.keystore`** — alias: **`tawfeer`** |
| assetlinks.json | ✅ محدّث ببصمتك الحقيقية (EE:E5:C2…) لكلا التطبيقين |
| المساران للتغليف | ① PWABuilder (TWA) ② Capacitor (Native Shell) |

---

## ب) مسارا التغليف — أيهما تختار؟

| المعيار | ① PWABuilder (TWA) | ② Capacitor (المهيأ بالمشروع) |
|---|---|---|
| ما هو | غلاف ترويجي حول الموقع الحي (يفتح كروم بلا شريط عنوان) | غلاف Native WebView + شاشة إقلاع أوفلاين (native-shell) |
| الحجم | ~2-5MB | ~3-5MB |
| الإشعارات | عبر تفويض كروم (Notification delegation) | عبر Firebase مباشرة (مهيأ بالمشروع) |
| التحديث | فوري من الموقع بلا متجر | المحتوى فوري + الحزمة عند تغيير الهيكل |
| الجهد | الأسرع (نموذج واحد + تحميل) | يتطلب Android Studio + Gradle |
| التوصية | **ابدأ به للإطلاق الأول** | استخدمه لو أردت إحساساً أعمق/رفض آبل |

> كلاهما يستخدم نفس الموقع الحي https://tawfir.giize.com — الكود واحد. يمكنك
> الإطلاق بـTWA اليوم والترقية لـCapacitor لاحقاً بنفس الحزم (com.tawfir.ye.app).

---

## ج) نموذج PWABuilder — الإجابة النهائية حقل-حقل (تطبيق العميل)

> على https://www.pwabuilder.com → الصق `https://tawfir.giize.com` → Start →
> Package for stores → Android. املأ **بالضبط** كما يلي:

### الحقول الأساسية

| الحقل | ✅ القيمة الصحيحة | ملاحظات |
|---|---|---|
| **Package ID** | `com.tawfir.ye.app` | ✓ صحيح كما عبّأت — يطابق assetlinks |
| **App name** | `توفير - بطاقة العضويات` | ✓ جيد (يظهر بالمتجر؛ حتى 30 حرفاً) |
| **Short name** | `توفير` | ✓ (يظهر تحت الأيقونة) |
| **Host** | `tawfir.giize.com` | ✓ |
| **Start URL** | `/` | ✓ |
| **Version** | `1.2.0` | ✓ — يجب أن يطابق package.json (المصدر الموحّد) |
| **Version code** | `1` | ✓ للإطلاق الأول — **ارفعه +1 مع كل تحديث حزمة** (2، 3، …) |

### الألوان — ⚠️ صحّح ما عبّأته

| الحقل | ما عبّأت | ✅ الصحيح | لماذا |
|---|---|---|---|
| **Theme color** | ~~#005B82~~ | **`#0A1A2F`** | #005B82 ليس من هوية توفير (بقي من دليل قديم). الكحلي #0A1A2F هو ثيم الموقع وشريط الحالة |
| **Theme dark color** | `#0A1A2F` | `#0A1A2F` | ✓ |
| **Background color** | `#F7F7F7` | `#F7F7F7` | ✓ (خلفية السبلاش الفاتحة) |
| **Nav color** (الأربعة) | `#0A1A2F` | `#0A1A2F` | ✓ كلها صحيحة |

### الأيقونات والـManifest

| الحقل | ✅ القيمة |
|---|---|
| **Icon URL** | `https://tawfir.giize.com/icons/icon-512.png` ✓ |
| **Maskable icon URL** | `https://tawfir.giize.com/icons/maskable-512.png` ✓ |
| **Monochrome icon URL** | (اتركه فارغاً — اختياري؛ نضيفه لاحقاً إن أردت) |
| **Manifest URL** | `https://tawfir.giize.com/manifest.webmanifest` ✓ |
| **Splash fade out duration (ms)** | `300` ✓ |

### الخيارات (Switches)

| الخيار | ✅ القيمة | السبب |
|---|---|---|
| **Settings shortcut** | ❌ إيقاف | يضيف اختصار إعدادات أندرويد — غير مطلوب |
| **Include source code** | ❌ إيقاف | حزمة أصغر |
| **Display mode** | **standalone** ✓ | كتطبيق أصيل بلا شريط عنوان |
| **Notification delegation** | ✅ **تفعيل** | **مهم**: يفوّض الإشعارات للنظام = إشعارات شاشة القفل (الإصلاح الجذري للجولة 22) |
| **Location delegation** | ❌ إيقاف | لا نستخدم تحديد الموقع (المناطق تُختار يدوياً) |
| **Google Play billing** | ❌ إيقاف | الدفع نقدي/محفظة خارج التطبيق — تفعيله يفرض سياسات فوترة |
| **ChromeOS only** | ❌ إيقاف | |
| **Meta Quest compatible** | ❌ إيقاف | |
| **Fallback behavior** | الافتراضي | |

### 🔑 قسم Signing Key — ⚠️ خطأك الوحيد المتبقي

عبّأت: alias `my-key-alias`، الاسم «توفير Admin»، المنظمة «توفير — بطاقة الخصومات»،
الوحدة «Engineering»، الدولة «US» — **كلها لا تطابق مفتاحك المنشأ فعلاً**.

**املأها ببيانات مفتاحك الحقيقي `tawfeer-release.keystore`** (الشهادة التي أنشأتها بـkeytool):

| الحقل | ✅ القيمة الصحيحة (من شهادتك الفعلية) |
|---|---|
| **Key alias** | `tawfeer` |
| **Key full name** (CN) | `Tawfeer App` |
| **Key organization** (O) | `Tawfeer` |
| **Key organizational unit** (OU) | `IT` |
| **Key country code** (C) | `YE` |
| **كلمة المرور** | `Tawfeer@2026#Secure` |

> **ملاحظة جوهرية**: PWABuilder يولّد مفتاحاً **جديداً** في المتصفح ببياناتك
> (بصمة مختلفة عن tawfeer-release.keystore المحلي حتى لو تطابقت البيانات).
> عندك خياران:
>
> **الخيار أ (الأبسط — موصى به للإطلاق الأول):** اقبل مفتاح PWABuilder المولّد
> بحقوقك أعلاه → حمّل الحزمة + **احتفظ بملف الـkeystore المرفق مع التنزيل**
> (هو مفتاح التحديثات المستقبلية على Play!). اجمع بصمته وأضفها لـassetlinks.
>
> **الخيار ب (استخدام مفتاحك المحلي):** نزّل الحزمة ثم أعد توقيعها محلياً
> بمفتاحك `tawfeer-release.keystore` (الأوامر في القسم و-2 أدناه). بصمته
> **موجودة أصلاً في assetlinks.json** فلا تحتاج تعديل الموقع.
>
> **القاعدة في كلا الخيارين**: أي مفتاح يوقّع الحزمة النهائية يجب أن تكون بصمته
> في `assetlinks.json` — وإلا ظهر شريط العنوان داخل التطبيق.

---

## د) مفتاح التوقيع الحقيقي — البيانات الكاملة (سجّلها بمكان آمن)

> 🔴 **قاعدة ذهبية**: فقدان المفتاح = استحالة تحديث التطبيق على Google Play للأبد.
> خزّن نسخاً في 3 أماكن (قرص خارجي مشفّر + تخزين سحابي + مدير كلمات مرور).

### المواصفات المسجلة من إنشائك الفعلي

| البند | القيمة |
|---|---|
| الملف | `C:\Users\Elite\Keystore\tawfeer-release.keystore` |
| **كلمة المرور** | `Tawfeer@2026#Secure` |
| **Alias** | `tawfeer` |
| الخوارزمية | RSA 2048-bit — توقيع SHA256withRSA |
| الصلاحية | حتى **21 يناير 2054** (10000 يوم من 5 سبتمبر 2026) |
| صاحب الشهادة | `CN=Tawfeer App, OU=IT, O=Tawfeer, L=Yemen, ST=Yemen, C=YE` |
| الرقم التسلسلي | `56380d59eb93df03` |

### البصمات (احفظها — تلزمك لـassetlinks وFirebase)

```
SHA1:    38:99:1A:C3:7E:64:B9:E1:BD:42:DE:93:EF:FF:0C:EA:77:3B:91:80
SHA256:  EE:E5:C2:B7:C4:CF:94:DE:8B:07:6A:5A:B9:E6:D8:6E:CB:27:EA:AC:4B:FF:A5:65:A4:B1:16:95:A0:96:72:1D
```

### التحقق من المفتاح متى شئت

```cmd
cd C:\Users\Elite\Keystore
keytool -list -v -keystore tawfeer-release.keystore -alias tawfeer
```

### أمر الإنشاء المرجعي (لو أعدت الكرة على جهاز آخر — للتوثيق فقط)

```cmd
keytool -genkeypair -v -keystore tawfeer-release.keystore -alias tawfeer -keyalg RSA -keysize 2048 -validity 10000
```
ثم تُجيب تفاعلياً بالقيم أعلاه (Tawfeer App / IT / Tawfeer / Yemen / Yemen / YE).

---

## هـ) assetlinks.json — الوضع الحالي ✅

ملف المشروع `public/.well-known/assetlinks.json` **محدّث بصمتك الحقيقية** لكلا التطبيقين:

```json
[
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "com.tawfir.ye.app",
      "sha256_cert_fingerprints": [
        "EE:E5:C2:B7:C4:CF:94:DE:8B:07:6A:5A:B9:E6:D8:6E:CB:27:EA:AC:4B:FF:A5:65:A4:B1:16:95:A0:96:72:1D"
      ]
    }
  },
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "com.tawfir.ye.owner",
      "sha256_cert_fingerprints": [
        "EE:E5:C2:B7:C4:CF:94:DE:8B:07:6A:5A:B9:E6:D8:6E:CB:27:EA:AC:4B:FF:A5:65:A4:B1:16:95:A0:96:72:1D"
      ]
    }
  }
]
```

### ⚠️ تحديث إلزامي بعد أول رفع على Play (Play App Signing)

عند رفع AAB على Google Play، **يعيد Google توقيعه بمفتاحه الخاص** (App Signing Key).
النسخة التي تُنزّل من المتجر موقّعة بمفتاح Google — وليس بمفتاحك. لذلك:

1. بعد أول رفع ناجح: Play Console → **Setup → App signing**
2. انسخ **App signing key certificate → SHA-256** (بصمة Google)
3. **أضِفها** لمصفوفة `sha256_cert_fingerprints` بجانب بصمتك (يمكن تعايشهما معاً):
   ```json
   "sha256_cert_fingerprints": [
     "EE:E5:C2:B7:...(بصمتك — للتثبيت المباشر/APK)",
     "أضف هنا بصمة Google من Play Console"
   ]
   ```
4. انشر الموقع (Vercel) — وتحقق:
   `https://tawfir.giize.com/.well-known/assetlinks.json` يجب أن يرجع 200
5. تحقق نهائي بأداة: **Google Digital Asset Links Tool** → يجب أن تقول Connected

> بدون هذه الخطوة: النسخة المثبتة **من المتجر** ستُظهر شريط عنوان (فشل التحقق)،
> بينما التثبيت المباشر بـAPK الموقّع بمفتاحك يعمل — لا تخلط بينهما أثناء الاختبار!

---

## و) إعادة التوقيع محلياً بمفتاحك (الخيار ب)

لو نزّلت حزمة غير موقّعة/موقّعة بمفتاح PWABuilder وأردت توقيعها بمفتاحك:

```cmd
cd C:\Users\Elite\Keystore

:: 1) موضع حزمة APK (غيّر المسار لموضع تنزيلك)
set APK=C:\Users\Elite\Downloads\app-release-unaligned.apk

:: 2) محاذاة أولاً (zipalign من Android SDK build-tools)
zipalign -v -p 4 %APK% tawfir-aligned.apk

:: 3) التوقيع بمفتاحك (apksigner — الحديثة)
apksigner sign --ks tawfeer-release.keystore --ks-key-alias tawfeer ^
  --ks-pass pass:Tawfeer@2026#Secure ^
  --key-pass pass:Tawfeer@2026#Secure ^
  --out tawfir-signed.apk tawfir-aligned.apk

:: 4) تحقق من التوقيع والبصمة
apksigner verify --print-certs tawfir-signed.apk
```

> المتوقع في الخطوة 4: بصمة SHA-256 مطابقة تماماً لـ`EE:E5:C2:B7:...:72:1D`
> (الموجودة في assetlinks.json ✓). أدوات `zipalign` و`apksigner` تأتي مع
> Android SDK (build-tools/34.x.x) — تجدها في `Android\Sdk\build-tools\`.

---

## ز) تطبيق المالك (بوابة المتاجر) — نفس النموذج بقيم مختلفة

على pwabuilder.com → الصق **`https://facility.tawfir.giize.com`** → Package for stores → Android:

| الحقل | القيمة |
|---|---|
| **Package ID** | `com.tawfir.ye.owner` ⚠️ **إلزامي مختلف** عن تطبيق العميل |
| **App name** | `توفير - بوابة المتاجر` |
| **Short name** | `توفير مالك` |
| **Host** | `facility.tawfir.giize.com` |
| **Start URL** | `/owner/login` |
| **Version / code** | `1.2.0` / `1` |
| **Theme color** | `#0A1A2F` |
| **Background** | `#F7F7F7` |
| **Icon URL** | `https://facility.tawfir.giize.com/icons/owner-icon-512.png` |
| **Maskable icon** | `https://facility.tawfir.giize.com/icons/owner-maskable-512.png` |
| **Manifest URL** | `https://facility.tawfir.giize.com/manifest.webmanifest` |
| **Display mode** | standalone |
| **Notification delegation** | ✅ تفعيل (إشعارات طلبات المالك) |
| **Signing key** | نفس بيانات مفتاحك (alias `tawfeer` … YE) |

> **نفس المفتاح** يوقّع التطبيقين معاً — يجوز تماماً (توقيع واحد بحزمتين مختلفتين).
> assetlinks للمالك محدّث بنفس بصمتك ✓.

---

## ح) لقطات الشاشة — الجاهزة الآن (الجولة 24)

**أُنجزت فعلاً**: 20 لقطة حقيقية عالية الجودة ملتقطة من التطبيق الحي بثلاث فئات أجهزة + الوضعين:

| الفئة | الأبعاد | الملفات |
|---|---|---|
| **موبايل** (Pixel 9) | 1080×2400 | customer-home/card/facility/offers/search/notifications/product/login/login-dark/membership + home-dark + facility-dark + offers-dark + owner-login |
| **تابلت** (iPad) | 1640×2360 | tablet-home / tablet-offers / tablet-facility |
| **سطح مكتب** | 1920×1080 | desktop-home / desktop-offers / desktop-facility / desktop-search |

الموقع: `public/screenshots/` — مرجعها الكامل: **دليل_اللقطات_الشامل.md**.

> للـPlay هاتفياً: ارفع حتى **8 لقطات** (استخدم: home، card، facility، offers،
> search، product، home-dark، notifications). النسبة الطويلة الحديثة (9:20) مقبولة
> (هي نسبة هواتف اليوم)، وإن اشتكى Play يوماً: القص إلى 1080×1920 بـsharp.

---

## ح-2) علامات تدقيق PWABuilder (Manifest Audit) — ما تحقق وما يتبقى

> سألت: «يجب أن نحصل على كل العلامات» — هذه خريطة الوضع بعد الجولة 24:

| البند (كما يظهر في التدقيق) | الحالة | التفصيل |
|---|---|---|
| **wide form_factor screenshot** | 🟢 **تحقق** | أُضيفت 4 لقطات سطح مكتب 1920×1080 بـ`form_factor: "wide"` في manifest العميل |
| **launch_handler** | 🟢 **تحقق** | `client_mode: "navigate-existing"` — الإطلاق يركّز النسخة المفتوحة (تطبيق بنافذة واحدة) |
| **display_override** | 🟢 **تحقق** | `["standalone", "minimal-ui", "browser"]` |
| **share_target** | 🟢 **تحقق** | استقبال النص المشارك → يفتح `/search` ويبحث به فوراً (ميزة حقيقية مفعّلة في SearchContent) |
| **related_applications** | 🟢 **تحقق** | رابط Play Store لحزمتي العميل والمالك (`prefer_related_applications: false` — الويب هو الأساس) |
| **protocol_handlers** | 🟢 **تحقق** | `web+tawfir://` → `/search?q=%s` |
| **iarc_rating_id** | ⬜ **لا يُحصل عليه إلا من Play** | مُعرّف IARC يصدر **فقط** بعد ملء استبيان تصنيف المحتوى في Play Console (القسم 4.4 في دليل Play). بعد الحصول على الرمز (UUID) أضفه للـmanifest: `"iarc_rating_id": "الرمز"`. لا تضع قيمة وهمية — الأداة تفحص صحتها |
| **scope_extensions** | ⬜ **غير قابل للتطبيق معماريّاً** | لتطبيقات متعددة النطاقات (مثل auth.example.com منفصلاً عن example.com). توفير **نطاق واحد** عبر بروكسي نفس الأصل (`/api/` → الباك إند) — إضافته تتطلب إعلان علاقة على النطاق الآخر وستفشل التحقق. تجاهله بأمان: بند اختياري لا يؤثر على التثبيت أو التقييم العام |
| **file_handlers** | ⬜ **غير منطقي لتطبيق طعام** | يخص تطبيقات تفتح ملفات (محرر نصوص/عارض PDF). توفير لا يفتح ملفات — إضافته تضخم بلا فائدة وقد تربك المتصفح |

> **النتيجة العملية**: كل بند قابل للتطبيق تقنياً وتسويقياً **مُحقق الآن**.
> البنود الثلاثة الأخيرة إما تتطلب بيانات خارجية حقيقية (Play Console) أو غير
> منطقية لطبيعة التطبيق — PWABuilder يصنّفها «اختيارية/توصيات» وليست أخطاء،
> ودرجة الجاهزية العامة لا تتأثر بها.

---

## ط) مسار Capacitor (البديل الأعمق — المهيأ بالمشروع)

المشروع مهيأ أصلاً بـCapacitor 8 (انظر `capacitor.config.ts` + دليل_الـCapacitor.md):

1. `npx cap sync android` → `npx cap open android`
2. في Android Studio: Build → Generate Signed Bundle → AAB
3. Key store path: `C:\Users\Elite\Keystore\tawfeer-release.keystore`
4. كلمة المرور: `Tawfeer@2026#Secure` — Alias: `tawfeer`
5. الإعداد في `android/app/build.gradle` (بملف خارج Git):
   ```gradle
   // android/keystore.properties (لا ترفعه لـGit أبداً)
   storeFile=../C:/Users/Elite/Keystore/tawfeer-release.keystore
   storePassword=Tawfeer@2026#Secure
   keyAlias=tawfeer
   keyPassword=Tawfeer@2026#Secure
   ```
   ```gradle
   // android/app/build.gradle
   def keystoreProps = new Properties()
   keystoreProps.load(new FileInputStream(rootProject.file("keystore.properties")))
   signingConfigs { release {
     storeFile file(keystoreProps['storeFile'])
     storePassword keystoreProps['storePassword']
     keyAlias keystoreProps['keyAlias']
     keyPassword keystoreProps['keyPassword']
   }}
   buildTypes { release { signingConfig signingConfigs.release } }
   ```
6. الناتج: `android/app/build/outputs/bundle/release/app-release.aab`

**تحقق قبل الرفع** (بصمة الحزمة يجب تطابق assetlinks):
```cmd
keytool -printcert -jarfile app-release.aab
```

---

## ي) Huawei AppGallery (اختياري)

- حساب مطور: https://developer.huawei.com (يُقبل من اليمن)
- يرفع **APK** (وليس AAB) — استخدم نسخة APK الموقّعة بمفتاحك (ط أو و)
- نفس الأصول: أيقونة 512 + لقطات + وصف عربي + سياسة الخصوصية
- نفس مفتاح التوقيع — لا تأثير على نسخة Google Play (متجران مستقلان)

---

## ك) Apple App Store (تقييم صادق — لاحقاً)

- خطر القاعدة 4.2 (Minimum Functionality) لتطبيقات الويب المغلفة قائم
- استراتيجيتنا: أطلق أندرويد أولاً + iOS يعمل كـPWA عبر Safari (إضافة للشاشة الرئيسية — السبلاش والزووم مهيآن للجولة 22)
- إن قررت لاحقاً: مسار Capacitor iOS هو الحل الأقوى (يتطلب Mac)

---

## ل) قائمة تحقق ما قبل الرفع النهائية

- [ ] نموذج PWABuilder معبأ **بالقيم أعلاه** (خصوصاً: الثيم #0A1A2F + تفعيل Notification delegation)
- [ ] قسم Signing Key ببيانات مفتاحك الحقيقي (tawfeer / Tawfeer App / Tawfeer / IT / YE)
- [ ] keystore (أيّاً كان: المحلي أو مرفق PWABuilder) محفوظ في 3 أماكن آمنة
- [ ] `https://tawfir.giize.com/.well-known/assetlinks.json` يرجع 200 ببصمة الموقّع
- [ ] تثبيت APK مباشر على جهاز أندرويد → يفتح **بلا شريط عنوان**
- [ ] إشعار اختباري من بوابة المالك → يظهر على **شاشة القفل**
- [ ] الطيران (وضع الطيران) → التطبيق يفتح ويعرض آخر البيانات
- [ ] بعد أول رفع على Play: إضافة بصمة App Signing من Console إلى assetlinks
- [ ] 8 لقطات هاتف + أيقونة 512 + Feature Graphic 1024×500 جاهزة للرفع
- [ ] Version code يبدأ 1 ويُرفع مع كل تحديث حزمة

---

*آخر تحديث: الجولة 24 — بعد رفعة v7 وتوليد اللقطات الحقيقية وتحقيق علامات PWABuilder*
</div>
