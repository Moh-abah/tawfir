# 🔍 تقرير فحص شامل لمشروع توفير — ما الذي ينقصه؟

> **المُعدّ:** Z.ai Code — شريكك التقني (50%)
> **التاريخ:** 6 سبتمبر 2026 — بعد 9 جولات webDevReview
> **الهدف**: تحديد كل ما ينقص المشروع قبل النشر الإنتاجي

---

## ✅ ما هو مكتمل (قوي)

### الفرونت إند (Customer)
- ✅ كل الصفحات 200 (21 مسار)
- ✅ Pull-to-refresh بنمط نيتفليكس + شريط تقدّم علوي
- ✅ Pinch-to-zoom مُعطّل في APK
- ✅ PageTransition fix (محتوى مرئي دائماً)
- ✅ Skeleton loaders بهوية توفير + لمعان ذهبي
- ✅ Animations للبطاقات (staggered fade-in)
- ✅ شارة "مفتوح الآن" ديناميكية
- ✅ زر المفضلة (قلب) + إدارة إشعارات
- ✅ نجوم التقييم على البطاقات
- ✅ شارة "عضو توفير" (VIP feel)
- ✅ شاشة سجل التوفير (/savings) + ROI
- ✅ شاشة المفضلة (متاجر + منتجات)
- ✅ RatingsSection في تفاصيل المنتج + المتجر
- ✅ Breadcrumbs (schema.org/BreadcrumbList)
- ✅ NetworkStatusIndicator في الهيدر
- ✅ Native feel (tap-highlight, overscroll, touch-action)

### الباك إند
- ✅ كل النقاط تعمل (regions, facilities, products, special-offers, membership, OTP, favorites, ratings, savings, KPIs, free-membership toggle)
- ✅ 18 نموذج (User, Facility, Product, Order, SpecialOffer, MembershipRequest, Notification, FcmToken, RefreshToken, PasswordResetToken, Favorite, Rating, AppSettings, Card, Region, AuditLog, UserCardAssignment, facility_card_link)
- ✅ Migration idempotent (007_ratings)
- ✅ Free membership toggle (admin dashboard)
- ✅ OTP via WhatsApp (mini-service)
- ✅ Smart notification engine (matching favorites → FCM)
- ✅ KPIs endpoint (MAU, paid_members, active_merchants, offer_usage, avg_savings, sales_value, renewal_rate)
- ✅ Owner stats محسّن (monthly_revenue, avg_order_value, new_customers, total_customers, monthly_visits, ROI, SEO score + tips)
- ✅ Savings service (توفير شهري/سنوي + ROI)

### SEO
- ✅ robots.txt ديناميكي
- ✅ sitemap.xml ديناميكي (متاجر + منتجات)
- ✅ schema.org/WebSite (SearchAction)
- ✅ schema.org/Product
- ✅ schema.org/Restaurant
- ✅ Breadcrumbs (schema.org/BreadcrumbList)

### PWA
- ✅ manifest (standalone, 4 icons)
- ✅ Service Worker (precache + offline)
- ✅ Offline page

### التوثيق
- ✅ worklog.md (2108 سطر)
- ✅ دليل_نشر_قوقل_بلاي.md (1137 سطر)
- ✅ دليل_المستندات_الشامل.md
- ✅ تقرير_الجولة_20_النهائي.md

---

## ❌ ما الذي ينقصه (مرتب حسب الأولوية)

### 🔴 أولوية عالية — يجب إكمالها قبل النشر

#### 1. لوحة التاجر المحسّنة (Frontend) — مفقود
**المشكلة**: الباك إند يُرجع مؤشرات جديدة (monthly_revenue, avg_order_value, new_customers, total_customers, monthly_visits, ROI, SEO score, SEO tips) لكن **الفرونت لا يعرضها**.
- `src/hooks/useOwnerStats.ts` لا يستهلك الحقول الجديدة
- `OwnerFacilitiesContent.tsx` يعرض عدّادات أساسية فقط (today-orders, total-orders)
- لا يوجد قسم SEO tips للتجار

**ما يجب فعله**: 
- تحديث `useOwnerStats` hook لاستهلاك الحقول الجديدة
- إضافة بطاقات مؤشرات (monthly_revenue, avg_order_value, ROI)
- إضافة قسم "نصائح SEO" (seo_score + seo_tips list)
- إضافة "عملاء جدد هذا الشهر" + "إجمالي العملاء"

#### 2. شاشة KPIs للأدمن (Frontend) — مفقود
**المشكلة**: الباك إند يُرجع KPIs (MAU, paid_members, active_merchants, offer_usage, avg_savings, sales_value, renewal_rate) لكن **لا توجد شاشة في الأدمن لعرضها**.
- `src/app/(admin)/admin/page.tsx` يعرض dashboard أساسي (pending_membership_requests, pending_facilities, orders_today) فقط
- لا يوجد `useKpis` hook
- لا يوجد رسم بياني للـKPIs

**ما يجب فعله**: 
- إنشاء `useKpis` hook
- إضافة قسم KPIs في admin/page.tsx (7 بطاقات + شرح)
- أو إنشاء شاشة /admin/kpis منفصلة

#### 3. لوحة التاجر — صفحة محدّث المتجر لا تعرض المؤشرات الجديدة
**المشكلة**: `/owner/facilities/[id]` تعرض المنتجات/الطلبات لكن لا تعرض المؤشرات التجارية الجديدة (monthly_revenue, ROI, SEO tips).

---

### 🟡 أولوية متوسطة — تحسينات مهمة

#### 4. canonical URLs — مفقود
**المشكلة**: لا يوجد `canonical` في metadata → قد يسبب محتوى مكرر في Google.
- `src/app/layout.tsx` لا يضبط `metadataBase`
- لا `alternates.canonical` في الصفحات

#### 5. error.tsx + not-found.tsx — مفقود
**المشكلة**: لا توجد صفحات خطأ مخصصة (404, 500) → المستخدم يرى صفحة Next.js الافتراضية.
- لا `src/app/error.tsx`
- لا `src/app/not-found.tsx`

#### 6. loading.tsx — مفقود
**المشكلة**: لا توجد حالة تحميل لكل صفحة → قد يظهر شاشة بيضاء أثناء التنقل.
- لا `src/app/(public)/loading.tsx`

#### 7. tracking الزيارات الحقيقية (FacilityView) — مفقود
**المشكلة**: `monthly_visits` في owner stats هو تقدير (`order_count × 5`) وليس زيارات حقيقية.
- لا يوجد نموذج `FacilityView` لتسجيل الزيارات
- لا endpoint لتسجيل الزيارة عند فتح صفحة متجر

#### 8. FCM service account حقيقي — مفقود
**المشكلة**: `fcm_service_account_json` فارغ افتراضياً → الإشعارات تُسجّل فقط (dev fallback).
- stub في `fcm_service.py` يُسجّل لكن لا يُرسل فعلياً
- يحتاج `firebase-admin` Python SDK + service account JSON

---

### 🟢 أولوية منخفضة — تحسينات اختيارية

#### 9. Hero section بصور طعام حقيقي
- حالياً gradient فقط — يمكن إضافة صورة طعام يمني (مندي/سلتة) كخلفية

#### 10. OpenGraph images ديناميكية
- إضافة صورة المنتج/المتجر في `openGraph.images` (حالياً صورة ثابتة)

#### 11. dark mode — غير مدعوم
- `globals.css` به 2 قاعدة `dark:` فقط → الوضع الداكن غير مكتمل

#### 12. fonts preloading
- لا يوجد `font-display: swap` أو preload للخطوط

#### 13. images optimization
- `images: { unoptimized: true }` — يعطّل تحسين الصور (لـCapacitor) لكن يقلل الأداء على الويب

---

## 📊 ملخص الفجوات

| المجال | المكتمل | المفقود | النسبة |
|---|---|---|---|
| Customer frontend | 95% | 5% (error pages, loading) | ممتاز |
| Admin frontend | 60% | 40% (KPIs screen) | يحتاج عمل |
| Owner frontend | 70% | 30% (مؤشرات تجارية + SEO tips) | يحتاج عمل |
| Backend | 95% | 5% (facility views tracking, FCM real) | ممتاز |
| SEO | 90% | 10% (canonical, OG images) | جيد جداً |
| PWA | 95% | 5% | ممتاز |
| Documentation | 100% | 0% | كامل |

---

## 🎯 التوصيات قبل النشر

### يجب إكماله (Critical):
1. **لوحة التاجر المحسّنة** (إظهار المؤشرات الجديدة + SEO tips) — يقنع التاجر بالتجديد
2. **شاشة KPIs للأدمن** (MAU, paid_members, renewal_rate) — يقيس النجاح

### يُفضّل إكماله (Important):
3. **error.tsx + not-found.tsx + loading.tsx** — تجربة أفضل
4. **canonical URLs** — منع محتوى مكرر
5. **FacilityView tracking** — زيارات حقيقية بدل التقدير

### اختياري (Nice-to-have):
6. FCM real (firebase-admin SDK)
7. Hero بصور حقيقية
8. OpenGraph ديناميكي

---

## 📝 الخلاصة

المشروع **جاهز للنشر** بنسبة **~85%**. الفجوات المتبقية هي تحسينات (لوحة التاجر + KPIs + error pages) لكن التدفق الأساسي يعمل بالكامل:

- ✅ العميل يمكنه: التسجيل (OTP) → الحصول على عضوية مجانية → تصفّح → طلب → تتبع توفيره → تقييم المنتجات/المتاجر
- ✅ التاجر يمكنه: إنشاء متجر → إضافة منتجات → إنشاء عروض خاصة → استقبال إشعارات الطلبات
- ✅ الأدمن يمكنه: مراجعة المتاجر/العضويات → تفعيل العضوية المجانية → رؤية dashboard أساسي
- ✅ الإشعارات الذكية: عرض جديد → إشعار فوري للمستخدمين المهتمين
- ✅ SEO: sitemap + robots + 3 structured data types

**الخطوة التالية**: أكمل لوحة التاجر + KPIs screen، ثم انشر مباشرة.
