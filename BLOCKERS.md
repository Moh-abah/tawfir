# BLOCKERS — توفير (الجولة 6 — محدّث بعد التحقق ببيانات الأدمن الحقيقية)

> كل ما لا تستطيع الواجهة فعله لغياب دعم الباك إند — بالترتيب حسب الأولوية.
> مواصفة الطلبات العاجلة في ملف «مطلوب_من_الباك_ند.md» (جاهز للإرسال لوكيل الباك إند).

## 1. 🔴 رفع صور المنتجات/المنشآت (أعلى أولوية — عائق حرج قبل الإطلاق التسويقي)

**Status:** غير متوفر في الباك إند

**الحقيقة (من OpenAPI الحي — 57 مساراً):** لا توجد أي نقطة رفع عامة (`POST /uploads` أو ما شابه). المساران الوحيدان اللذان يقبلان multipart هما:
- `POST /api/v1/membership/subscribe` (صورة التحويل receipt_image) — مُستخدَم فعلاً ✓
- `POST /api/v1/owner/{fid}/products/import` (ملف Excel) — مُستخدَم فعلاً ✓

بينما `ProductCreate`/`OwnerFacilityUpdate`/`FacilityCreate` تقبل `image_url` **نصاً فقط** (JSON، maxLength=500). النتيجة العملية الحالية: **102 منتج بلا صورة** في قاعدة البيانات (منصة طعام بلا صور!) لأن صاحب مطعم يمني لن يرفع صورة لاستضافة خارجية ثم يلصق رابطاً.

**المطلوب من الباك إند:** `POST /api/v1/uploads` (multipart/form-data → file) — يخزّن في `uploads/products/` و`uploads/facilities/` ويعيد `{url}` — المواصفة الدقيقة في مطلوب_من_الباك_ند.md.

**Workaround الحالي (الجولة 5):** مكوّن `ImageUrlField` مشترك (`src/components/shared/ImageUrlField.tsx`) في كل نماذج الصور (منتج/منشأة مالك/منشأة أدمن/تسجيل مالك): placeholder عربي واضح + معاينة فورية عند لصق رابط صالح + تحقق من صيغة الرابط + زر «حذف الصورة» + رسالة عربية عند فشل تحميل المعاينة. عند توفر endpoint الرفع يُستبدل الحقل برافع فعلي (drag&drop + ضغط client-side).

## 2. ✅ (مُغلَق في الجولة 6) بيانات دخول المشرف

**Status:** حُسمت — البريد الصحيح `admin@tawfir.giize.com` / `Admin@Tawfir2026#Secure`
(يعيد access_token فعلياً). بيانات `gleeze` الموثّقة في الجولة 2 كانت خاطئة (401 دائماً)
— ادعاء «دخول gleeze نجح» سُجّل كاذباً في جدول أمانة التقارير (البند 11). بوابة المشرف
اختُبرت كاملةً بالنقر في الجولة 6 — انظر قسم «التحقق الختامي ببيانات الأدمن» في
تقرير_توفير.md.

## 3. 🔴 موافقة/رفض طلبات العضوية — 500 من الخادم (حرج: الوظيفة الأساسية للأدمن معطّلة)

**Status:** عطل خادم مؤكد (مُختبر مباشرة بالجولة 6)

`PATCH /api/v1/admin/membership-requests/{id}/approve` و`.../reject` يرجعان **500
"Internal server error"** — بجسم صحيح (`{"reason": "..."}`) وبلا جسم وبأي توكن أدمن
صالح. الرفض تحديداً يفشل حتى قبل التحقق من السبب. **كل الإشراف على العضوية معطّل** —
الطلب المعلّق الوحيد (id=1) لا يمكن البتّ فيه إطلاقاً.

**الواجهة:** ترسل الطلب الصحيح (مُتحقَّق في Network) وتعرض رسالة عربية واضحة عند الفشل
(أُصلحت في الجولة 6 من «Internal server error» الإنجليزية).

**المطلوب من الباك إند:** إصلاح الخلل في endpoint الموافقة/الرفض (على الأرجح استثناء
غير معالَج عند إنشاء العضوية/كتابة rejection_reason/إشعار العميل). للتحقق: PATCH على
طلب pending موجود مع تتبع سجل الخادم.

## 4. 🔴 صور إيصالات العضوية (seed) — 404 من الخادم

**Status:** ملفات مفقودة (مُختبر بالجولة 6)

`/uploads/membership_receipts/seed_pending_demo.png` و`seed_approved_demo.png`
ترجعان **404** مباشرة من الخادم رغم أن سجلات الـ DB تشير إليهما (receipt_image_url).
أي موافقة حالية ستتم «على ثقة» بلا رؤية الإيصال.

**الواجهة:** الحوار يعرض الآن حالة خطأ عربية أنيقة (أُصلحت الجولة 6 — كانت صورة مكسورة).

**المطلوب من الباك إند:** استرجاع/إعادة توليد ملفات seed للإيصالات في مجلد uploads.

## 5. 🟡 رموز JWT 15 دقيقة بلا آلية refresh

**Status:** قيد معماري (مُختبر بالجولة 6 — انقطعت جلسات الاختبار الطويلة فعلياً)

كل توكن (عميل/مالك/أدمن) ينتهي بعد **900 ثانية** ولا يوجد endpoint لتحديثه
(/auth/refresh غير موجود في OpenAPI). بعد 15 دقيقة يبدأ كل طلب بـ 401 → تُمسح الجلسة
ويعاد المستخدم للدخول. «تذكرني (7 أيام)» يحفظ توكن **منتهي الصلاحية** في الكوكي —
المدة الفعلية للجلسة = 15 دقيقة مهما كانت الكوكي.

**المطلوب من الباك إند:** `POST /api/v1/auth/refresh` (يرجع access_token جديداً
بتوكن refresh طويل) — المواصفة في مطلوب_من_الباك_ند.md.

## 6. 🟡 لا إشعارات للأدمن إطلاقاً + لا دفع فوري للمالك عند الطلبات الجديدة

**Status:** سلوك خادم مؤكد (مُختبر بالجولة 6)

- **الأدمن:** لا يُنشئ الباك إند أي إشعار للمشرفين (طلب جديد/طلب عضوية/منشأة معلّقة) —
  `GET /notifications` للأدمن = قائمة فارغة دائماً (unread=0 مطابق للـ API). جرس الأدمن
  الحقيقي سيعرض 0 دائماً حتى يدعم الخادم ذلك.
- **المالك:** إشعار «📦 طلب جديد» **يُنشأ في DB** (يظهر عبر الاستطلاع/الجرس) لكن **لا
  يُدفع فورياً عبر WebSocket** — الدفع الفوري يعمل فقط للعميل عند تغيير حالة طلبه
  (مُتحقَّق فعلياً: toast «✅ تم تأكيد طلبك» وصل خلال ثانيتين).

**المطلوب من الباك إند:** توليد إشعارات للأدمن (طلب جديد/عضوية معلّقة/منشأة معلّقة) +
دفع WS فوري للمالك عند الطلب الجديد.

## 7. 🟡 علة تسجيل WebSocket: أي مقبس يُغلق يُلغي دفع المستخدم كلياً

**Status:** عطل خادم مؤكد (شُخّص بالجولة 6)

الخادم يبدو أنه يزيل تسجيل المستخدم من مدير الاتصالات عند **أي** close لأي مقبس له —
وليس فقط المقبس المسجّل. النتيجة: فتح تبويبين لنفس الحساب (أو أي إعادة اتصال) يكسر
الدفع الفوري حتى اتصال جديد.

**التخفيف المنفّذ في الواجهة (الجولة 6):** `connect()` أصبح خاملًا إذا كان المقبس نفسه
قيد الفتح — فلا قطع/إعادة اتصال غير ضرورية من جهتنا إطلاقاً (اتصال واحد نظيف لكل جلسة).
يبقى الخادم مسؤولاً عن فصل المقابس عن بعضها في مدير الاتصالات.

## 8. 🟡 تغيير كلمة المرور (أدمن/مالك/عميل) — لا endpoint

**Status:** غير متوفر في الباك إند

لا يوجد أي مسار لتغيير كلمة المرور في OpenAPI (لا للمشرف ولا للمالك ولا للعميل، ولا إعادة تعيين عبر بريد). لذلك حُذفت في الجولة 5 كل الأزرار الوهمية المرتبطة:
- قسم «أمان الحساب» (3 حقول كلمة مرور) من admin/settings — كان toast بلا API.
- زر «نسيت كلمة المرور؟» (toast «قريباً») من صفحتي دخول الأدمن والمالك — حُذف نهائياً.
- المطلوب مستقبلاً: `PUT /api/v1/me/password` + `POST /api/v1/auth/forgot-password` (مواصفة في مطلوب_من_الباك_ند.md).

## 9. 🟡 FCM Push Notifications (خارج التطبيق)

**Status:** موقوف مؤقتاً (Partial — بدون FCM حقيقي)

**Reason:** مكتبة `firebase` غير مثبتة في المشروع. الـ WebSocket المُنفّذ في `src/lib/ws-client.ts` (wss://api.tawfir.giize.com/api/v1/ws/notifications?token=) يكفي للإشعارات الفورية **داخل التطبيق** — **مُتحقَّق فعلياً بالجولة 6 من طرف إلى طرف**: أدمن غيّر حالة طلب عميل → toast فوري في متصفح العميل خلال ~2 ثانية + تحدّث العدّاد لحظياً. لاحظ: مسار الـ WS غير موثّق في OpenAPI لكنه يعمل فعلاً.

**Workaround الحالي:** `FcmRegistrar.tsx` يولّد pseudo-token فريد لكل جلسة، يُسجّله في `/fcm/token` (للتحقق من أن المسار يعمل)، ويحذفه عند الخروج.

**للتفعيل الكامل:** تثبيت firebase + `firebase-messaging-sw.js` + vapidKey (خطة الترقية في أسفل الملف).

## 10. ℹ️ قيود موثّقة أقل حرجاً

- **فلتر `only_members` في GET /special-offers رمزي:** وصف الباك إند نفسه يقول «فلترة رمزية — جميع العروض تظهر لكن مع أسعار العضو/غير العضو» — لا فائدة عملية من زر UI له، وُثّق بدل إضافته.
- **بحث الطلبات غير مدعوم خلفياً:** GET /orders لا يدعم search — بحث المالك في طلباته يبقى محلياً على الصفحة الحالية (100 طلب).
- **ترقيم صفحات طلبات المالك/الأدمن غير موجود في الواجهة:** الفلترة من الخادم (status) تعمل ✓ لكن page_size=100 مثبتة — عند تجاوز 100 طلب لن تظهر الزائدة (قيد خفيف).
- **GET /admin/dashboard لا يرجع حقول البطاقات الإجرائية** (pending_membership_requests/pending_facilities/orders_today) — الواجهة تجلبها الآن من نقاطها الفعلية (3 استعلامات إضافية) — يُفضّل إضافتها في endpoint التجميعي لاحقاً.
- **آلة حالات الطلب صارمة في الخادم** (pending→confirmed→preparing→out_for_delivery→delivered) — 422 برسالة عربية عند القفز (سلوك صحيح — توثيق فقط).
