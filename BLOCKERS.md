# BLOCKERS — توفير

> **آخر تحديث: بعد الجولة 8 — للأصوات**
> كل ما لا تستطيع الواجهة فعله لغياب دعم الباك إند — بالترتيب حسب الأولوية.

## ✅ مُغلَقة — أُصلحت في الجولة الختامية للباك إند (63 مساراً v1.1.0) واستُهلكت/اختُبرت فعلياً

> الجولة الختامية للباك إند أصلحت البنية التحتية، والجولتان 7 + 8 استهلكتا كل جديداتها
> واختبرتاها بالشبكة الحية على الإنتاج. لا يوجد أي عائق مفتوح منها بعد الآن.

| # | العائق السابق | الحقيقة الحالية (بالدليل) |
|---|---|---|
| 3 | 🔴 موافقة/رفض طلبات العضوية — 500 من الخادم | **مُغلق** — اختبره المشرف بنفسه على السيرفر في الجولة الختامية للباك إند: الموافقة رجعت **200 برقم عضوية حقيقي (4828176684929838) + expires_at** |
| 4 | 🔴 صور إيصالات العضوية (seed) — 404 من الخادم | **مُغلق** — أُصلح بنفس الجولة: ملفات الإيصالات تُولَّد الآن بـ **PIL عند الزرع** فتُخدم من `/uploads/membership_receipts/` بنجاح |
| 1 | 🔴 رفع صور المنتجات/المنشآت غير متوفر | **مُغلق** — `POST /api/v1/uploads` حي: ImageUploader كامل (drag&drop + ضغط canvas 1200px/webp/0.85 + تقدم XHR + إعادة محاولة) في المواضع الأربعة. مُتحقَّق: رفع 1600×1200 → 201 + الصورة ظهرت في كرت المنتج؛ exe مرفوض عربياً قبل الإرسال؛ 4000×3000 (2.6MB) ضغطت لـ163KB وقُبلت |
| 5 | 🟡 JWT 15 دقيقة بلا refresh | **مُغلق** — `POST /auth/refresh` حي مع تدوير: التجديد الشفاف في عملاء API الثلاثة. مُتحقَّق: توكن مفسود → 401→refresh→200 وإعادة الطلب بلا خروج؛ **8 طلبات 401 متوازية → تجديد واحد** (القفل)؛ التوكن القديم يُرفض عند إعادة استخدامه (401) |
| 6 | 🟡 لا إشعارات للأدمن + لا دفع فوري للمالك | **مُغلق** — الجسر + notify_admins: أنشئ طلباً كعميل عبر API → جرس الأدمن زاد 5→6 خلال ثوانٍ + «📦 طلب جديد #39» ظهر فوراً في القائمة |
| 8 | 🟡 تغيير/نسيت كلمة المرور بلا endpoint | **مُغلق** — PUT /me/password + forgot-password + reset-password كلها حية ومستهلَكة: قسم «أمان الحساب» في الإعدادات الثلاث (خطأ الحالية يظهر تحت الحقل + خروج تلقائي بعد 2 ثانية + الدخول بالجديدة يعمل) + زر «نسيت كلمة المرور؟» في الدخول الثلاث + صفحة /reset-password |
| 10-أ | بحث الطلبات غير مدعوم خادمياً | **مُغلق** — باراميتر search في المسارات الثلاثة: مُتحقَّق بـ Network في الشاشات الثلاث (?search= يُرسل بـURL-encoding للعربية) |
| 10-ب | dashboard لا يرجع الحقول التجميعية | **مُغلق** — pending_membership_requests/pending_facilities/orders_today تُستهلَك من استجابة dashboard الواحدة (حُذفت استعلامات الصفحة الثلاثة) |

## العوائق المفتوحة المتبقية

### 7. 🟡 علة تسجيل WebSocket: أي مقبس يُغلق يُلغي دفع المستخدم كلياً

**Status:** عطل خادم مؤكد (شُخّص بالجولة 6)

الخادم يبدو أنه يزيل تسجيل المستخدم من مدير الاتصالات عند **أي** close لأي مقبس له.
فتح تبويبين لنفس الحساب (أو أي إعادة اتصال) يكسر الدفع الفوري حتى اتصال جديد.

**التخفيف المنفّذ في الواجهة:** `connect()` أصبح خاملًا إذا كان المقبس نفسه قيد الفتح،
والاستطلاع الاحتياطي كل 30 ثانية (useUnreadCount) يغطي فقدان الدفع — **ومن الجولة 8
يُشغّل صوت أحدث إشعار عند اكتشاف زيادة في العدّاد** (المسار الاحتياطي صار مسموعاً).
يبقى الخادم مسؤولاً عن فصل المقابس عن بعضها في مدير الاتصالات.

### 9. 🟡 FCM Push Notifications (خارج التطبيق)

**Status:** موقوف جزئياً — بنية جاهزة، التفعيل مستقبلي

**Reason:** يتطلب إنشاء مشروع Firebase Console + مفاتيح رسمية (Server Key /
google-services.json) — قرار نشر ومفاتيح لدى المشرف، لا نقص كود في الواجهة.
الـ WebSocket يكفي للإشعارات الفورية **داخل التطبيق** (مُتحقَّق من طرف إلى طرف)،
ومن الجولة 8 صارت **مسموعة** عبر نظام أصوات الإشعارات (18 صوتاً قابلاً للاستبدال بلا كود).

**Workaround:** `FcmRegistrar.tsx` يولّد pseudo-token لكل جلسة ويُسجّله في `/fcm/token`.

### 10. ℹ️ قيود موثّقة أقل حرجاً (ما زالت صحيحة)

- **ترقيم صفحات طلبات المالك/الأدمن غير موجود في الواجهة:** الفلترة (status) والبحث (search) من الخادم يعملان ✓ لكن page_size=100/20 مثبتة — عند تجاوزها لن تظهر الزائدة (قيد خفيف).
- **آلة حالات الطلب صارمة في الخادم** (pending→confirmed→…) — 422 عربي عند القفز (سلوك صحيح — توثيق).
- **forgot-password في الإنتاج يرجع reset_token=null دائماً** (البريد هو القناة) — الواجهة تعرضه كملاحظة فقط إن حضر (بيئة اختبار).
