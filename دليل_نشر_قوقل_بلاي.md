<div dir="rtl">

# دليل نشر تطبيق «توفير» على Google Play Console

> دليل عربي عملي شامل — من بناء الـAAB إلى طرح التطبيق على متجر Google Play.
> التطبيق: Next.js 16 + Capacitor (Live WebView يحمّل https://tawfir.giize.com)
> الحزمة (appId): `com.tawfir.ye.app` — الاسم: **توفير** — Keystore: `android/tawfir.keystore` (alias: `tawfir`)
>
> ⚠️ الجولة 22: كان appId سابقاً com.tawfir.app — صُحّح إلى com.tawfir.ye.app ليطابق
> assetlinks.json (com.tawfir.ye.app / com.tawfir.ye.owner) — روابط أندرويد العميقة
> تتحقق من الحزمة في assetlinks؛ أي اختلاف = فشل التحقق وفقدان إحساس Native.

💡 **هذا الدليل جاهز للنسخ واللصق مباشرة في Google Play Console.** كل الحقول النصية (الوصف، الملاحظات، الأذونات) مكتوبة بالعربية ومُجهّزة للنسخ فوراً.

---

## فهرس المحتويات

1. [المتطلبات الأساسية قبل النشر](#1-المتطلبات-الأساسية-قبل-النشر)
2. [بناء الـAAB (Android App Bundle)](#2-بناء-الـaab-android-app-bundle)
3. [تسجيل تطبيق جديد في Google Play Console](#3-تسجيل-تطبيق-جديد-في-google-play-console)
4. [قائمة المحتوى (App content) — إلزامي](#4-قائمة-المحتوى-app-content--إلزامي)
5. [تحميل الـAAB وإصدار الإنتاج](#5-تحميل-الـaab-وإصدار-الإنتاج)
6. [مراجعة Play App Signing (مهم!)](#6-مراجعة-play-app-signing-مهم)
7. [التحقق من الإصدار والطرح المرحلي (Staged rollout)](#7-التحقق-من-الإصدار-والطرح-المرحلي-staged-rollout)
8. [الأصول البصرية المطلوبة](#8-الأصول-البصرية-المطلوبة)
9. [الأصول الجاهزة (نماذج عربية مباشرة)](#9-الأصول-الجاهزة-نماذج-عربية-مباشرة)
10. [الأسئلة الشائعة حول المراجعة (Review FAQ)](#10-الأسئلة-الشائعة-حول-المراجعة-review-faq)
11. [بعد النشر — المراقبة والتحديثات](#11-بعد-النشر--المراقبة-والتحديثات)
12. [قائمة تحقق نهائية (Checklist)](#12-قائمة-تحقق-نهائية-checklist)

---

## 1. المتطلبات الأساسية قبل النشر

### 1.1 حساب مطوّر Google Play

| العنصر | التفاصيل |
|---|---|
| الرابط | https://play.google.com/console/signup |
| الرسوم | **25$ أمريكي دفعة واحدة** (طوال عمر الحساب، غير مستردة) |
| بطاقة الدفع | Visa / Mastercard / بطاقة مدى مع تفعيل الدفع الدولي |
| الحساب Google | بريد Gmail فعال + رقم هاتف للتحقق (SMS) |
| الهوية | بطاقة هوية / جواز سفر / رخصة قيادة (للتحقق من العمر 18+) |
| المدة المتوقعة | 1–3 أيام عمل للتحقق اليدوي من قوقل |

**خطوات التسجيل:**

1. ادخل https://play.google.com/console/signup وسجّل الدخول بحساب Google.
2. أدخل اسمك الكامل وبريدك وعنوانك (يطابق الهوية).
3. اربط بطاقة دفع لرسوم 25$.
4. تحقق من رقم الهاتف (رسالة SMS بكود).
5. تحقق من الهوية (رفع صورة بطاقة/جواز) — قد يستغرق 1–3 أيام.
6. بعد التفعيل، ادخل https://play.google.com/console للوصول للوحة المطور.

⚠️ **تنبيه:** لا يمكن استخدام حساب شخصي مُعتاد على نشر تطبيقات تجارية أخرى بشكل مزدوج. كل تطبيق يجب أن يكون تحت حساب مطوّر واضح (شركة أو فرد). لأي تطبيق تجاري يخدم اليمن، يفضّل حساب باسم «توفير» أو باسم المالك القانوني.

### 1.2 البيئة التقنية على جهاز التطوير

```bash
# تحقق من الإصدارات المطلوبة
java -version          # يجب 17.x (JDK 17)
node -v                # 18+ (المشروع يستخدم 20+)
bun -v                 # 1.x (موصى به للسرعة)
npx cap --version      # Capacitor CLI 6+
```

| الأداة | الإصدار | الغرض |
|---|---|---|
| JDK | 17 (يجب، لا 18+) | Android Gradle Plugin 8.x يتطلب JDK 17 |
| Android Studio | Hedgehog 2023.1.1 أو أحدث | بناء AAB + محاكي |
| Capacitor CLI | 6.x | `npx cap sync` + `npx cap open android` |
| Bun / Node | Bun 1.x / Node 20+ | تشغيل سكربتات + بناء Next.js |
| Android SDK | API 34 (Platform) + Build Tools 34 | مطلوب من Gradle |

💡 **على Debian/Ubuntu:** ثبّت JDK 17 بـ `sudo apt install openjdk-17-jdk` ثم حدّث `JAVA_HOME` في `~/.bashrc`:
```bash
export JAVA_HOME=$(dirname $(dirname $(readlink -f $(which java)))
```

### 1.3 Keystore مُوقّع (التوقيع)

> 🔴 **القاعدة الذهبية:** استخدم نفس `android/tawfir.keystore` (alias: `tawfir`) الذي أنشأ `assetlinks.json`.
> بصمة SHA-256 المسجّلة هناك: `3F07592415B7AC0C96246799376FB7244EEF22A2CE1EEA949E358037C6EA6635`.
> مفتاح مختلف = **شريط عنوان أخضر** يظهر في أندرويد + كسر روابط Deep Links.

#### التحقق من Keystore الموجود:

```bash
cd /home/z/my-project/tawfir-workspace/tawfir-frontend

# قائمة محتويات keystore + بصمة SHA-256
keytool -list -v \
  -keystore android/tawfir.keystore \
  -alias tawfir \
  -storepass YOUR_STORE_PASS | grep -E "SHA256|SHA1|Valid|Owner"
```

**المتوقع:** ظهور السطر:
```
SHA256: 3F07592415B7AC0C96246799376FB7244EEF22A2CE1EEA949E358037C6EA6635
```

#### إنشاء Keystore جديد (إذا لم يوجد):

```bash
keytool -genkey -v \
  -keystore android/tawfir.keystore \
  -alias tawfir \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -storepass YOUR_STORE_PASS \
  -keypass YOUR_KEY_PASS \
  -dname "CN=Tawfir, OU=Mobile, O=Tawfir, L=Sanaa, ST=Sanaa, C=YE"
```

⚠️ **احفظ كلمتي المرور في مكان آمن جداً (مدير كلمات مرور + نسخة مشفّرة على تخزين سحابي).** فقدان keystore = فقدان القدرة على تحديث التطبيق نهائياً على Google Play.

#### استخراج بصمة SHA-256 لتحديث assetlinks.json (لو أنشأت keystore جديد):

```bash
keytool -list -v \
  -keystore android/tawfir.keystore \
  -alias tawfir \
  -storepass YOUR_STORE_PASS | grep SHA256
```
ثم حدّث `public/.well-known/assetlinks.json` بالبصمة الجديدة ونشرها على `https://tawfir.giize.com/.well-known/assetlinks.json`.

### 1.4 نسخة التطبيق (versionName + versionCode)

افتح `android/app/build.gradle` وتحقق من كتلة `defaultConfig`:

```gradle
defaultConfig {
    applicationId "com.tawfir.ye.app"          // ⚠️ يجب أن يطابق capacitor.config.ts
    minSdkVersion 23
    targetSdkVersion 34
    versionCode 1                            // عدد صحيح، يُرفع لكل تحديث
    versionName "1.0.0"                      // نص حر مثل "1.0.0"
}
```

| الحقل | القيمة الأولى | قاعدة الرفع |
|---|---|---|
| `applicationId` | `com.tawfir.ye.app` | ثابت للأبد |
| `versionCode` | `1` | عدد صحيح يزيد دائماً (1, 2, 3...) — يمنع Google Play رفض التحديث |
| `versionName` | `1.0.0` | نص للمستخدم، يظهر في المتجر |

💡 **علاقة versionName بإصدار الويب:** إصدار الـPWA في `src/lib/pwa/version.ts` هو `1.2.0` حالياً (لرسائل التحديث الداخلية)، بينما `versionName` في Gradle للـAPK نفسه. يُفضّل جعلهما متساويين عند الإطلاق الأول: `versionName = "1.2.0"` و `versionCode = 1`.

### 1.5 التحقق من appId

```bash
# تأكد أن كل القيم الثلاث متطابقة:
grep -h "applicationId" android/app/build.gradle
grep -h "appId" capacitor.config.ts
cat public/.well-known/assetlinks.json | grep -o '"package_name":"[^"]*"'
```

**المتوقع:** `com.tawfir.ye.app` في كل المواضع الثلاثة.

---

## 2. بناء الـAAB (Android App Bundle)

> Google Play **لا يقبل APK للنشر الأولي** — يجب رفع `.aab` (Android App Bundle). يحوّله Play تلقائياً إلى APKs محسّنة لكل جهاز.

### 2.1 خطوات Capacitor — مزامنة وفتح

```bash
cd /home/z/my-project/tawfir-workspace/tawfir-frontend

# 1) مزامنة أصول الويب مع مشروع Android (ينسخ native-shell/index.html + الإضافات)
npx cap sync android

# 2) فتح مشروع Android في Android Studio
npx cap open android
```

### 2.2 بناء AAB من Android Studio (الأسهل)

1. في Android Studio: قائمة **Build → Generate Signed Bundle / APK...**
2. اختر **Android App Bundle** ← Next
3. **Key store path:** `android/tawfir.keystore`
4. **Key store password:** YOUR_STORE_PASS
5. **Key alias:** `tawfir`
6. **Key password:** YOUR_KEY_PASS ← Next
7. **Build variants:** `release` ← Finish
8. الملف الناتج:
   ```
   android/app/build/outputs/bundle/release/app-release.aab
   ```

### 2.3 بناء AAB من سطر الأوامر (بديل)

```bash
cd /home/z/my-project/tawfir-workspace/tawfir-frontend/android

# تنظيف ثم بناء AAB للإنتاج
./gradlew clean bundleRelease

# المسار الناتج:
# android/app/build/outputs/bundle/release/app-release.aab
```

### 2.4 إعداد التوقيع في Gradle (مرة واحدة فقط)

في `android/app/build.gradle` أضف داخل كتلة `android { ... }`:

```gradle
signingConfigs {
    release {
        storeFile file('tawfir.keystore')
        storePassword 'YOUR_STORE_PASS'
        keyAlias 'tawfir'
        keyPassword 'YOUR_KEY_PASS'
    }
}
buildTypes {
    release {
        signingConfig signingConfigs.release
        minifyEnabled true
        proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
    }
}
```

⚠️ **لا ترفع كلمات المرور إلى Git.** ضعها في `android/keystore.properties` (مُستثنى من Git) واقرأها ديناميكياً:
```gradle
// keystore.properties (لا ترفعه لـGit)
storePassword=YOUR_STORE_PASS
keyPassword=YOUR_KEY_PASS
// في build.gradle:
def keystoreProps = new Properties()
keystoreProps.load(new FileInputStream(rootProject.file("keystore.properties")))
signingConfigs {
    release {
        storeFile file('tawfir.keystore')
        storePassword keystoreProps['storePassword']
        keyAlias 'tawfir'
        keyPassword keystoreProps['keyPassword']
    }
}
```

### 2.5 التحقق من توقيع الـAAB

```bash
# طباعة شهادة التوقيع داخل الـAAB (للتأكد قبل الرفع)
keytool -printcert -jarfile android/app/build/outputs/bundle/release/app-release.aab

# المتوقع: Owner: CN=Tawfir, O=Tawfir, C=YE
# + بصمة SHA-256 مطابقة لـ3F0759...6635 (إن استخدمت نفس keystore)
```

### 2.6 توقيع إضافي بـjarsigner (إذا لزم)

> عادة لا يلزم لأن Gradle يوقّع تلقائياً. هذه الخطوة فقط إذا اشتكى Play من توقيع مفقود:

```bash
jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
  -keystore android/tawfir.keystore \
  -storepass YOUR_STORE_PASS \
  android/app/build/outputs/bundle/release/app-release.aab \
  tawfir

# تحقق:
jarsigner -verify android/app/build/outputs/bundle/release/app-release.aab
```

### 2.7 الحجم المتوقع

| العنصر | الحجم التقريبي |
|---|---|
| غلاف Capacitor الأصلي (Native shell) | ~2–3MB |
| الأيقونات + Splash (في `res/`) | ~1.1MB |
| الأصول المحلية (`native-shell/index.html`) | 12KB |
| **حجم الـAAB النهائي المتوقع** | **~3–5MB** (أقل من حد 150MB المسموح) |

```bash
# تحقق من حجم الـAAB
ls -lh android/app/build/outputs/bundle/release/app-release.aab
```

---

## 3. تسجيل تطبيق جديد في Google Play Console

### 3.1 الدخول وإنشاء التطبيق

1. ادخل https://play.google.com/console
2. من القائمة اليمنى: **«كل التطبيقات» (All apps)** ← زر **«إنشاء تطبيق» (Create app)**
3. املأ الحقول التالية:

| الحقل | القيمة | ملاحظة |
|---|---|---|
| اسم التطبيق | **توفير** | يظهر للمستخدم في المتجر |
| اللغة الافتراضية | **العربية (ar-YE / ar-SA)** | صفحة المتجر الافتراضية |
| التطبيق مجاني أم مدفوع | **مجاني (Free)** | لا توجد مشتريات داخلية |
| البيانات: تطبيق أم لعبة | **تطبيق (App)** | |
| الفئة الأساسية (Category) | **تسوق (Shopping)** | لأنه طعام وشراب + خصومات |
| البيان | اقرأ ووافق على الشروط | ضروري |

4. اضغط **«إنشاء تطبيق»** في الأسفل.

### 3.2 البيانات الأساسية (Main store listing)

من قائمة التطبيق: **«توسيع التطبيق» (Grow) → «حضور المتجر» (Store presence) → «القائمة الرئيسية» (Main store listing)**

| الحقل | القيمة الجاهزة | ملاحظة |
|---|---|---|
| اسم التطبيق | `توفير` | حتى 30 حرف |
| اسم قصير (Short name) | `توفير` | اختياري — يظهر على شاشة التثبيت |
| الوصف المختصر | انسخ من [القسم 9.2](#92-الوصف-المختصر-80-حرف) | حتى 80 حرف |
| الوصف الكامل | انسخ من [القسم 9.3](#93-الوصف-الكامل-حتى-4000-حرف) | حتى 4000 حرف |
| رابط سياسة الخصوصية | `https://tawfir.giize.com/privacy` | إلزامي |
| الفئة | **تسوق** | القائمة المنسدلة |
| العلامات (Tags) | `طعام وشراب`، `توصيل`، `خصومات`، `مطاعم`، `عضوية` | حتى 5 علامات |
| البريد للاتصال | `moohabhb68@gmail.com` | أو بريد الدعم المناسب |
| رقم هاتف الدعم | `+967 780 090 882` | اختياري لكنه موصى به |
| موقع الويب | `https://tawfir.giize.com` | اختياري |
| نموذج بيانات التطبيق | يُحفظ تلقائياً بعد رفع أول AAB | |

💡 **العلامات تؤثر على ظهور التطبيق في البحث.** اخترها بدقة؛ أنصح: `طعام وشراب`، `توصيل`، `خصومات`، `مطاعم`، `عروض`.

### 3.3 اللغات والترجمات (Translations)

أضف ترجمة إنجليزية إضافية (اختياري لكنه موصى به لجذب المغتربين):

| اللغة | اسم التطبيق | الوصف المختصر |
|---|---|---|
| العربية (الافتراضية) | توفير | (القسم 9.2) |
| English (United States) | Tawfir | Tawfir — up to 30% off your favorite Yemeni meals. |

---

## 4. قائمة المحتوى (App content) — إلزامي

> من القائمة اليمنى: **«سياسة التطبيق والموافقة» (Policy and programs) → «محتوى التطبيق» (App content)**.
> **لا يمكن نشر التطبيق قبل إكمال كل البنود التالية.**

### 4.1 سياسة الخصوصية (Privacy policy) — إلزامي

| الحقل | القيمة |
|---|---|
| هل يوجد رابط سياسة خصوصية؟ | **نعم** |
| رابط سياسة الخصوصية | `https://tawfir.giize.com/privacy` |
| نوع الالتزام | يجري جمع/مشاركة بيانات المستخدم (نعم) |

💡 الصفحة موجودة فعلاً في `src/app/(public)/privacy/page.tsx` ومتاحة على `https://tawfir.giize.com/privacy`. تحقق أن الرابط يفتح 200 قبل الرفع:
```bash
curl -I https://tawfir.giize.com/privacy
# المتوقع: HTTP/2 200
```

### 4.2 الإعلانات (Ads)

| الحقل | القيمة |
|---|---|
| هل يحتوي التطبيق على إعلانات؟ | **لا** |

### 4.3 الوصول للتطبيق (Accessibility / Service)

| الحقل | القيمة |
|---|---|
| هل يطلب التطبيق أذونات إمكانية الوصول (AccessibilityService)؟ | **لا** |

### 4.4 استهداف المحتوى (Content rating)

اضغط **«بدء الاستبيان» (Start questionnaire)** وأجب:

| السؤال | الإجابة |
|---|---|
| هل يحتوي على عنف؟ | **لا** |
| هل يحتوي على دماء؟ | **لا** |
| محتوى جنسي؟ | **لا** |
| ألفاظ نابية؟ | **لا** |
| مقامرة؟ | **لا** |
| محتوى مرعب؟ | **لا** |
| مواد خاضعة للرقابة (كحول/تبغ/مخدرات)؟ | **لا** |
| معلومات مالية (شراء/دفع)؟ | **نعم — لكن خارج التطبيق** (الدفع نقداً عند الاستلام أو عبر المالك) |
| معلومات شخصية (اسم/هاتف)؟ | **نعم** |

**التصنيف المتوقع:** **«الجميع» (Everyone)** ← احفظ.

### 4.5 الجمهور المستهدف (Target audience)

| الحقل | القيمة | ملاحظة |
|---|---|---|
| الفئة العمرية المستهدفة | **18 سنة فأكثر** | تطبيق طعام + شراء، الأفضل 18+ لتفادي فرض رقابة على الأطفال |
| الفئة العمرية التي قد تجذب الأطفال | **لا — لا يجذب الأطفال تحت 13** | يمنع فرض سياسة الأطفال |
| التحقق من الجمهور المستهدف | أجب عن الأسئلة (هل المحتوى موجه للأطفال؟ لا) | |

### 4.6 التطبيقات الإخبارية (News app)

| الحقل | القيمة |
|---|---|
| هل التطبيق تطبيق أخبار؟ | **لا** |

### 4.7 تتبع البيانات (Data safety) — الأهم والأكثر تعقيداً

اضغط **«بدء» (Start)** وأجب عن سؤالين رئيسيين:

#### السؤال 1: هل يجمع التطبيق أو يشارك بيانات مستخدم؟
**الإجابة: نعم** (يجمع اسم + بريد + هاتف + سجل الطلبات + الصور إن رفعها)

#### السؤال 2: املأ جدول أنواع البيانات

| نوع البيانات | المجموعة؟ | الهدف | إلزامي؟ | مشفّر أثناء النقل؟ | يمكن حذفها بطلب؟ |
|---|---|---|---|---|---|
| **المعلومات الشخصية** (الاسم) | ✅ نعم | حساب المستخدم + عرضه في بطاقة العضوية | اختياري (للعضوية) | ✅ نعم (HTTPS) | ✅ نعم بطلب |
| **عناوين البريد** (Email) | ✅ نعم | معرّف حساب + تواصل | اختياري (للعضوية) | ✅ نعم | ✅ نعم |
| **رقم الهاتف** | ✅ نعم | التحقق (OTP) + تواصل بشأن الطلبات | إلزامي | ✅ نعم | ✅ نعم |
| **العنوان الفعلي** (للتوصيل) | ✅ نعم | توصيل الطلبات | اختياري (للطلب فقط) | ✅ نعم | ✅ نعم |
| **الصور** (لرفع صورة الإشعار عند الحاجة) | ⚠️ اختياري | رفع صورة لإثبات الطلب (إن لزم) | اختياري | ✅ نعم | ✅ نعم |
| **السجل (Activity)** — الطلبات والبحث | ✅ نعم | تاريخ الطلبات + التوصيات | تلقائي عند الاستخدام | ✅ نعم | ✅ نعم |
| **المعرّفات** (معرّف جهاز FCM للإشعارات) | ✅ نعم | إرسال الإشعارات | اختياري (عند تفعيلها) | ✅ نعم | ✅ نعم |

#### السؤال 3: هل يمكن للمستخدم طلب حذف بياناته؟
**الإجابة: نعم** — أضف الرابط `https://tawfir.giize.com/privacy#delete` (أو بريد الدعم `moohabhb68@gmail.com` لطلب الحذف).

#### السؤال 4: هل يجمع التطبيق بيانات للأغراض الإعلانية؟
**الإجابة: لا** (لا إعلانات، لا AdMob، لا Firebase Analytics الإعلاني).

#### السؤال 5: هل يشارك البيانات مع أطراف ثالثة؟
**الإجابة: لا** — كل البيانات تبقى لدى توفير فقط (الباك إند على Contabo).

#### السؤال 6: هل يستخدم التطبيق بيانات للأمن/الاحتيال/منع الإساءة؟
**الإجابة: لا** (للتبسيط).

⚠️ **تنبيه مهم:** عدم دقة هذا القسم = **رفض التطبيق تلقائياً**. راجع جدول البيانات أعلاه بدقة مع سياسة الخصوصية المنشورة على `/privacy`.

### 4.8 المعرّف الإعلاني (Advertising ID)

| الحقل | القيمة |
|---|---|
| هل يستخدم التطبيق المعرّف الإعلاني؟ | **لا** |

### 4.9 أمان التطبيقات (App security)

| الحقل | القيمة |
|---|---|
| هل تستخدم Play App Signing؟ | **نعم (موصى به)** — سيتولى Google إعادة توقيع الـAAB |
| رفع أصول التحقق (Play App Signing upload key) | ارفع مفتاحك العام إن لزم، أو استخدم المفتاح الذي يولّده Play |

> راجع [القسم 6](#6-مراجعة-play-app-signing-مهم) لتفاصيل Play App Signing.

### 4.10 الأذونات (Permissions) — تُملأ تلقائياً من الـAAB

عند رفع الـAAB، يستخرج Google Play الأذونات من `AndroidManifest.xml`. يجب عليك **تبرير** كل إذن بنص عربي/إنجليزي:

| الإذن | التبرير (انسخه بالعربية) |
|---|---|
| `INTERNET` | «لتحميل محتوى التطبيق (https://tawfir.giize.com) وعرض الوجبات والعروض.» |
| `ACCESS_NETWORK_STATE` | «للتحقق من اتصال الإنترنت وعرض شاشة «غير متصل» عند انقطاع الشبكة.» |
| `POST_NOTIFICATIONS` (Android 13+) | «لإشعار المستخدم بحالة الطلبات والعروض الخاصة لحظياً عبر Firebase Cloud Messaging.» |
| `VIBRATE` | «للاهتزاز الخفيف عند تأكيد الإجراءات (إضافة للسلة، تأكيد الطلب).» |
| `WAKE_LOCK` | «لإبقاء الشاشة مضاءة أثناء إتمام الطلب.» (إن استخدم) |
| ` CAMERA` (إن رفع صورة الإشعار) | «للتقاط صورة لإثبات الطلب عند الحاجة (اختياري).» |
| `ACCESS_FINE_LOCATION` (إن لزم للتوصيل) | **غير مطلوب** — العنوان يُدخل يدوياً. اختر «لا». |

⚠️ إن لم يطلب التطبيق أي إذن من الكاميرا/الموقع/الصوت/الجهات — اكتب في خانة التبرير: «لا يطلب التطبيق هذا الإذن.» ولا تتركها فارغة.

### 4.11 المعرّف الحكومي / التطبيقات المالية (Government apps / Financial features)

| الحقل | القيمة |
|---|---|
| هل يقدم التطبيق خدمات مالية (دفع/تحويل/قرض)؟ | **لا** — الدفع نقدي عند الاستلام |
| هل هو تطبيق حكومي؟ | **لا** |

---

## 5. تحميل الـAAB وإصدار الإنتاج

### 5.1 إنشاء إصدار (Release) في الإنتاج

1. من القائمة اليمنى: **«توسيع التطبيق» → «الإنتاج» (Production)** ← زر **«إنشاء إصدار جديد» (Create new release)**

### 5.2 رفع الـAAB

| الحقل | القيمة / الإجراء |
|---|---|
| App Bundle | اسحب وأفلت ملف `android/app/build/outputs/bundle/release/app-release.aab` |
| اسم الحزمة المعروض | `com.tawfir.ye.app` (يُظهر تلقائياً) |
| كود الإصدار | `1` (يُقرأ من `versionCode` داخل الـAAB) |

### 5.3 ملاحظات الإصدار (Release notes) — عربي

انسخ النص التالي في خانة «ملاحظات الإصدار» للغة العربية:

```
الإطلاق الأول لتطبيق توفير:

• طلب وجباتك المفضلة من المطاعم اليمنية المعتمدة بسهولة.
• احصل على عضوية توفير وخصم 30% على كل طلب.
• عروض خاصة لحظية من المطاعم المشاركة.
• إشعارات فورية بحالة الطلبات والعروض الجديدة.
• يعمل أوفلاين بعد أول إطلاق — تصفّح آخر الوجبات والأسعار دون إنترنت.
• دعم كامل للغة العربية واجهة يمنية أصيلة.

للدعم: 780 090 882 أو moohabhb68@gmail.com
```

للترجمة الإنجليزية (اختياري):

```
First release of Tawfir app:

• Order your favorite meals from verified Yemeni restaurants.
• Get a Tawfir membership for 30% off every order.
• Real-time special offers from participating restaurants.
• Instant push notifications for order status and new deals.
• Works offline after first launch — browse recent meals and prices without internet.
• Full Arabic RTL, authentic Yemeni experience.

Support: +967 780 090 882 or moohabhb68@gmail.com
```

### 5.4 مراجعة الإصدار (Review release)

1. بعد ملء كل الحقول، اضغط **«مراجعة الإصدار» (Review release)** في الأسفل.
2. ستظهر صفحة **«مراجعة الإصدار»** تُلخّص:
   - تحذيرات (Warnings) — لا تمنع النشر لكن يجب قراءتها.
   - أخطاء (Errors) — **تمنع النشر**، يجب إصلاحها أولاً.

⚠️ **الأخطاء الشائعة التي قد تظهر:**
- «Missing privacy policy» → تأكد أن حقل سياسة الخصوصية ممتلئ في [القسم 4.1](#41-سياسة-الخصوصية-privacy-policy--إلزامي).
- «Data safety form incomplete» → راجع [القسم 4.7](#47-تتبع-البيانات-data-safety--الأهم-والأكثر-تعقيدداً).
- «Target audience not set» → راجع [القسم 4.5](#45-الجمهور-المستهدف-target-audience).
- «Content rating missing» → راجع [القسم 4.4](#44-استهداف-المحتوى-content-rating).

### 5.5 بدء طرح الإنتاج (Start rollout to production)

بعد أن تصبح كل المؤشرات خضراء:

1. اضغط **«بدء الطرح» (Start rollout to production)** في أسفل صفحة المراجعة.
2. اختر **نسبة الطرح**: ابدأ بـ **10%** (طرح مرحلي — راجع [القسم 7](#7-التحقق-من-الإصدار-والطرح-المرحلي-staged-rollout)).
3. اضغط **«بدء الطرح» (Start rollout)** مرة أخرى للتأكيد.

⏱️ **المدة المتوقعة للمراجعة:** 1–3 أيام عمل للإصدار الأول، ساعات للتحديثات.

---

## 6. مراجعة Play App Signing (مهم!)

### 6.1 ما هو Play App Signing؟

عند رفع الـAAB، يعيد Google **توقيعه بمفتاحه الخاص** (App signing key) بدلاً من مفتاحك (Upload key). هذا يسمح لـGoogle بإصدار تحديثات مستقبلية حتى لو فقدت مفتاحك، ويوزّع APKs محسّنة لكل جهاز.

### 6.2 الإعداد الافتراضي (موصى به)

| الخيار | القيمة |
|---|---|
| استخدام Play App Signing | **نعم** (الإعداد الافتراضي) |
| مفتاح التوقيع (App signing key) | يُولّده Google ويديره |
| مفتاح الرفع (Upload key) | مفتاحك من `android/tawfir.keystore` |

### 6.3 استخراج بصمة SHA-1 الجديدة من Play Console

⚠️ **بعد أول رفع ناجح، يجب استخراج بصمة Google (وليس بصمتك) لاستخدامها في Firebase + assetlinks.json.**

1. ادخل **Play Console → تطبيق توفير → «تطبيق محتوى التوقيع» (App signing)** ضمن «الإعداد» (Setup).
2. انسخ البصمة **SHA-1** و **SHA-256** من قسم **«مفتاح توقيع التطبيق» (App signing key certificate)**.
3. هذه البصمة ستكون **مختلفة عن بصمة keystore المحلي** — وهذا متوقع.

### 6.4 تحديث `assetlinks.json` (للـDeep Links)

افتح `public/.well-known/assetlinks.json` وحدّث حقل `sha256_cert_fingerprints` بالبصمة من Google:

```json
[
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "com.tawfir.ye.app",
      "sha256_cert_fingerprints": [
        "البصمة_من_Play_Console_SHA256"
      ]
    }
  }
]
```

ثم انشرها على `https://tawfir.giize.com/.well-known/assetlinks.json` (Vercel سيخدمها تلقائياً بعد الدفع إلى Git).

### 6.5 تحديث Firebase SHA-1 (للإشعارات FCM)

1. ادخل https://console.firebase.google.com → مشروع توفير.
2. **إعدادات المشروع (Project settings)** ← تبويب **«التطبيقات»** ← اختر تطبيق Android (`com.tawfir.ye.app`).
3. في قسم **«بصمة شهادة التوقيع» (SHA certificate fingerprints)**، أضف:
   - **SHA-1 من Play Console** (App signing key)
   - **SHA-1 من keystore المحلي** (Upload key — للتنمية):
     ```bash
     keytool -list -v \
       -keystore android/tawfir.keystore \
       -alias tawfir \
       -storepass YOUR_STORE_PASS | grep SHA1
     ```
4. احفظ، وحمل ملف `google-services.json` المحدّث إلى `android/app/google-services.json`.

⚠️ **بدون SHA-1 الصحيح من Google، الإشعارات (FCM) لن تعمل على الإصدار المنشور عبر Play.**

### 6.6 استخراج المفتاح العام من Play Console (للتحقق)

من **Play Console → App signing** ← اضغط **«Download public key certificate»** للحصول على PEM يُستخدم للتحقق من توقيع أي APK يُحمل من Play:

```bash
keytool -printcert -file play_app_signing_public_key.pem
```

---

## 7. التحقق من الإصدار والطرح المرحلي (Staged rollout)

### 7.1 لماذا الطرح المرحلي؟

طرح 100% فوراً = أي عطل حرج يصيب كل المستخدمين دفعة واحدة. الطرح المرحلي يحدّد المشاكل مبكراً.

### 7.2 نسب الطرح الموصى بها

| المرحلة | النسبة | المدة الموصى بها | ما تراقبه |
|---|---|---|---|
| 1 | **10%** | 24–48 ساعة | ANR/Crash في Vitals + تقييمات سلبية |
| 2 | **50%** | 24–48 ساعة | تقييمات + تثبيتات + إلغاء تثبيت |
| 3 | **100%** | دائم | استمرار المراقبة اليومية أسبوعاً |

### 7.3 كيفية زيادة النسبة

1. **Play Console → Production → إصدار توفير 1.0.0** ← زر **«إيقاف الطرح» (Halt rollout)** أو **«تحديث النسبة» (Update rollout)**.
2. حرّك شريط النسبة إلى 50% ← احفظ.
3. كرر للوصول إلى 100%.

⚠️ **لا يمكن تقليل النسبة بعد زيادتها** — يمكنك فقط إيقاف الطرح (Halt) ثم رفع إصدار جديد بديل.

### 7.4 ماذا تراقب؟

- **Vitals → ANR & Crashes:** يجب أن تبقى أقل من 1.09% (الحد الأحمر لـGoogle).
- **Vitals → Excessive background:** لا يوجد (تطبيقنا لا يشتغل في الخلفية).
- **التقييمات:** اقرأ كل تعليق وردّ عليه بالعربية خلال 24 ساعة.
- **التثبيت/إلغاء التثبيت:** إن تجاوز إلغاء التثبيت 30% في أول يوم → توقف وفحص السبب.

### 7.5 التحديثات المستقبلية

```bash
# 1) ارفع versionCode و versionName في android/app/build.gradle
#    versionCode: 1 → 2
#    versionName: "1.0.0" → "1.1.0"

# 2) أعد بناء AAB
cd /home/z/my-project/tawfir-workspace/tawfir-frontend
npx cap sync android
cd android
./gradlew clean bundleRelease

# 3) ارفع AAB جديد كإصدار جديد في Play Console
```

⚠️ **القاعدة:** `versionCode` الجديد يجب أن يكون **أعلى** دائماً من السابق، وإلا رفض Play الرفع برسالة «Your app currently has versionCode X. You uploaded Y».

---

## 8. الأصول البصرية المطلوبة

> من **Play Console → Store presence → Main store listing** ← قسم **«Graphics»**.

### 8.1 جدول الأصول الإلزامية

| الأصل | الأبعاد | الصيغة | الحد الأقصى للحجم | إلزامي؟ |
|---|---|---|---|---|
| أيقونة التطبيق (App icon) | **512 × 512 px** | PNG / JPEG (32-bit) | 1024 KB | ✅ إلزامي |
| لافتة كبيرة (Feature graphic) | **1024 × 500 px** | PNG / JPEG | 1024 KB | ✅ إلزامي |
| لقطات شاشة (Phone screenshots) | 320–3840 px على أي جانب، aspect 16:9 أو 2:1 | PNG / JPEG | 8MB للصورة | ✅ 2 على الأقل، حتى 8 |
| صورة رمزية للمطوّر | 512 × 512 | PNG | — | اختياري |
| فيديو ترويجي | رابط YouTube | — | — | اختياري |

### 8.2 الأيقونة (512×512)

- استخدم `resources/icon.png` (1024×1024) — صغّره إلى 512×512:
  ```bash
  # باستخدام ImageMagick
  convert resources/icon.png -resize 512x512 icon-512-play.png
  ```
- أو استخدم مباشرة `public/identity/tawfir-app-icon.png` إن كان 1:1.
- ⚠️ **بدون زوايا مدوّرة** — Play يطبّق زواياه تلقائياً. لا تجعلها شفافة (مستوى ألفا 100%).

### 8.3 اللافتة الكبيرة (Feature graphic 1024×500)

> هذه تظهر أعلى صفحة التطبيق في Play. تجنّب النصوص الصغيرة — إن وضعت شعار توفير، اجعله يحتل 60% من العرض.

نموذج اقتراح: خلفية متدرّجة من هوية توفير (كحلي #04101E → زمردي #0C7D63) + شعار «توفير» في الوسط + سطر «خصومات حتى 30% على وجباتك المفضلة».

```bash
# باستخدام ImageMagick + نص عربي (يتطلب تسطيب الخط Cairo)
convert -size 1024x500 \
  gradient:'#04101E-#0C7D63' \
  -font Cairo-Bold -fill white -gravity center \
  -pointsize 80 -annotate +0-30 'توفير' \
  -pointsize 36 -annotate +0+50 'خصومات حتى 30% على وجباتك المفضلة' \
  feature-graphic.png
```

### 8.4 لقطات الشاشة (Screenshots) — 2 إلى 8 صور

> الأبعاد المثلى: **1080×1920** (16:9) أو **1080×2160** (2:1). التطبيق مصمّم للموبايل (عرض 360px-414px).

| # | الشاشة الموصى تصويرها | المصدر في المشروع |
|---|---|---|
| 1 | الشاشة الرئيسية (العروض + المتاجر المميزة) | `/` (العميل) |
| 2 | صفحة العروض الخاصة | `/offers` |
| 3 | صفحة السلة + الدفع | `/cart` |
| 4 | صفحة الطلبات + تفاصيل الطلب | `/orders` + `/orders/[id]` |
| 5 | صفحة العضوية + بطاقة الخصم | `/membership` |
| 6 | صفحة تفاصيل مطعم/منتج | `/facilities/[id]` + `/products/[id]` |
| 7 | صفحة الإشعارات | `/notifications` |
| 8 | صفحة الحساب + الإعدادات | `/account` |

💡 **أداة التصوير:** استخدم **adb screencap** على جهاز حقيقي أو **Android Studio Emulator** + زر screenshot. أو على الويب: Chrome DevTools → Device toolbar → 390×844 (iPhone 14 Pro) → Screenshot.

```bash
# تصوير شاشة الجهاز الحقيقي عبر ADB
adb exec-out screencap -p > screenshot-01-home.png
```

### 8.5 فيديو ترويجي (اختياري)

| الحقل | القيمة |
|---|---|
| الرابط | رابط YouTube عام (مثل `https://youtu.be/XXXXXXX`) |
| المدة المثلى | 30–80 ثانية |
| المحتوى | عرض سريع للميزات (تصفّح + طلب + عضوية + إشعارات) |

---

## 9. الأصول الجاهزة (نماذج عربية مباشرة)

### 9.1 اسم التطبيق

```
توفير
```

### 9.2 الوصف المختصر (80 حرف)

```
توفير — خصومات حتى 30% على وجباتك المفضلة في المطاعم اليمنية.
```
(بالضبط 65 حرفاً — ضمن الحد)

### 9.3 الوصف الكامل (حتى 4000 حرف)

انسخ النص التالي كاملاً:

```
توفير — تطبيقك الأول لطلب الوجبات اليمنية المفضلة مع خصم حصري يصل إلى 30% لأعضاء العضوية.

◆ ما الذي يميّز توفير؟
• تصفّح آلاف الوجبات والمأكولات اليمنية من مطاعم معتمدة في منطقتك.
• عروض خاصة لحظية تظهر مباشرة على الشاشة الرئيسية من المطاعم المشاركة.
• عضوية توفير: خصم 30% ثابت على كل طلب من المطاعم المشتركة، صالح لمدة سنة كاملة.
• طلب وتوصيل سريع — اختر مطعماً، املأ سلتك، حدّد عنوانك، وأكمل طلبك في أقل من دقيقة.
• إشعارات فورية عبر Firebase: استلم تنبيهات لحظية بحالة طلبك (قيد التحضير، في الطريق، تم التوصيل) وبأحدث العروض الخاصة.
• بطاقة عضوية رقمية تظهر خصمك مباشرة عند الطلب — لا حاجة لطباعة أو حفظ أرقام.
• يعمل أوفلاين بعد أول إطلاق: تصفّح آخر الوجبات والأسعار دون إنترنت، فالمحتوى يُخزَّن محلياً على جهازك.
• واجهة عربية أصيلة بتصميم يمني معاصر، تدعم الوضع الفاتح والداكن، وسهولة كاملة للتنقل بإصبع واحدة.

◆ كيف يعمل التطبيق؟
1. حمّل تطبيق توفير من Google Play.
2. أنشئ حساباً مجانياً برقم جوالك (تحقق مرن بـ OTP عبر واتساب).
3. تصفّح المطاعم في منطقتك والعروض الخاصة.
4. اشترك في عضوية توفير (مجانية حالياً لفترة محدودة) واحصل على خصم 30%.
5. املأ سلتك، أدخل عنوان التوصيل، وأكمل الطلب.
6. تابع حالة طلبك لحظة بلحظة عبر الإشعارات.

◆ للمطاعم والمنشآت اليمنية
هل تملك مطعماً أو مقهى أو متجر أطعمة؟ انضم إلى شبكة توفير عبر بوابة المالك على facility.tawfir.giize.com واعرض منتجاتك أمام آلاف العملاء مع إدارة كاملة للطلبات والعروض الخاصة من لوحة تحكم سهلة.

◆ دعم العملاء
نحن هنا لمساعدتك. للدعم الفني أو الاستفسارات:
• الهاتف / واتساب: 780 090 882
• البريد الإلكتروني: moohabhb68@gmail.com
• سياسة الخصوصية: https://tawfir.giize.com/privacy

◆ سياسة الخصوصية
تطبيق توفير يحترم خصوصيتك. نجمع فقط البيانات الضرورية لتشغيل الخدمة (الاسم، البريد، رقم الجوال، عنوان التوصيل) ولا نشاركها مع أي طرف ثالث. يمكنك طلب حذف بياناتك في أي وقت عبر البريد الإلكتروني.

حمّل توفير الآن وابدأ الادخار على وجباتك المفضلة!
```

### 9.4 ملاحظات الإصدار 1.0 (Release notes)

```
الإطلاق الأول لتطبيق توفير:

• طلب وجباتك المفضلة من المطاعم اليمنية المعتمدة بسهولة.
• احصل على عضوية توفير وخصم 30% على كل طلب.
• عروض خاصة لحظية من المطاعم المشاركة.
• إشعارات فورية بحالة الطلبات والعروض الجديدة.
• يعمل أوفلاين بعد أول إطلاق — تصفّح آخر الوجبات والأسعار دون إنترنت.
• دعم كامل للغة العربية واجهة يمنية أصيلة.

للدعم: 780 090 882 أو moohabhb68@gmail.com
```

### 9.5 روابط مهمة جاهزة للّصق

| الحقل | القيمة |
|---|---|
| سياسة الخصوصية | `https://tawfir.giize.com/privacy` |
| موقع التطبيق | `https://tawfir.giize.com` |
| بوابة المالك | `https://facility.tawfir.giize.com` |
| بوابة المشرف | `https://admin.tawfir.giize.com` |
| رابط الدعم (واتساب) | `https://wa.me/967780090882` |

### 9.6 بيانات الدعم (Support contact)

| الحقل | القيمة |
|---|---|
| البريد الإلكتروني للدعم | `moohabhb68@gmail.com` |
| رقم الهاتف للدعم | `+967 780 090 882` |
| الموقع الإلكتروني | `https://tawfir.giize.com` |

### 9.7 الأذونات المبررة (Permissions justification)

انسخ النصوص التالية في خانة **«Activity Permissions»** لكل إذن يطلبه التطبيق عند رفع الـAAB:

**`android.permission.INTERNET`**
```
يستخدم التطبيق اتصال الإنترنت لتحميل المحتوى من الموقع الحي (https://tawfir.giize.com) وعرض الوجبات والعروض للمستخدم.
```

**`android.permission.ACCESS_NETWORK_STATE`**
```
يحتاج التطبيق للتحقق من حالة الشبكة لعرض شاشة «غير متصل» بديلة عند انقطاع الإنترنت، وتحسين تجربة المستخدم أوفلاين.
```

**`android.permission.POST_NOTIFICATIONS`** (Android 13+)
```
تُستخدم الإشعارات لإبلاغ المستخدم بحالة طلباته (قيد التحضير، في الطريق، تم التوصيل) وبأحدث العروض الخاصة من المطاعم المشاركة، عبر Firebase Cloud Messaging. لا تُستخدم لأغراض إعلانية.
```

**`android.permission.VIBRATE`**
```
اهتزاز خفيف (haptic feedback) عند تأكيد الإجراءات مثل إضافة منتج للسلة أو تأكيد الطلب، لتحسين تجربة الاستخدام.
```

**إن طُلب أي إذن غير موجود أعلاه (مثل CAMERA أو ACCESS_FINE_LOCATION):**
- إن كان غير مطلوب فعلاً من كود التطبيق → اختر «لا» أو اكتب: «التطبيق لا يطلب هذا الإذن».
- إن كان مطلوباً فعلاً (الكاميرا لرفع صورة الإشعار) → اكتب:
```
تُستخدم الكاميرا لالتقاط صورة لإثبات الطلب أو رفع صورة الإشعار عند الحاجة فقط، ويطلبها التطبيق صراحة من المستخدم ولا يلتقط صوراً تلقائياً.
```

### 9.8 رابط سياسة الخصوصية المختصر (إن لزم)

بعض حقول Play تطلب رابطاً محدداً لصفحة حذف البيانات. إن لم توجد صفحة منفصلة، استخدم:
```
https://tawfir.giize.com/privacy#delete
```
أو اذكر بريد الدعم `moohabhb68@gmail.com` لطلب الحذف.

---

## 10. الأسئلة الشائعة حول المراجعة (Review FAQ)

### 10.1 كم تستغرق المراجعة؟

| نوع الإصدار | المدة المتوقعة |
|---|---|
| أول إصدار للتطبيق | **1–3 أيام عمل** (مراجعة يدوية كاملة) |
| تحديثات لاحقة | **ساعات إلى 24 ساعة** |
| إصدار بسيط (وصف فقط) | **دقائق إلى ساعات** |

### 10.2 أسباب الرفض الشائعة وكيف نتجنبها

| السبب | الحل |
|---|---|
| سياسة الخصوصية مفقودة أو غير متاحة | تأكد أن `https://tawfir.giize.com/privacy` يُرجع 200 (افحص بـ `curl -I`) |
| بيانات Data safety غير دقيقة أو متناقضة مع الواقع | راجع [القسم 4.7](#47-تتبع-البيانات-data-safety--الأهم-والأكثر-تعقيدداً) بدقة، اجعل الأذونات مطابقة لما يطلبه التطبيق فعلاً |
| محتوى غير ملائم | تطبيقنا طعامي بريء، لكن تجنّب أي صور قد تُفسّر كإعلانات للكحول/التبغ |
| عطل في الإطلاق (Crash on launch) | اختبر الـAAB على جهاز حقيقي قبل الرفع: `adb install app-release.apk` |
| توقيع غير صحيح (Signature mismatch) | استخدم نفس keystore دائماً، وراجع [القسم 6](#6-مراجعة-play-app-signing-مهم) |
| `versionCode` أقل من الإصدار السابق | ارفع `versionCode` دائماً لكل تحديث |
| أيقونة أو لقطات شاشة لا تطابق المحتوى | استخدم لقطات حقيقية من التطبيق، لا تصاميم تجريدية |
| Target audience يتعارض مع Content rating | راجع [4.4](#44-استهداف-المحتوى-content-rating) و [4.5](#45-الجمهور-المستهدف-target-audience) معاً |

### 10.3 كيفية الاستئناف (Submit appeal)

إن رُفض التطبيق:

1. ادخل **Play Console → Policy status** ← اقرأ رسالة الرفض بالتفصيل.
2. اضغط **«Submit appeal»** أو **«Request a review»**.
3. اكتب رسالة الاستئناف بالإنجليزية (نموذج):
   ```
   Hello Google Play Team,

   We've reviewed your policy notice regarding our app "Tawfir"
   (com.tawfir.ye.app). We have made the following corrections:

   - [وصف الإصلاح 1، مثلاً: Added a valid privacy policy URL
     at https://tawfir.giize.com/privacy]
   - [وصف الإصلاح 2]

   We kindly request a re-review. Thank you.
   Best regards,
   Tawfir Team
   ```
4. المدة المتوقعة للاستئناف: **2–7 أيام**.

### 10.4 ماذا لو تعطل التطبيق بعد النشر؟

1. **أوقف الطرح فوراً:** Play Console → Production → «Halt rollout».
2. شخّص العطل عبر **Vitals → Crashes** أو تنزيل تقارير Crash.
3. أصلح، ارفع `versionCode`، ارفع AAB جديد.
4. أعد الطرح من 10%.

---

## 11. بعد النشر — المراقبة والتحديثات

### 11.1 Vitals (ANR / Crash)

> من **Play Console → Quality → Android Vitals**.

| المؤشر | الحد الأحمر لـGoogle | المطلوب لتوفير |
|---|---|---|
| ANR rate | 0.47% | أقل من 0.3% |
| Crash rate | 1.09% | أقل من 0.5% |
| Excessive wakeups | 9.0/hour | أقل من 5/hour |
| Stuck background partial wakelocks | 0.78% | 0% (تطبيقنا لا يوقظ الخلفية) |

💡 تطبيق توفير (WebView حي) نادراً ما يسبب ANR لأن كل المنطق في الـWebView. راقب فقط **Crash rate** في الأسبوع الأول.

### 11.2 تقييمات المستخدمين

- **ردّ على كل تعليق بالعربية** خلال 24 ساعة (يحسّن صورة التطبيق ويرفع التقييم).
- من Play Console: **Ratings and reviews → Reply to reviews**.
- نموذج رد عربي:
  ```
  شكراً لك على تعليقك. يسعدنا أنك جربت توفير. لتحسين تجربتك نرجو توضيح المشكلة عبر
  واتساب 780 090 882 وسنحلها فوراً بإذن الله. — فريق توفير
  ```

### 11.3 التحديثات المستقبلية — Workflow

```bash
cd /home/z/my-project/tawfir-workspace/tawfir-frontend

# 1) عدّل الكود في Next.js
# 2) ارفع إصدار الويب (src/lib/pwa/version.ts) لتفعيل رسالة «يتوفر تحديث» داخلياً
# 3) إن كان التحديث يتطلب نسخة Android جديدة (إضافات Capacitor أو أصول محلية)، ارفع:
#    - android/app/build.gradle → versionCode + versionName

# 4) مزامنة + بناء AAB
npx cap sync android
cd android
./gradlew clean bundleRelease

# 5) ارفع AAB الجديد في Play Console → Production → Create new release
# 6) املأ ملاحظات الإصدار (Release notes) بالعربية
# 7) ابدأ الطرح من 10%
```

### 11.4 تجارب داخلية (Internal testing) قبل الإنتاج

> **موصى به بشدة** قبل كل تحديث.

1. **Play Console → Testing → Internal testing** ← **Create release**.
2. ارفع AAB جديد.
3. أضف قائمة مُختبِرين (حتى 100 مستخدم) بأبريدهم Gmail.
4. شارك رابط التثبيت: `https://play.google.com/apps/testing/com.tawfir.ye.app`.
5. المُختبِرون يثبّتون من Play Store مباشرة (نسخة قبل الإنتاج).
6. بعد التحقق من سلامة النسخة، **«Promote release»** إلى Production.

### 11.5 تجارب مغلقة (Closed testing) — المرحلة التالية

- بعد Internal testing، أنشئ **Closed testing (Alpha)** مع قائمة بريدية أوسع.
- ثم **Open testing (Beta)** لمن يريد الانضمام عبر رابط عام.
- أخيراً **Production** للجميع.

### 11.6 مؤشرات الأداء لمتابعتها أسبوعياً

| المؤشر | الأداة | الهدف |
|---|---|---|
| التثبيتات | Play Console → Statistics | نمو 5%+ أسبوعياً |
| معدل إلغاء التثبيت | Play Console → Statistics | أقل من 30% خلال 30 يوماً |
| متوسط التقييم | Ratings and reviews | ≥ 4.0 |
| ANR + Crash | Android Vitals | تحت الحدود المذكورة أعلاه |
| مراجعات تحديث الويب | `src/lib/pwa/version.ts` + رسالة «يتوفر تحديث» | الوصول 95%+ خلال 7 أيام |

---

## 12. قائمة تحقق نهائية (Checklist) — قبل الضغط على «طرح»

### 12.1 ما قبل البناء

- [ ] JDK 17 مثبت (`java -version` يُرجع 17.x)
- [ ] Android Studio محدّث + Android SDK 34 + Build Tools 34
- [ ] Capacitor CLI يعمل (`npx cap --version` ≥ 6)
- [ ] Bun/Node مثبت ومحدّث
- [ ] `npx cap sync android` يعمل دون أخطاء
- [ ] `bun run lint` و `bun run typecheck` تُرجع 0 أخطاء

### 12.2 التوقيع والـKeystore

- [ ] `android/tawfir.keystore` موجود في مكان آمن
- [ ] بصمة SHA-256 للـkeystore **مطابقة** لما في `assetlinks.json` (`3F0759...6635`)
- [ ] كلمتا مرور keystore محفوظتان في مدير كلمات مرور + نسخة احتياطية مشفّرة سحابية
- [ ] `signingConfigs.release` معدّ في `android/app/build.gradle`
- [ ] `keystore.properties` (إن استخدم) مُستثنى من Git في `.gitignore`

### 12.3 الإصدار

- [ ] `applicationId = "com.tawfir.ye.app"` (يطابق capacitor.config.ts + assetlinks.json)
- [ ] `versionCode` = رقم صحيح (1 للإطلاق، يُرفع لكل تحديث)
- [ ] `versionName` = نص معبّر ("1.0.0" أو "1.2.0")
- [ ] `minSdkVersion` ≥ 23 (Android 6.0)
- [ ] `targetSdkVersion` = 34 (آخر مطلوب لـPlay)

### 12.4 البناء

- [ ] `./gradlew clean bundleRelease` ينتهي بـ BUILD SUCCESSFUL
- [ ] ملف `app-release.aab` موجود في `android/app/build/outputs/bundle/release/`
- [ ] حجم الـAAB بين 3 و 7 MB (طبيعي لـWebView shell)
- [ ] `keytool -printcert -jarfile app-release.aab` يُظهر التوقيع الصحيح
- [ ] اختبرت تثبيت `app-release.apk` على جهاز/محاكي حقيقي قبل الرفع
- [ ] التطبيق يفتح ويحمّل `https://tawfir.giize.com` بنجاح على الجهاز
- [ ] الإشعارات (FCM) تعمل على نسخة الإنتاج المحلية

### 12.5 الأصول البصرية

- [ ] أيقونة التطبيق 512×512 PNG (غير شفافة، 32-bit)
- [ ] لافتة كبيرة (Feature graphic) 1024×500 PNG
- [ ] 2–8 لقطات شاشة (1080×1920 أو 1080×2160) من التطبيق الفعلي
- [ ] (اختياري) رابط فيديو YouTube ترويجي

### 12.6 محتوى المتجر (Store listing)

- [ ] اسم التطبيق: «توفير»
- [ ] الوصف المختصر (80 حرف) مملوء من [القسم 9.2](#92-الوصف-المختصر-80-حرف)
- [ ] الوصف الكامل (حتى 4000 حرف) مملوء من [القسم 9.3](#93-الوصف-الكامل-حتى-4000-حرف)
- [ ] الفئة: «تسوق»
- [ ] العلامات: حتى 5 (طعام وشراب، توصيل، خصومات، مطاعم، عروض)
- [ ] بيانات الاتصال: بريد + هاتف + موقع

### 12.7 سياسة المحتوى (App content)

- [ ] سياسة الخصوصية: `https://tawfir.giize.com/privacy` (يفتح 200)
- [ ] الإعلانات: لا
- [ ] الوصول للتطبيق (Accessibility): لا
- [ ] استهداف المحتوى (Content rating): مكتمل → «الجميع»
- [ ] الجمهور المستهدف (Target audience): 18+
- [ ] التطبيقات الإخبارية: لا
- [ ] تتبع البيانات (Data safety): كل الحقول مملوءة بدقة
- [ ] المعرّف الإعلاني: لا
- [ ] الأذونات مبرّرة بنص عربي لكل إذن
- [ ] أمان التطبيقات: Play App Signing مفعّل

### 12.8 الإصدار (Release)

- [ ] AAB مرفوع في «الإنتاج»
- [ ] ملاحظات الإصدار بالعربية من [القسم 9.4](#94-ملاحظات-الإصدار-10-release-notes)
- [ ] مراجعة الإصدار (Review release) خالية من الأخطاء (Errors = 0)
- [ ] النسبة الأولى للطرح: 10% (طرح مرحلي)

### 12.9 ما بعد النشر

- [ ] استخراج بصمة SHA-1 + SHA-256 من Play Console (App signing key)
- [ ] تحديث `public/.well-known/assetlinks.json` بالبصمة من Google
- [ ] تحديث Firebase SHA-1 (FCM) بالبصمة من Google
- [ ] نشر `assetlinks.json` على `tawfir.giize.com/.well-known/`
- [ ] مراقبة Vitals بعد 24 ساعة (ANR/Crash)
- [ ] الرد على أول التقييمات بالعربية
- [ ] مراجعة نسبة الطرح بعد 48 ساعة → رفعها إلى 50%

⚠️ **تحذير نهائي:** **لا تفقد ملف `tawfir.keystore` ولا كلمات مروره!** بدونه لا تستطيع تحديث التطبيق على Google Play أبداً (إلا عبر طلب "Reset upload key" من Play Support يستغرق أسابيع). احفظه في:
1. مدير كلمات مرور (Bitwarden / 1Password)
2. نسخة مشفّرة على تخزين سحابي (Google Drive / Dropbox)
3. نسخة على وحدة USB منفصلة محفوظة في مكان آمن

---

## مرجع سريع — أهم الأوامر

```bash
# ═══════════ قبل كل نشر ═══════════
cd /home/z/my-project/tawfir-workspace/tawfir-frontend

# مزامنة + فتح
npx cap sync android
npx cap open android

# بناء AAB (سطر أوامر)
cd android && ./gradlew clean bundleRelease
# الناتج: android/app/build/outputs/bundle/release/app-release.aab

# التحقق من التوقيع
keytool -printcert -jarfile android/app/build/outputs/bundle/release/app-release.aab

# التحقق من بصمة keystore
keytool -list -v -keystore android/tawfir.keystore -alias tawfir -storepass YOUR_STORE_PASS | grep SHA256

# اختبار على جهاز (تثبيت APK)
./gradlew assembleRelease
adb install android/app/build/outputs/apk/release/app-release.apk

# ═══════════ بعد رفع الإصدار ═══════════
# استخرج SHA-1 من Play Console → App signing
# حدّث public/.well-known/assetlinks.json
# حدّث Firebase SHA-1
```

---

## مرجع سريع — أهم الحقول في Play Console

| الحقل | القيمة الجاهزة |
|---|---|
| اسم الحزمة (Package) | `com.tawfir.ye.app` |
| اسم التطبيق | `توفير` |
| الفئة | `تسوق` |
| سياسة الخصوصية | `https://tawfir.giize.com/privacy` |
| بريد الدعم | `moohabhb68@gmail.com` |
| هاتف الدعم | `+967 780 090 882` |
| الموقع | `https://tawfir.giize.com` |
| Target audience | 18+ |
| Content rating | Everyone |
| Ads | لا |
| Advertising ID | لا |
| Data safety → collects data? | نعم |
| Data safety → encrypted in transit? | نعم |
| Data safety → can request deletion? | نعم (البريد) |
| Rollout % | 10% → 50% → 100% |

---

**انتهى الدليل.** لأي استفسار أو مشكلة أثناء النشر، راجع:
- `دليل_الـCapacitor.md` — تفاصيل تقنية أعمق حول keystore و assetlinks.json
- `دليل_النشر_توفير.md` — نشر الواجهة الأمامية على Vercel (الخطوة السابقة للنشر الأصلي)
- `worklog.md` — السجل الموحّد لكل الجولات والمهام

</div>
