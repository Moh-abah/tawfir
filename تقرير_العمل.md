# 📄 تقرير العمل — تجهيز فرونت اند «توفير» وتنفيذ التعديلات المطلوبة
**التاريخ**: 18 سبتمبر 2026 · **البيئة**: Next.js 16 + App Router + Tailwind 4 + shadcn/ui (المنفذ 3000) · **الباك اند الحي**: https://api.tawfir.giize.com

---

## ١) ملخص تنفيذي
تم تحميل مستودع الفرونت اند من GitHub (`Moh-abah/tawfir`) وتشغيله في بيئة العمل وربطه بالباك اند الحي، ثم **تنفيذ كامل المتطلبات المطلوبة في بوابتي التاجر والمناديب** (مع تحسينات موازية في واجهات العميل والإدارة) واختبارها فعلياً عبر متصفح حي ضد الباك اند الحقيقي. الفرونت اند **جاهز للتشغيل الإنتاجي** مع كل ما يوفره الباك اند الحالي، مع حلول أمامية مؤقتة موثقة لنقصين في الخادم (رفع صورة ما قبل المصادقة + استقبال الإحداثيات عند التسجيل) فُصّلا في `تقرير_تعديلات_الباك_اند.md` لوكيل الباك اند.

---

## ٢) تجهيز بيئة العمل
- استنساخ المستودع ودمجه في بيئة Next.js 16 (src/public/scripts/resources/native-shell + الإعدادات + workflows).
- تثبيت 307 حزمة عبر bun، تشغيل خادم التطوير (جاهز خلال ~1 ثانية).
- تحديث عقد OpenAPI الحي (`openapi_live.json` — 123 مساراً) وفحص حي للنقاط الحرجة: `/health` ✅ · `uploads` بلا توكن = 401 (جذر مشكلة رفع الصورة) · `otp/request` = 200 · تكرار جوال مندوب = 409 عربي · `regions` ✅.

---

## ٣) التعديلات المنجزة (مقابل كل متطلب حرفياً)

### بوابة التاجر `/owner/register`
| المتطلب | التنفيذ | الحالة |
|---|---|---|
| مقهى → **كافتيريا** | تعديل TYPE_LABEL + FILTER_CHIPS (القيمة البرمجية `cafe` محفوظة) | ✅ مختبر بصرياً |
| **ساعات العمل بقائمة/تشيك بوكس** (لا حقل كتابة) | مكوّن `WorkingHoursSelector`: أيام كأزرار تحديد + أوقات كل 30 دقيقة + أزرار سريعة («كل الأيام» / «الرسمية السبت-الخميس») + ملخص حي «مفتوح السبت-الخميس من 08:00 إلى 23:00» → يولّد نصاً متوافقاً مع الباك اند | ✅ |
| **موقع المتجر بخيارين** | مكوّن `StoreLocationPicker`: (أ) «أنا داخل المتجر» زر GPS ديناميكي (أصلي داخل APK عبر Capacitor + مسار ويب) (ب) «أنا بعيد» → فتح تطبيق الخرائط للجهاز (Apple Maps على iPhone / Google Maps على أندرويد — **مجرّب: فتح خرائط قوقل على صنعاء فعلياً**) + خطوات موجهة (اضغط مطولاً على الموقع → انسخ الإحداثيات → عد والصق) + زر «الصق من الخريطة» يقرأ الحافظة ويستخرج الإحداثيات + كشف الإحداثيات المعكوسة (تلميح ذكي لليمن) + وضع تأكيد أخضر مع «عرض على الخريطة» | ✅ |
| **العنوان إلزامي** | zod `min(3)` + إزالة «اختياري» — مختبر: فرغ النموذج يظهر «العنوان مطلوب (٣ أحرف على الأقل)» | ✅ |
| **إصلاح رفع صورة المتجر** | السبب الجذري: uploads يتطلب توكن (401) قبل وجود حساب. الحل الأمامي: رصد 401 → بطاقة توجيه عربية + فتح حقل «ألصق رابط صورة» تلقائياً + إخفاء منطقة السحب (مختبر بملف حقيقي: البطاقة تظهر والرابط يعمل) + البند B-1 في تقرير الباك اند للحل النهائي | ✅ |
| **تحقق رقم الهاتف يتقبل المسافات** | مكوّن `PhoneInput` (تنسيق جميل أثناء الكتابة) + `validateYemeniPhone`/`normalizeYemeniPhone` (يقبل مسافات/شرطات/+967/0967/أرقام هندية) — مختبر حياً: «777 123 456» يُقبل ويُرسل «777123456» | ✅ |
| **كود تحقق رقم التاجر (OTP)** | تدفق كامل: Submit → `POST /otp/request` (واتساب، 200 حياً) → شاشة OTP: 6 خانات + رقم مقنّع 77\*\*\*\*\*56 + عدّاد من ttl_seconds + إعادة إرسال + «تغيير الرقم» يحفظ كل القيم → `POST /otp/verify` → **عندها فقط** يُنفّذ التسجيل. مختبر حياً: كود خاطئ يرد 422 «الكود غير صحيح. محاولات متبقية: 4» | ✅ |

### بوابة المناديب `/courier/register`
| المتطلب | التنفيذ | الحالة |
|---|---|---|
| **رقم الرخصة → رقم البطاقة الشخصية (إلزامي)** | تسمية جديدة + zod أرقام فقط 5-20 + placeholder + نص إرشادي «سيتطابق مع صورة الهوية المرفوعة» — يُرسل في نفس الحقل البرمجي | ✅ |
| **لوحة يمنية كاملة إلزامية** | مكوّن `YemenPlateField`: قائمة المحافظات (22 كوداً بالأسماء) + رقم 4-6 خانات + حرف عربي (17 حرفاً) + **بطاقة معاينة لوحة يمنية** («3-222156-أ») — لا يُرسل إلا مكتمل الأجزاء + البند B-3 للتحقق الخادمي | ✅ مختبر (عدن + 222156 + أ) |
| **إصلاح الرجوع من الخطوة 4** (فشل إنشاء الحساب) | ٣ مستويات: (١) زر ذكي: عند وجود حساب بالجلسة يتحول إلى **«متابعة إلى المستندات»** → دخول → تحديث البيانات المعدلة عبر `PATCH /courier/me` → الخطوة 4 (٢) كشف 409: بطاقة «هذا الجوال مسجل سابقاً» بخيارَي دخول/بريد آخر (٣) استئناف الجلسة عبر sessionStorage بعد التحديث — **مختبر حياً بثلاث سيناريوهات: رجوع→متابعة بلا خطأ، تحديث الصفحة→استئناف توست «استأنفنا تسجيلك»** | ✅ |
| **تصميم نيتف للموبايل** | إزالة التمدد (`wide`) + أزرار ملتفة (السبب الجذري) + شبكة المركبة مقيدة + break-words شامل — **مختبر: scrollWidth = clientWidth على 360px و390px في كل الخطوات، صفر تمدد أفقي** | ✅ |

### بقية البوابات (تزامنا مع المطلوب)
- **مقهى → كافتيريا**: 21 ملفاً/42 موضعاً في كل الشاشات (عميل/تاجر/إدارة/manifest) — مختبر: `مقاهي`/`كافيهات` = صفر في الواجهات (المتبقي أسماء متاجر فعلية من قاعدة البيانات مثل «مقهى القفول» — بيانات لا تُمس).
- **أذونات الموقع في APK**: أُصلحت شاشة الانتظار + `WaitMode` + تحديث نص `NSLocationWhenInUseUsageDescription` لسكربت iOS (يشمل البوابات الثلاث) + دليل شامل `docs/إصلاح-أذونات-الموقع-APK.md` (تشخيص سبب «تعذر الوصول إلى موقعك الحي» = APK قديم مبني قبل حقن الأذونات؛ خطوات إعادة البناء عبر GitHub Actions؛ 4 طرق تحقق يدوي بعد البناء؛ خطة التعافي بزر «افتح إعدادات التطبيق») — **التشخيص الجذري: أذونات FINE/COARSE و`cap sync` موجودة في الكود الحالي — يلزم إعادة بناء APK جديد فقط**.
- ملفات جديدة مشتركة: `src/lib/yemen.ts` (بيانات يمنية موحدة) + 4 مكونات قابلة لإعادة الاستخدام (`YemenPlateField` · `WorkingHoursSelector` · `StoreLocationPicker` · `PhoneInput`).

---

## ٤) الاختبار الشامل المنفّذ (متصفح حي ضد الباك اند الحقيقي)
| الاختبار | النتيجة |
|---|---|
| الصفحة الرئيسية + facilities + privacy + owner/login + courier/login | كلها 200، صفر أخطاء console |
| نموذج التاجر: تحقق حي (عنوان مطلوب/بريد/منطقة) | ✅ الرسائل العربية الصحيحة |
| هاتف بالمسافات «777 123 456» | ✅ مقبول ومُطبَّع |
| OTP: request حقيقي (200) + كود خاطئ (422 عربي بعدّاد) + رجوع يحفظ القيم | ✅ |
| محدد الموقع: فتح خرائط قوقل على صنعاء (تبويب جديد، النموذج سليم) + لصق إحداثيات → «تم تحديد موقع متجرك» | ✅ |
| رفع صورة حقيقية → 401 → بطاقة التوجيه + حقل الرابط | ✅ |
| ساعات العمل: أزرار الأيام + الملخص الحي «مفتوح السبت-الخميس…» (أصلحت خلل الزر السريع كان 5 أيام) | ✅ |
| المندوب: إنشاء حساب حي باللوحة «3-222156-أ» + بطاقة «01234567» → خطوة 4 | ✅ |
| **الرجوع من خطوة 4 → «متابعة إلى المستندات» بلا فشل** + تحديث الصفحة → استئناف | ✅ (المشكلة الجوهرية حُلّت) |
| صفر تمدد أفقي 360px/390px | ✅ (scrollWidth = clientWidth) |
| TypeScript `tsc --noEmit` | **صفر أخطاء** |
| ESLint | **0 أخطاء** (5 تحذيرات قديمة بملفات غير معدّلة) |
| dev.log | نظيف، لا أخطاء ترجمة/تشغيل |
| فحص بصري (VLM) لنماذج اللقطات (التسجيل/OTP/اللوحة/360px) | «نظيف، بلا تداخل، احترافي، نيتف» |

---

## ٥) المخرجات النهائية
| الملف | الوصف |
|---|---|
| `تقرير_تعديلات_الباك_اند.md` | **الموجه لوكيل الباك اند**: 10 بنود مرتبة بالأولوية (B-1 رفع قبل المصادقة، B-2 إحداثيات التسجيل، B-3 تحقق اللوحة/البطاقة، B-5 ربط OTP بالتسجيل…) + قيم اختبار جاهزة |
| `docs/إصلاح-أذونات-الموقع-APK.md` | دليل إصلاح أذونات الموقع/الإشعارات في APK (تشخيص + إعادة بناء + تحقق + iOS + تعافٍ) |
| `worklog.md` | سجل عمل ٦ مهام مفصّل (منسّق بين الوكلاء) |
| `سجل_التغييرات.md` | سجل تغييرات المستودع الأصلي (محدّث بالمستودع) |
| `openapi_live.json` | عقد الـ API الحي المحدّث |

---

## ٦) ملاحظات للمالك
1. **المعاينة**: افتح التطبيق من لوحة المعاينة (يمين الشاشة) أو زر «فتح في تبويب جديد». المسارات الجاهزة للتجربة: `/` · `/owner/register` (جرّب OTP برقمك الحقيقي — يصل واتساب) · `/courier/register` · `/facilities` (كافتيريات).
2. **الـAPK**: مشكلة أذونات الموقع **محلولة في الكود** — يلزم **إعادة بناء APK جديد** من فرع محدّث عبر GitHub Actions (الدليل في `docs/إصلاح-أذونات-الموقع-APK.md`).
3. **حلان مؤقتان أماميان** بانتظار الباك اند (يرفعهما تقرير الباك اند): رابط الصورة بدل الرفع قبل الدخول (B-1) + تخزين إحداثيات التسجيل محلياً حتى أول دخول (B-2) — كلاهما يعمل الآن ولا يمنع الإنتاج.
4. **حساب اختبار** أُنشئ حياً في الباك اند أثناء الاختبارات (courier.test874@example.com) — يمكن حذفه من لوحة الإدارة.
