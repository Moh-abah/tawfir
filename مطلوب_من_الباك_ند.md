# مطلوب من الباك إند — توفير

> قائمة جاهزة للإرسال لوكيل الباك إند — مرتبة بالأولوية.
> كل مواصفة تشمل: المسار + الطلب + الاستجابة + سبب الحاجة.
> تاريخ التوليد: الجولة 5 — **تحديث الجولة 6** (بعد التحقق الكامل ببيانات الأدمن الحقيقية:
> اكتُشف عطلان خادم حرجان + سلوكيات موثّقة).

---

## 🔴 الأولوية القصوى (قبل الإطلاق التسويقي)

### 1) إصلاح عطل 500 في الموافقة/الرفض على طلبات العضوية ⚠️ جديد (الجولة 6)

- **الحالة:** `PATCH /api/v1/admin/membership-requests/{id}/approve` و`.../reject`
  يرجعان **500 "Internal server error"** — مُختبر مباشرة على الإنتاج بجسم صحيح
  (`{"reason":"..."}`) وبلا جسم وبأي توكن أدمن صالح (توكن `admin@tawfir.giize.com`).
- **الأثر:** **الوظيفة الأساسية للأدمن معطّلة كلياً** — لا يمكن البتّ في أي طلب عضوية.
  الطلب المعلّق الحالي (id=1) عالق من الجولة 1.
- **المطلوب:** مراجعة سجل الخادم عند استدعاء الـ endpoint (على الأرجح استثناء غير
  معالَج عند توليد رقم العضوية أو إنشاء إشعار العميل أو كتابة rejection_reason)
  وإصلاحه.
- **للتحقق بعد الإصلاح:** `curl -X PATCH .../admin/membership-requests/1/approve -H "Authorization: Bearer <admin-token>"` → 200 مع MembershipRequestOut.

### 2) استرجاع ملفات صور إيصالات العضوية (seed) ⚠️ جديد (الجولة 6)

- **الحالة:** `/uploads/membership_receipts/seed_pending_demo.png` و
  `seed_approved_demo.png` ترجعان **404** من الخادم رغم أن سجلات DB تشير إليهما
  (`receipt_image_url` لطلبي العضوية 1 و2).
- **الأثر:** الأدمن يوافق/يرفض «على ثقة» بلا رؤية الإيصال. الواجهة تعرض الآن حالة
  خطأ عربية أنيقة بدل الصورة المكسورة.
- **المطلوب:** إعادة توليد ملفات seed للإيصالات (أو رفع إيصالات حقيقية لطلبات الاختبار).

### 3) POST /api/v1/uploads — رفع الصور (multipart)

- **الطلب:** `multipart/form-data` بحقل `file` (png/jpg/webp، حد 5MB) + حقل اختياري `folder` (`products` | `facilities`).
- **الاستجابة:** `201 → { "url": "/uploads/products/abc123.webp" }` (مسار نسبي كصور التحويل الحالية `uploads/membership_receipts/…` — الواجهة تحوّله تلقائياً عبر `resolveImageUrl`).
- **الأمان:** يتطلب توكن مالك/مشرف (HTTPBearer).
- **لماذا:** حالياً كل صور المنتجات/المنشآت `null` في قاعدة البيانات (102 منتج بلا صورة). صاحب المطعم اليمني لن يرفع صورة على استضافة خارجية ثم يلصق رابطاً — كل نماذج الواجهة تعمل اليوم بحقل رابط نصي كحل مؤقت (ImageUrlField بمعاينة). **جاهز في الواجهة:** فور توفر المسار يُستبدل الحقل برافع فعلي (drag&drop + ضغط client-side لأقصى 1200px) في مواضع: إنشاء/تعديل منتج، تعديل منشأة (مالك/أدمن)، تسجيل مالك.
- **اختياري (أفضل):** بدل المسار العام، قبول multipart مباشرة في `POST /owner/{fid}/products` و`PUT /owner/facility/{fid}` — نعمل بأي الخيارين.

### 4) POST /api/v1/auth/refresh — تحديث التوكن ⚠️ جديد (الجولة 6)

- **الحالة:** كل رموز JWT تنتهي بعد **900 ثانية (15 دقيقة)** ولا يوجد أي مسار refresh
  في OpenAPI. «تذكرني (7 أيام)» يحفظ توكن **منتهياً** — الجلسة الفعلية 15 دقيقة
  مهما كانت مدة الكوكي (مُختبر فعلياً: انقطعت جلسات التحقق الطويلة بالجولة 6).
- **الطلب:** `POST /api/v1/auth/refresh` body `{ "refresh_token": string }` (أو توكن
  الوصول الحالي في Authorization إن كان تصميمكم sliding).
- **الاستجابة:** `200 → { "access_token": string, "token_type": "bearer" }`.
- **لماذا:** تجربة الاستخدام تنهار بعد 15 دقيقة (خاصة المالك الذي يدير طلبات طوال
  اليوم). الواجهة جاهزة لدمجه في api-client تلقائياً عند 401.

## 🟠 الأولوية العالية (بعد الإطلاق بأقرب جولة)

### 5) إصلاح علة تسجيل WebSocket ⚠️ جديد (الجولة 6)

- **الحالة:** يبدو أن مدير اتصالات WS **يزيل تسجيل المستخدم كلياً عند إغلاق أي مقبس
  له** (وليس المقبس المغلق فقط).
- **الإثبات (مُختبر فعلياً):** مقبس نظيف يستقبل الدفع فوراً؛ بعد فتح أي مقبس ثانٍ
  لنفس المستخدم ثم إغلاقه، **يتوقف الدفع للمقبس الأول نهائياً** حتى اتصال جديد.
- **الأثر:** فتح التطبيق في تبويبين لنفس الحساب يكسر الإشعارات الفورية.
- **التخفيف المنفّذ في الواجهة:** اتصال واحد نظيف لكل جلسة (connect خامل أثناء
  CONNECTING) — لكن الأصل علة في الخادم.
- **المطلوب:** في مدير الاتصالات، إزالة تسجيل **المقبس المغلق فقط** من قائمة
  المستخدم (وليس مفتاح المستخدم كله)، والدفع لكل مقابس المستخدم النشطة.

### 6) إشعارات للأدمن + دفع فوري للمالك عند الطلب الجديد ⚠️ جديد (الجولة 6)

- **الحالة المؤكدة:**
  - **الأدمن:** لا يُنشأ أي إشعار للمشرفين إطلاقاً (طلب جديد/طلب عضوية معلّق/منشأة
    معلّقة) — جرس الأدمن يعرض 0 دائماً (مطابق للـ API).
  - **المالك:** إشعار «📦 طلب جديد» **يُنشأ في DB** لكن **لا يُدفع عبر WS** — المالك
    يراه بعد ≤30 ثانية (استطلاع) وليس لحظياً.
  - **العميل:** يعمل كاملاً (إنشاء + دفع فوري — مُتحقَّق: toast «✅ تم تأكيد طلبك»
    خلال ~2 ثانية).
- **المطلوب:** توليد إشعارات for-admins عند: طلب جديد (أي منشأة)، طلب عضوية جديد،
  منشأة جديدة بانتظار الموافقة + دفع WS للمالك عند الطلب الجديد.

### 7) PUT /api/v1/me/password — تغيير كلمة المرور

- **الطلب:** `{ "current_password": string, "new_password": string }` (min 8).
- **الاستجابة:** `200 → { "detail": "تم تغيير كلمة المرور" }` + خطأ 400 عربي عند خطأ الحالية.
- **لماذا:** حُذف قسم «أمان الحساب» من إعدادات الأدمن والمالك كاملاً (كان أزراراً وهمية toast بلا API) — الصفحتان تعرضان ملاحظة «غير متوفر حالياً». يُعاد بناؤه فوراً عند توفر المسار.

### 8) POST /api/v1/auth/forgot-password — إعادة تعيين كلمة المرور

- **الطلب:** `{ "email": string }` → إرسال رابط إعادة تعيين بالبريد (توكن صالح 30 دقيقة).
- **+ PUT /api/v1/auth/reset-password:** `{ "token": string, "new_password": string }`.
- **لماذا:** حُذف زر «نسيت كلمة المرور؟» الوهمي من صفحتي دخول الأدمن والمالك (كان toast «قريباً»). يُعاد في صفحات الدخول الثلاث عند التوفر.

### 9) توثيق WS في OpenAPI

- مسار `wss://api.tawfir.giize.com/api/v1/ws/notifications?token=` يعمل فعلياً (مُتحقَّق من طرف إلى طرف في الجولة 6: الدفع للعميل يعمل) لكنه غير موجود في openapi.json — يُرجى توثيقه رسمياً. **صيغة الرسائل الفعلية الموثّقة:**
  - التحية عند الاتصال: `{"type": "hello", "user_id": <id>}`
  - الإشعار: **كائن NotificationOut مباشرة** `{id, user_id, title, body, notification_type, data, is_read, created_at}` — (ليس مغلّفاً في `{type:"notification", data:...}`)

## 🟡 الأولوية المتوسطة (تحسينات تُستغل فوراً)

### 10) بحث في الطلبات

- GET /orders وGET /owner/{fid}/orders وGET /admin/orders: بارامتر `search` (رقم الطلب أو اسم العميل) — الواجهة تجلبه اليوم بلا فلترة بحث خلفية (بحث المالك محلي على أول 100 طلب فقط).

### 11) ترتيب results الطلبات حسب created_at قابلاً للتوجيه

- بارامتر `sort` (`newest` | `oldest`) في مسارات الطلبات — الواجهة ترتب محلياً (يكفي حالياً لكنه لا يتجاوز الصفحة الأولى).

### 12) إضافة حقول البطاقات الإجرائية في GET /admin/dashboard ⚠️ جديد (الجولة 6)

- الباك إند لا يرجع `pending_membership_requests` / `pending_facilities` /
  `orders_today` في `/admin/dashboard` — الواجهة تجلبها اليوم من 3 endpoints إضافية
  (تعمل ✓ لكن 3 طلبات بدل واحد). إضافتها في endpoint التجميعي أفضل.

---

## ✅ ما حُسم في الجولة 6 (للمعلومية)

- **بيانات الأدمن:** البريد الصحيح `admin@tawfir.giize.com` /
  `Admin@Tawfir2026#Secure` (يعيد access_token فعلياً) — بيانات `gleeze` كانت خاطئة.
- **بوابة المشرف اختُبرت كاملةً** (دخول/تذكرني/لوحة/طلبات/تفاصيل/منشآت/فلترة/خصم/
  إعدادات) — كلها تعمل من الواجهة؛ العطلان الوحيدان (approve/reject 500 + صور
  الإيصالات 404) خادميان (البندان 1 و2 أعلاه).
- **فلترة status في /admin/facilities:** البارامتر موجود في API وكان غير مستغل —
  **استُغل الآن في الواجهة** (الجولة 6) — لا حاجة لعمل خادمي.

## ملخص أرقام الفحص العكسي (57 مساراً — الجدول الكامل في تقرير_توفير.md §الجولة 5)

| الحكم | العدد |
|---|---|
| ✅ مكتمل (مستهلك + بارامتراته مستغلة) | **40** (39 + status في /admin/facilities استُغل في الجولة 6) |
| 🟡 ناقص استغلال (أُصلح أهم 4 في الجولة 5 + فلترة منشآت الأدمن في الجولة 6) | 12 |
| 🔴 غير مستهلك (موثّق سبب عدم الاستهلاك: /health تشغيلي، /cards عُرِض قرار عدم إضافته مؤقتاً...) | 5 |
