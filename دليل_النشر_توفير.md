# دليل نشر توفير (Tawfir) على Vercel

> دليل عربي لنشر الواجهة الأمامية (Frontend) لمنصة «توفير» على Vercel.
> الباك إند حي على: https://api.tawfir.giize.com

---

## 1. المتطلبات قبل النشر

- حساب Vercel (مجاني): https://vercel.com/signup
- مستودع Git يحوي كود المشروع (GitHub / GitLab / Bitbucket)
- Node.js 18+ محلياً (للبناء التجريبي)
- الباك إند يعمل ويمكن الوصول إليه من Vercel (مؤكد: https://api.tawfir.giize.com)

---

## 2. متغيرات البيئة (Environment Variables)

في لوحة Vercel: Project → Settings → Environment Variables. أضف:

| المتغير | القيمة | البيئة |
|---------|--------|--------|
| `NEXT_PUBLIC_API_URL` | `https://api.tawfir.giize.com` | Production + Preview + Development |
| `NEXT_PUBLIC_SITE_URL` | `https://tawfir.giize.com` | Production |
| `NEXT_PUBLIC_ADMIN_URL` | `https://admin.tawfir.giize.com` | Production |
| `NEXT_PUBLIC_OWNER_URL` | `https://facility.tawfir.giize.com` | Production |
| `DATABASE_URL` | (قاعدة البيانات محلياً فقط — Vercel لا يحتاجها) | — |

> ملاحظة: الواجهة لا تستخدم Prisma/قاعدة بيانات على الإنتاج — كل البيانات من الـ API. `DATABASE_URL` فقط للتنمية المحلية.

> `NEXT_PUBLIC_*` تُحقن في الكود العميلي وقت البناء، لذا يجب ضبطها قبل أول Build.

---

## 3. خطوات النشر على Vercel

### 3.1 ربط المستودع
1. ادخل https://vercel.com/new
2. اختر مستودع Git يحوي كود توفير
3. Vercel يكتشف Next.js تلقائياً (Framework Preset: Next.js)

### 3.2 إعدادات البناء (Build & Output)
- **Build Command**: `next build` (افتراضي Vercel)
- **Output Directory**: `.next` (افتراضي)
- **Install Command**: `bun install` (إن استخدمت bun) أو `npm install`
- **Node.js Version**: 20.x (أحدث)

### 3.3 متغيرات البيئة
أضف متغيرات الجدول أعلاه (القسم 2) قبل أول Build.

### 3.4 اضغط Deploy
- Vercel يبني وينشر. تتراوح المدة 1-3 دقائق.
- عند النجاح: يُعطيك رابط `*.vercel.app` للمعاينة.

---

## 4. رط الدومينات الثلاثة

بوابات توفير الثلاث تستخدم نفس الكود لكن تتمايز بالـ Host عبر `src/middleware.ts`:

| البوابة | الدومين | السلوك |
|---------|---------|--------|
| العميل | `tawfir.giize.com` | الصفحة الرئيسية + تصفّح + طلبات + عضوية |
| المالك | `facility.tawfir.giize.com` | يعيد كتابة `/` → `/owner` (بوابة المالك) |
| المشرف | `admin.tawfir.giize.com` | يعيد كتابة `/` → `/admin` (لوحة المشرف) |

### 4.1 إضافة الدومينات في Vercel
1. Project → Settings → Domains → Add
2. أضف `tawfir.giize.com` (Primary)
3. أضف `facility.tawfir.giize.com`
4. أضف `admin.tawfir.giize.com`

### 4.2 سجلات DNS (عند مزود الدومين giize.com)

أضف سجلات CNAME (أو A) لكل دومين فرعي تشير إلى Vercel:

| النوع | الاسم | القيمة |
|------|------|--------|
| CNAME | `tawfir` | `cname.vercel-dns.com` |
| CNAME | `facility.tawfir` | `cname.vercel-dns.com` |
| CNAME | `admin.tawfir` | `cname.vercel-dns.com` |

> للدومين الجذري `tawfir.giize.com` (لو كان جذرياً): سجل A يشير إلى `76.76.21.21` (Vercel) أو استخدم Vercel Nameservers.

### 4.3 شهادات SSL
Vercel يُصدر شهادات SSL تلقائياً (Let's Encrypt) لكل دومين بعد تحقق DNS (خلال دقائق). **PWA يتطلب HTTPS** — Vercel يوفّره تلقائياً.

### 4.4 التحقق
بعد انتشار DNS:
- https://tawfir.giize.com → الصفحة الرئيسية للعميل
- https://facility.tawfir.giize.com → يعيد توجيهك إلى /owner/login
- https://admin.tawfir.giize.com → يعيد توجيهك إلى /admin/login

---

## 5. ⚠️ PWA يتطلب HTTPS

PWA (manifest + Service Worker + زر التثبيت) يعمل **فقط عبر HTTPS**. Vercel يوفّر HTTPS تلقائياً بشهادات Let's Encrypt مجانية لكل دومين. لا حاجة لإعداد إضافي.

على localhost (تطوير): المتصفح يسمح بـ SW على `localhost` (استثناء) — يعمل التطوير محلياً.

---

## 6. اختبار التثبيت على الأجهزة

### 6.1 أندرويد (Chrome)
1. افتح https://tawfir.giize.com على Chrome أندرويد
2. اضغط أيقونة القائمة (⋮) → **إضافة إلى الشاشة الرئيسية** (Add to Home screen)
   - أو سيظهر بانر تثبيت تلقائي بعد فترة (مع زر «تثبيت» في /account)
3. التطبيق يفتح standalone (بلا شريط عنوان المتصفح) بعد التثبيت
4. كرر على https://facility.tawfir.giize.com لتثبيت بوابة المالك (أيقونة مختلفة)

### 6.2 iOS (Safari)
1. افتح https://tawfir.giize.com على Safari iOS
2. اضغط زر المشاركة (□↑) → **إضافة إلى الشاشة الرئيسية** (Add to Home Screen)
3. iOS لا يدعم beforeinstallprompt — يعتمد على الإرشاد اليدوي
   - التطبيق يعرض إرشاد «اضغط مشاركة ← إضافة إلى الشاشة الرئيسية» في زر التثبيت
4. كرر على https://facility.tawfir.giize.com

### 6.3 سطح المكتب (Chrome / Edge)
- افتح https://tawfir.giize.com
- أيقونة التثبيت (⊕) في شريط العنوان → Install
- أو قائمة (⋮) → Cast, save, and share → Install توفير

---

## 7. آلية التحديث (رسالة «يتوفر تحديث»)

### 7.1 كيف تعمل
- `src/lib/pwa/version.ts` يحوي رقم الإصدار (الحالي: `1.1.0`).
- Service Worker (`/sw.js`) يضمن هذا الرقم في كل بناء.
- عند نشر نسخة جديدة على Vercel: زائر قديم يفتح التطبيق → SW يقارن رقم الإصدار → إن اختلف يُحمّل في الخلفية → ينتظر → يعرض شريحة بأسفل الشاشة: **«يتوفر تحديث لتطبيق توفير»** + زر **«تحديث الآن»**.
- الضغط على الزر يستدعي `skipWaiting()` + `location.reload()` → التطبيق يتحدث فوراً.

### 7.2 ترقيم الإصدار
- لكل نشر بإصلاحات/ميزات جديدة: ارفع `VERSION` في `src/lib/pwa/version.ts` (مثلاً `1.1.0` → `1.2.0`).
- هذا يضمن أن كل المستخدمين يرون رسالة التحديث بعد النشر.
- **Vercel Preview Deployments** لا تطلق تحديثاً (الـ IS_PROD=false) — فقط الإنتاج.

---

## 8. قيود iOS المعروفة وحدود الكاش

### 8.1 قيود iOS Safari للـ PWA
| القيد | التأثير | الحل |
|------|--------|-----|
| لا `beforeinstallprompt` | لا بانر تثبيت تلقائي | إرشاد يدوي «مشاركة ← إضافة إلى الشاشة الرئيسية» (مُنفّذ في PWAInstallButton) |
| حد ذاكرة الكاش ~50MB | قد تُمسح بيانات SWR القديمة | SWR يعيد الجلب عند الحاجة — تجربة حسّنة |
| `display-mode: standalone` iOS 15.4+ | قبل ذلك يعمل كـ web app | ميزة `hide-in-standalone` في globals.css تعمل على iOS 15.4+ |
| لا دعم Push Notifications (قبله Web Push) | تنبيهات الخلفية لا تعمل | لا ميزة Push في توفير حالياً |
| `apple-mobile-web-app-capable` محدود | بعض الـ APIs معطّلة في standalone | لا توجد ميزات تعتمد عليها |

### 8.2 حدود كاش Service Worker
- **StaleWhileRevalidate** لـ /products /facilities /regions /cards: آخر نسخة تبقى أوفلاين للأبد، وتُحدّث في الخلفية عند الاتصال.
- **NetworkOnly** لكل POST/PUT/PATCH/DELETE + auth + orders + membership: لا تخزين للتوكنات أو العمليات الحساسة.
- **CacheFirst** للصور (حد 50 إدخالاً): تُحفظ مرة واحدة.
- عند امتلاء الكاش: SWR يحذف أقدم الإدخالات تلقائياً (LRU).

---

## 9. اختبار ما بعد النشر (Checklist)

- [ ] https://tawfir.giize.com يفتح → الصفحة الرئيسية تعرض وجبات يمنية
- [ ] https://facility.tawfir.giize.com يعيد توجيهك إلى /owner/login
- [ ] https://admin.tawfir.giize.com يعيد توجيهك إلى /admin/login
- [ ] /manifest.webmanifest يُرجع JSON صحيح (عميل على tawfir، مالك على facility)
- [ ] /sw.js يُرجع JS صحيح («توفير Service Worker — الإصدار X»)
- [ ] /.well-known/assetlinks.json يُرجع 200 + Content-Type صحيح
- [ ] التثبيت على أندرويد Chrome يعمل (standalone بلا شريط عنوان)
- [ ] التثبيت على iOS Safari يعمل (أيقونة على الشاشة الرئيسية)
- [ ] الوضع الفاتح والداكن سليمان على كل شاشة
- [ ] 360px بلا overflow أفقي
- [ ] رسالة «يتوفر تحديث» تظهر بعد نشر نسخة جديدة برقم إصدار أعلى

---

## 10. استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| الصفحة بيضاء بعد النشر | تحقق من متغيرات `NEXT_PUBLIC_*` ضُبطت قبل البناء (Vercel → Settings → Environment Variables) |
| طلبات API ترجع 404 | تأكد أن `NEXT_PUBLIC_API_URL=https://api.tawfir.giize.com` وأن `next.config.ts` rewrite يشير للدومين الصحيح |
| manifest لا يظهر | تحقق أن `/manifest.webmanifest` يُرجع 200 على الإنتاج (Vercel قد يحتاج إعادة بناء) |
| SW لا يُسجّل | تحقق من HTTPS (Vercel تلقائي) + أن `/sw.js` يُرجع مع `Service-Worker-Allowed: /` |
| الدومينات لا تعمل | تحقق من سجلات CNAME تشير إلى `cname.vercel-dns.com` + انتظار انتشار DNS (حتى ساعة) |
| رسالة التحديث لا تظهر | ارفع `VERSION` في `src/lib/pwa/version.ts` لكل نشر |

---

## 11. ملاحظات ختامية

- **نفس الكود** يخدم البوابات الثلاث — `src/middleware.ts` يقرأ Host ويعيد كتابة المسار. لا حاجة لثلاث مشاريع.
- **الـ rewrite proxy**: كل طلبات `/api/*` تُحال إلى `${NEXT_PUBLIC_API_URL}/api/v1/*` عبر `next.config.ts` — لا CORS، لا تعرّض للتوكنات في العميل المباشر.
- **Vercel Edge**: middleware يعمل على Edge Network (سريع عالمياً).
- **البناء**: `next build` يُنتج standalone قابل للتشغيل بـ `next start` (Vercel يديره تلقائياً).
