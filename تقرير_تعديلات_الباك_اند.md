# 📋 تقرير تعديلات الباك اند المطلوبة — منصة توفير
**التاريخ**: 18 سبتمبر 2026 · **المُعد**: وكيل تطوير الفرونت اند · **الباك اند المستهدف**: `tawfir-backend` (https://api.tawfir.giize.com)

> هذا التقرير مُعدّ لوكيل تطوير الباك اند. الفرونت اند **جاهز ومختبر** ويعمل بكامل ميزاته مع الباك اند الحالي عبر حلول أمامية مؤقتة. كل بند هنا يرفع هذه الحلول المؤقتة إلى حلول نهائية في الخادم، مرتبة حسب الأولوية للإنتاج.

---

## 🔴 أولوية قصوى (تفسّر مشاكل يراها المستخدم اليوم)

### B-1) السماح برفع صورة المتجر قبل تسجيل الدخول (مشكلة «رفع الصورة يفشل عند إنشاء متجر جديد»)
- **الس symptom**: في صفحة `/owner/register`، عند رفع صورة المتجر: `POST /api/v1/uploads` يرد **401 Missing bearer token** لأن التاجر لا يملك حساباً بعد.
- **الحل الأمامي الحالي (مؤقت)**: الفرونت يعرض بطاقة توجيه + حقل «ألصق رابط صورة» تلقائياً عند فشل 401.
- **المطلوب في الباك اند**:
  1. خيار أ (الموصى به): نقطة رفع منفصلة للملفات غير المصادقة `POST /api/v1/uploads/public` مقيدة بـ:
     - مجلد واحد فقط `facilities`
     - حد حجم أصغر (مثلاً 2MB) + rate limit صارم (10 ملفات/ساعة/IP) + فحص MIME فعلي (magic bytes)
     - ربط الملف بجلسة مؤقتة (uuid يُرسل مع `OwnerRegister.image_url`) وحذف الملفات غير المرتبطة بعد 24 ساعة (مهمة Celery).
  2. خيار ب: قبول `image_file` كـ multipart مباشرة داخل `POST /owner/register` (بجانب JSON).
- **اختبار قبول**: رفع صورة من صفحة التسجيل بدون توكن ينجح ويعيد `{url, folder, size_bytes}`.

### B-2) استقبال إحداثيات الموقع عند تسجيل المتجر (`OwnerRegister`)
- **الحقيقة المؤكدة**: سكيما `OwnerRegister` **لا تحوي** `latitude`/`longitude` (فقط `address` نصي اختياري). `OwnerFacilityUpdate` (بعد الدخول) تحويهما — لكن التسجيل الأول يضيع الموقع.
- **الحل الأمامي الحالي (مؤقت)**: الفروント يجمع الإحداثيات (GPS أو من الخريطة) ويخزنها في `sessionStorage["tawfir_owner_pending_location"]` كـ `{lat, lng, source, label}` ولا يستطيع إرسالها.
- **المطلوب في الباك اند**: إضافة حقول اختيارية إلى `OwnerRegister`:
  ```json
  "latitude":  number | null   (نطاق -90..90)
  "longitude": number | null   (نطاق -180..180)
  ```
  تُخزَّن في سجل المتجر عند الإنشاء. عند وجودها + address، يظهر المتجر مباشرة في `/facilities?lat&lng` وفي رادار المناديب.
- **ملاحظة إضافية**: إضافة حقل `location_source` اختياري ("gps" | "map") يفيد التحقق من جودة الموقع لاحقاً.

### B-3) توثيق ملكية سكيما بيانات المندوب (اللوحة اليمنية + البطاقة الشخصية)
- **التغيير الأمامي المنجز**: الفرونت الآن يرسل في `POST /courier/auth/register`:
  - `license_number` = **رقم البطاقة الشخصية اليمنية** (أرقام فقط، إلزامي أمامياً)
  - `vehicle_plate` = **لوحة يمنية كاملة بصيغة `"1-222156-أ"`** (كود المحافظة 1-22 + رقم 4-6 خانات + حرف عربي، إلزامي أمامياً)
- **المطلوب في الباك اند**:
  1. جعل الحقلين **إلزاميين** (مثل الفرونت) في `CourierRegister` مع رسائل عربية: «رقم البطاقة الشخصية مطلوب» و«بيانات اللوحة كاملة إلزامية».
  2. تحقق تنسيقي في Pydantic:
     - `license_number`: `^\d{5,20}$`
     - `vehicle_plate`: `^([1-9]|1\d|2[0-2])-\d{4,6}-[\u0621-\u064A]$` (22 محافظة فقط)
  3. (اختياري لكن مهم للجودة): إضافة أعمدة منظمة للوحة: `plate_city_code INT`, `plate_number VARCHAR(10)`, `plate_letter CHAR(1)` — بجانب النص الكامل أو بدله، مع دالة بناء موحدة.
  4. إرجاع قيم اللوحة والبطاقة في `GET /courier/me` و`admin/couriers/verification-requests` (لعرضها للإدارة بدل «—»).
- **مرجع أكواد المحافظات** (موجود في `src/lib/yemen.ts` بالفرونت): 1 أمانة العاصمة · 2 صنعاء · 3 عدن · 4 تعز · 5 حضرموت · 6 الحديدة · 7 إب · 8 حجة · 9 ذمار · 10 صعدة · 11 أبين · 12 لحج · 13 البيضاء · 14 المحويت · 15 الجوف · 16 مأرب · 17 شبوة · 18 المهرة · 19 عمران · 20 الضالع · 21 ريمة · 22 سقطرى.

### B-4) تحديث بيانات المندوب بعد التسجيل (`PATCH /courier/me`)
- **المشكلة**: سكيما `CourierPublicUpdate` الحالية **لا تقبل** حقول المركبة (vehicle_type, vehicle_plate, license_number, region_id) — تتجاهلها الخادم. المندوب الذي يعدّل لوحته/بطاقته بعد الإنشاء لا يُحدَّث فعلياً.
- **الحل الأمامي الحالي (مؤقت)**: الفروند يرسلها ويستمر للمستندات مع توست تحذيري.
- **المطلوب**: توسيع `CourierPublicUpdate` لتقبل (مع نفس تحقق B-3): `public_name`, `vehicle_type`, `vehicle_plate`, `license_number`, `region_id`, `preferred_shifts` — وتسجيلها في audit log.

---

## 🟠 أولوية عالية (تكامل وتحقق أفضل)

### B-5) ربط توثيق الجوال بتسجيل التاجر (منع الالتفاف على OTP)
- **الحالة الأمامية المنجزة**: صفحة تسجيل التاجر تطلب OTP (`/otp/request` → واتساب) وتتحقق (`/otp/verify`) **قبل** إرسال `/owner/register` — تدفق كامل يعمل حياً ✅.
- **الثغرة**: الخادم لا يعرف أن الجوال تم توثيقه — أي عميل API يستطيع تسجيل متجر بجوال غير موثق مباشرة عبر `/owner/register`.
- **المطلوب (واحد من اثنين)**:
  1. إضافة حقل اختياري `otp_verified_token` في `OwnerRegister`: بعد نجاح `/otp/verify` يعيد الخادم توكن توثيق قصير الأجل (مثلاً JWT موقّع بحقل target، صالح 10 دقائق) يُرفق بالتسجيل — الخادم يتحقق منه ويرفض التسجيل بدونه (أو يعلّم الحساب `phone_verified=false` ويتطلب توثيقاً لاحقاً).
  2. أو: تسجيل حالة التوثيق في Redis عند نجاح verify، و`/owner/register` يفحصها للجوال نفسه (نافذة 15 دقيقة) ويرفض/يعلم وفقاً لها.
- **مهم أيضاً**: تفعيل نفس التحقق في `/auth/register` للعملاء (نفس البنية موجودة أصلاً في واجهة العميل).

### B-6) ساعات العمل المنظمة (اختياري لكن يفتح مزايا)
- **الحالة الأمامية المنجزة**: محدد أيام + أوقات (كل 30 دقيقة) يولّد نصاً متوافقاً مع الحقل الحالي `working_hours` (مثل `"السبت-الخميس 08:00-23:00"`) — لا تغيير مطلوب للعمل الآن ✅.
- **المقترح للإنتاج**: عمود JSON منظم:
  ```json
  "working_hours": {"days": ["sat","sun","mon","tue","wed","thu"], "open": "08:00", "close": "23:00"}
  ```
  مع طبقة توافق تعرض/تقبل النص القديم. يتيح «مغلق الآن» دقيقاً وبحث «المفتوح الآن» — النص الحر يجعلها تقريبية (الفرونت فيه محلل نصي تجريبي بالفعل في `src/lib/facility-hours.ts` يمكن الرجوع له).

### B-7) عدد محاولات OTP وإعادة الإرسال — نصوص متسقة
- **الملاحظة**: رد 422 الحالي `"الكود غير صحيح. محاولات متبقية: 4"` ممتاز ويُعرض أمامياً كما هو ✅.
- **المطلوب**: التأكد أن `/otp/resend` يرد بنفس البنية `{ttl_seconds, delivered, dev_code}` وأن رسالة التهدئة تتضمن عدد الثواني بنمط «... N ثانية» (الفرونت يقرأها ويحدث العداد تلقائياً). موحّد الحد الأقصى للمحاولات (يبدو 5) بين الجوال والبريد.

---

## 🟡 أولوية متوسطة (جودة بيانات وتحكم إداري)

### B-8) تنسيق الجوال الموحد في كل نقاط الاستقبال
- **الحل الأمامي المنجز**: الفرونت يطبع كل أرقام الجوال قبل الإرسال إلى الشكل المحلي الخالص `777123456` (يتقبل الإدخال بالمسافات/`+967`/`0967`) ✅.
- **المطلوب في الخادم**: تطبيع موحد في Pydantic (validator مركزي) لكل الحقول: `phone`, `phone_facility`, `license... target` في OTP — يقبل الصيغ الشائعة ويطبّعها قبل المقارنة/التخزين، ويمنع التسجيل المكرر بصيغ مختلفة لنفس الرقم (مثال: `777123456` و`+967777123456` حسابان مختلفان حالياً — ثغرة ازدواج فعلية).

### B-9) نوع المنشأة «كافتيريا»
- **الحالة**: الطلب كان تغيير التسمية فقط — أُنجز أمامياً بالكامل (كل الواجهات تعرض «كافتيريا»/«كافتيريات» وقيمة الـ enum تبقى `cafe`) ✅ **لا يلزم شيء الآن**.
- **خيار مستقبلي**: إضافة قيمة `cafeteria` للـ enum مع ترحيل بيانات (إذا أراد المالك تمييزاً برمجياً بين مقهى/كافتيريا مستقبلاً). الفرونت جاهز للإضافة فور توفرها (تعريف واحد في `constants.ts`).

### B-10) سجل تغيير فئة المتجر بعد الموافقة
- مع إتاحة `working_hours` و`phone` للتعديل عبر `PUT /owner/facility/{id}` — يُقترح تسجيل هذه التعديلات في `audit_logs` (الجدول موجود أصلاً) لتظهر في شاشة سجل التغييرات بالإدارة (الواجهة موجودة وتقرأ `/admin/audit-logs`).

---

## 🟢 نقاط استُخدمت حياً وتعمل — للعلم فقط (لا تعديل مطلوب)
| النقطة | الحالة |
|---|---|
| `POST /otp/request` عبر واتساب | ✅ يعمل حياً (رد 200 + ttl_seconds=300) |
| `POST /otp/verify` | ✅ يرد 422 عربي بعدّاد محاولات عند الخطأ |
| `GET /regions` | ✅ يعيد المناطق الحقيقية |
| `POST /courier/auth/register` | ✅ أنشأ حساب اختبار فعلي (409 عند التكرار — يستخدمه الفرونت لكشف الازدواج) |
| `POST /owner/register` | ✅ يستقبل النموذج كاملاً (مختبر عبر OTP → تسجيل) |
| `PATCH /courier/me` | ✅ يستقبل الطلب (لكن يتجاهل حقول المركبة → B-4) |
| `POST /api/v1/uploads` (بعد الدخول) | ✅ يعمل للمالك/المشرف |

---

## 📎 ملحق: قيم اختبار جاهزة للوكيل
```bash
# OTP تدفق كامل (واتساب):
curl -X POST https://api.tawfir.giize.com/api/v1/otp/request -H "Content-Type: application/json" -d '{"target":"7XXXXXXXX","name":"اختبار"}'
curl -X POST https://api.tawfir.giize.com/api/v1/otp/verify -H "Content-Type: application/json" -d '{"target":"7XXXXXXXX","code":"123456"}'

# لوحة يمنية صالحة للتحقق: "1-222156-أ" (صنعاء) · "3-222156-أ" (عدن) · "22-12345-ب" (سقطرى)
# لوحات يجب رفضها: "0-222156-أ" (كود 0) · "23-222156-أ" (فوق 22) · "1-222156" (بلا حرف) · "1-22156-AB" (حروف لاتينية)

# بطاقة شخصية: "01234567" (8 خانات — يقبل 5-20 رقماً)

# جوالات يجب أن تطبّع لنفس القيمة: "777 123 456" = "+967 777123456" = "0967 777 123 456" → "777123456"
```

**أولوية التنفيذ المقترحة**: B-1 → B-2 → B-3 → B-5 → B-4 → الباقي. B-1 وB-2 يرفعان أهم حلين مؤقتين في الفرونت، وB-3 يغلق فجوة تحقق البيانات.
