# 🍎 دليل نشر «توفير» على App Store — من الصفر حتى القبول
### iOS / iPhone — بناء سحابي كامل بلا أي Mac + توثيق كل خطوة

> **تاريخ الإعداد:** الجولة 24 · **الإصدار:** 1.2.0 · **Bundle ID:** `com.tawfir.ye.app`
> هذا الدليل مكمل لـ `دليل_نشر_قوقل_بلاي.md` (أندرويد) ويغطي حصريًا آيفون.

---

## 📋 جدول المحتويات

1. [ما أُنجز في هذه الجولة (ملخص تقني)](#1-ما-أُنجز-في-هذه-الجولة)
2. [المتطلبات قبل أي شيء (مرة واحدة)](#2-المتطلبات-قبل-أي-شيء)
3. [الإعداد لمرة واحدة: حساب Apple + المفاتيح + الأسرار](#3-الإعداد-لمرة-واحدة)
4. [تفعيل إشعارات iOS (APNs في Firebase)](#4-تفعيل-إشعارات-ios)
5. [الروابط العميقة Universal Links](#5-الروابط-العميقة)
6. [البناء السحابي: تشغيل الـWorkflow خطوة بخطوة](#6-البناء-السحابي)
7. [TestFlight: التثبيت على آيفونك فورًا](#7-testflight)
8. [التقديم للمراجعة: App Store Connect بالتفصيل](#8-التقديم-للمراجعة)
9. [لقطات الشاشة المطلوبة](#9-لقطات-الشاشة)
10. [ملصقات الخصوصية + التصنيف العمري](#10-ملصقات-الخصوصية)
11. [مخاطر الرفض الشائعة — وكيف عالجناها مسبقًا](#11-مخاطر-الرفض)
12. [بعد القبول: التحديثات والترقيات](#12-بعد-القبول)
13. [استكشاف الأخطاء وإصلاحها](#13-استكشاف-الأخطاء)
14. [أسئلة شائعة](#14-أسئلة-شائعة)

---

## 1) ما أُنجز في هذه الجولة

### أ) إضافات iOS الأصلية (تعمل داخل GitHub Actions — لا مجلد ios في مستودعك)

| الملف | الوظيفة |
|---|---|
| `.github/workflows/build-ios.yml` | **سير البناء السحابي الكامل** على منفّذي macOS: توليد مشروع Xcode → الأيقونات والسبلاش → هوية توفير → أرشفة → توقيع آلي → تصدير IPA → رفع اختياري إلى TestFlight |
| `scripts/patch-ios-identity.mjs` | **نظير iOS لسكربت أندرويد**: الأذونات بالعربية (الموقع/الكاميرا/الصور — بدلها أي استدعاء يُسقِط التطبيق فورًا)، قفل Portrait + iPhone فقط، entitlements الإشعارات والروابط العميقة، دمج Firebase/FCM عبر Swift Packages، توليد `TawfirNativePlugin.swift` (توكن FCM عبر الجسر — نفس مسار أندرويد حرفيًا فلا يتغير أي كود ويب)، توليد Scheme لتصدير xcodebuild |
| `capacitor.config.ts` | قسم `ios`: خلفية مطابقة للسبلاش + قفل تمرير الـWKWebView (إحساس Native أصيل بلا ارتداد مطاطي) |
| `GoogleService-Info.plist` | ملف Firebase الذي حمّلته — مُنسوخ لجذر المستودع (نفس نمط `google-services.json` لأندرويد) — يقرؤه سير البناء تلقائيًا |

### ب) إضافات الويب (تعمل على الويب وأندرويد وiOS معًا)

| الملف | الوظيفة |
|---|---|
| `src/app/.well-known/apple-app-site-association/route.ts` | ملف الروابط العميقة لـApple (نظير `assetlinks.json`) — يُخدَم بـ `Content-Type: application/json` كما يشترط Apple حرفيًا |
| `src/lib/capacitor.ts` + `NativeBridge.tsx` | مستمع `appUrlOpen`: فتح أي رابط توفير من Safari/الرسائل → التطبيق يفتح **ويتنقل للمسار المطلوب** (Capacitor لا يفعلها تلقائيًا) |
| `src/app/api/customer/account/route.ts` + `DeleteAccountCard.tsx` + `useDeleteAccount.ts` | **ميزة حذف الحساب نهائيًا** — شرط قبول إلزامي (قاعدة Apple 5.1.1(v): أي تطبيق يسمح بإنشاء حساب يجب أن يسمح بحذفه من داخل التطبيق) |

### ج) كيف يعمل كل شيء معًا (بنية موحّدة مع أندرويد)

```
تطبيق iOS (WKWebView يفتح tawfir.giize.com)
   │  نفس الموقع الحي — Service Worker يعمل 100% (أوفلاين بعد أول زيارة)
   │
   ├─ الإشعارات: Firebase/FCM أصلي ← توكن عبر TawfirNativePlugin ←
   │   FcmRegistrar يسجّله في الباك إند (POST /fcm/token) ← نفس أندرويد
   │
   ├─ الروابط العميقة: apple-app-site-association + entitlement ←
   │   setupNativeUrlOpen يوجّه الـWebView
   │
   └─ شريط الحالة/الاهتزاز/الشبكة: نفس إضافات @capacitor الحالية
```

---

## 2) المتطلبات قبل أي شيء

| المتطلب | التكلفة | ملاحظات |
|---|---|---|
| **حساب Apple Developer Program** | **99$ سنويًا** | اشتراك فردي كافٍ تمامًا (لا حاجة لحساب مؤسسة 299$) |
| جهاز آيفون للاختبار | — | لأجل TestFlight (المراجعة لا تتطلبه) |
| Mac | **غير مطلوب إطلاقًا** ✅ | كل البناء على GitHub Actions — مثل أندرويد تمامًا |
| مستودع GitHub | مجاني | **ملاحظة مهمة:** إن كان مستودعك خاصًا (Private) فدقائق منفّذي macOS تُحسب 10× — المستودع العمومي (Public) مجاني بلا حدود عملية |

> 💡 **التطبيق لا يُرفض لأن موقعك من اليمن** — Apple لا تفرّق. المطلوب فقط وسيلة دفع دولية (بطاقة) للاشتراك.

---

## 3) الإعداد لمرة واحدة

### 3-أ) التسجيل في Apple Developer Program

1. افتح **developer.apple.com/programs** → زر **Enroll**.
2. سجّل الدخول بـ Apple ID (أنشئ واحدًا مخصصًا للعمل إن أحببت — استخدم بريدًا تملكه فعليًا).
3. اختر **Individual / شخصي**.
4. أدخل بياناتك الشخصية + عنوانًا (أي عنوان دولي صحيح لشحن لا يلزم — التحقق بالهوية يتم غالبًا عبر مكالمة/تحقق آلي).
5. ادفع 99$ ببطاقة (فيزا/ماستر كارد).
6. **الانتظار:** من دقائق إلى يومين (تأكيد الهوية).
7. عند القبول ستدخل **App Store Connect** (appstoreconnect.apple.com).

### 3-ب) استخراج معرّف الفريق APPLE_TEAM_ID

- **developer.apple.com → Membership** (بعد القبول).
- انسخ **Team ID** — سطر من 10 أحرف إنجليزية وأرقام (مثل `A1B2C3D4E5`).
- احفظه — ستحتاجه في الأسرار وفي الروابط العميقة.

### 3-ج) تسجيل الـBundle ID (معرّف التطبيق)

1. **developer.apple.com → Certificates, Identifiers & Profiles → Identifiers** → زر **(+)**.
2. اختر **App IDs** ثم **App**.
3. Description: `Tawfir Customer` — Bundle ID: **Explicit** → `com.tawfir.ye.app` (**حرفيًا — أي اختلاف = فشل التوقيع**).
4. في قائمة Capabilities فعّل: ✅ **Push Notifications** (للإشعارات).
5. Continue → Register.

> لتطبيق المالك لاحقًا: كرر الخطوات بـ `com.tawfir.ye.owner`.

### 3-د) إنشاء مفتاح App Store Connect API (قلب التوقيع السحابي)

هذا المفتاح يتيح لـGitHub Actions **إنشاء شهادة التوزيع والبروفايل تلقائيًا** — لا Keychain ولا Mac ولا تصدير يدوي:

1. **appstoreconnect.apple.com → Users and Access → Integrations** (تبويب App Store Connect API).
2. زر **(+) Generate API Key** (أو Create).
3. الاسم: `GitHub Actions` — النوع: **App Store Connect API** — الصلاحية: **App Manager**.
4. اضغط Generate ثم **نزّل ملف `AuthKey_XXXXXXXXXX.p8` فورًا** (يُنزَّل مرة واحدة فقط! إن فقدته أنشئ مفتاحًا جديدًا).
5. من نفس الصفحة انسخ:
   - **Key ID** (10 أحرف — يظهر بجوار اسم المفتاح).
   - **Issuer ID** (UUID طويل أعلى الصفحة).

### 3-هـ) إضافة الأسرار الأربعة إلى GitHub

المستودع → **Settings → Secrets and variables → Actions → New repository secret**:

| اسم السر | القيمة | من أين |
|---|---|---|
| `APPLE_TEAM_ID` | معرّف الفريق (10 أحرف) | §3-ب |
| `APPLE_AUTH_KEY_ID` | Key ID (10 أحرف) | §3-د |
| `APPLE_AUTH_ISSUER_ID` | Issuer ID (UUID) | §3-د |
| `APPLE_AUTH_KEY_P8_B64` | ملف `.p8` بترميز Base64 | §3-د |

**ترميز ملف p8 إلى Base64 على ويندوز (نفس طريقة الـKeystore التي تعرفها):**

```bat
certutil -encode AuthKey_XXXXXXXXXX.p8 p8b64.txt
```
افتح `p8b64.txt` وانسخ كل محتواه (السطور + حتى سطور `-----BEGIN/END...` — سير العمل ينظّفها آليًا) والصقها كقيمة السر `APPLE_AUTH_KEY_P8_B64`.

### 3-و) إنشاء التطبيق في App Store Connect

1. **appstoreconnect.apple.com → My Apps → (+) New App**.
2. Platform: **iOS** — Name: **توفير** — Primary Language: **العربية (Arabic)**.
3. Bundle ID: اختر `com.tawfir.ye.app` (ظاهر بعد §3-ج) — SKU: `tawfir-ios-customer` (أي معرّف داخلي).
4. **Create** — ستفتح صفحة التطبيق (App Store tab).

---

## 4) تفعيل إشعارات iOS

إشعارات أندرويد تعمل عبر `google-services.json` + FCM. **iOS يحتاج خطوة إضافية لمرة واحدة: مفتاح APNs.**

### 4-أ) إنشاء مفتاح APNs

1. **developer.apple.com → Certificates, Identifiers & Profiles → Keys → (+)**.
2. الاسم: `Tawfir APNs` — فعّل ✅ **Apple Push Notifications service (APNs)** → Continue → Register.
3. **نزّل ملف `.p8` فورًا** (مرة واحدة!) — وانتبه لـ **Key ID** الظاهر في صفحة المفتاح.

### 4-ب) رفعه في Firebase

1. **Firebase Console → مشروع tawfeer-b99c2 → ⚙️ Project Settings → Cloud Messaging**.
2. في قسم **Apple app** (com.tawfir.ye.app) → **APNs Authentication Key → Upload**.
3. ارفع ملف `.p8` وأدخل **Key ID** و **Team ID** (من §3-ب).
4. Save — انتهى! 🎉

> ⚠️ **بدون هذه الخطوة:** التطبيق يُبنى ويعمل كل شيء، لكن **الإشعارات لا تصل لأجهزة iOS** (توكن FCM لا يتولّد). لا تؤثر إطلاقًا على أندرويد.

### 4-ج) كيف تختبر

- أرسل رسالة من **Firebase Console → Messaging → حملة جديدة**.
- Target: **Topic = `tawfir_all`** (التطبيق يشترك آليًا — نفس أندرويد).
- أو استهدف جهازًا واحدًا عبر توكن FCM (يظهر في سجلات التطبيق عند الطلب).

---

## 5) الروابط العميقة

تم تفعيل البنية كاملة:

- ✅ ملف `apple-app-site-association` يُخدَم على `https://tawfir.giize.com/.well-known/apple-app-site-association`
- ✅ entitlement `applinks:tawfir.giize.com` داخل التطبيق (يقوم به السكربت آليًا)

**يبقى عليك خطوة واحدة** — تفعيل الملف بمعرّف فريقك: أنشئ ملف `apple-team-id.txt` في **جذر المستودع** محتواه سطر واحد فقط:

```
A1B2C3D4E5
```
(معرّف فريقك الحقيقي من §3-ب — وارفعه مع الكود في المرة القادمة، أو أضف `APPLE_TEAM_ID` لبيئة استضافة الموقع).

**النتيجة:** فتح أي رابط توفير من Safari/واتساب على آيفون → يفتح التطبيق مباشرة على الشاشة الصحيحة (منتج/متجر/طلب).

> 🕐 ملاحظة: خادم Apple CDN يكاش الملف حتى 24 ساعة — إن لم تعمل الروابط فور النشر فانتظر ثم أعد المحاولة.

---

## 6) البناء السحابي

### 6-أ) التشغيل

المستودع → **Actions** → من القائمة: **"Build iOS IPA — توفير"** → **Run workflow** → عبّئ:

| الحقل | القيمة |
|---|---|
| app | `customer` |
| version_name | `1.2.0` (يجب أن يطابق ما سترفعه في App Store Connect) |
| version_code | `1` — **يجب أن يزيد مع كل رفعة جديدة** (1، 2، 3...) |
| allow_unsigned | اتركه ✗ (لا تفعله إلا لاختبار البناء قبل تجهيز الأسرار) |
| upload_testflight | ✅ بعد تجهيز §3 (يرفع الـIPA إلى TestFlight آليًا فور البناء) |

اضغط **Run workflow** وانتظر **25–40 دقيقة** (أول تشغيل ينزّل حزم Swift — التشغيلات التالية أسرع).

### 6-ب) النواتج

- **Artifacts** (أسفل صفحة التشغيل): ملف `tawfir-customer-v1.2.0-1.ipa` + `dSYMs.zip` (رموز الأعطال) + بصمة SHA-256.
- إن فعّلت TestFlight: التطبيق يظهر مباشرة في **App Store Connect → TestFlight** خلال دقائق (وقت معالجة إضافي ~10 دقائق على خوادم Apple).

### 6-ج) الأوضاع

| الوضع | متى | الناتج |
|---|---|---|
| **موقّع** (الأسرار الأربعة موجودة) | الوضع الطبيعي | IPA جاهز للرفع على App Store/TestFlight |
| **غير موقّع** (allow_unsigned=true بلا أسرار) | لاختبار سلامة البناء قبل فتح حساب Apple | IPA بلا توقيع — **لا يُثبَّت** على الأجهزة العادية (يُوقَّع يدويًا عبر أدوات مثل Sideloadly فقط) |

> 💡 **نصيحة:** قبل فتح حساب Apple يمكنك تشغيل البناء بوضع غير موقّع للتأكد أن كل شيء يترجم ويُبنى — ثم عند تجهيز الأسرار ستحصل على الموقّع مباشرة.

---

## 7) TestFlight

أسرع طريقة لتثبيت التطبيق على آيفونك:

1. (بعد الرفع الآلي §6) افتح **App Store Connect → تطبيقك → TestFlight**.
2. ستجد البناء بانتظار المعالجة (بضع دقائق) ثم زر **Test** أخضر.
3. **Internal Testing:** تبويب TestFlight → Apple iOS → أضف نفسك (مستخدمو App Store Connect الداخليون) → على الآيفون نزّل تطبيق **TestFlight** من App Store → افتح الرابط/الرمز → ثبّت توفير.
4. **External Testing (لأشخاص خارج فريقك):** أنشئ مجموعة + أضف بريد المدعوين — يتطلب موافقة مراجعة سريعة من Apple (عادة أقل من 24 ساعة، أسهل بكثير من مراجعة المتجر).

> TestFlight يعطي **90 يومًا** لكل بناء — كافٍ جدًا للاختبار، والنسخة النهائية ستكون من المتجر نفسه بلا انتهاء.

---

## 8) التقديم للمراجعة

في **App Store Connect → تطبيقك → تبويب App Store (نسخة iOS 1.2.0)** عبّئ الأقسام التالية:

### 8-أ) بيانات أساسية

| الحقل | القيمة المقترحة |
|---|---|
| اسم التطبيق | توفير |
| Subtitle (30 حرفًا) | `متاجر وجمع الطلبات وتوصيل` |
| **Primary Language** | العربية |
| Category أساسية | **Shopping / التسوق** |
| Category ثانوية | **Food & Drink / الطعام والشراب** |
| Keywords (100 حرف) | `توفير,توصيل,متاجر,سوبرماركت,طلبات,عروض,جملة,توفير السعر` |
| Support URL | `https://tawfir.giize.com/privacy` (أو صفحة تواصل إن وُجدت) |
| **Privacy Policy URL** | `https://tawfir.giize.com/privacy` — **إلزامي** وموجود لديك ✅ |
| Marketing URL | (اختياري) |

### 8-ب) الوصف (اقتراح جاهز)

```
توفير — منصّة التسوق الذكية التي تجمع متاجرك المفضلة في تطبيق واحد.

• تصفّح آلاف المنتجات من متاجر وسوبرماركتات قريبة منك
• قارن الأسعار ووفّر أكثر مع عروض يومية حصرية
• اجمع طلباتك من عدة متاجر في سلة واحدة واستلمها معًا
• تابع طلبك لحظة بلحظة حتى باب منزلك
• إشعارات فورية بكل جديد: تأكيد الطلب، التحضير، الخروج للتوصيل
• يعمل بدون إنترنت (آخر ما شاهدته يبقى أمامك)

حمّل توفير الآن واجعل التسوق اليومي أذكى وأرخص.
```

### 8-ج) What's New (لمرة الأولى يمكن تركه أو وصف موجز)

### 8-د) معلومات المراجعة — **الأهم لتجنب الرفض** ⚠️

في قسم **App Review Information**:

1. **Sign-In Required? → Yes** ثم أنشئ **حساب عميل تجريبي** مقدّمًا (سجّل حسابًا في التطبيق ببريد مثل `review@tawfir.com` وكلمة مرور سهلة) وأدخل بياناته في **Sign-In Information**. هذا يوفّر على المراجع عناء التسجيل ويمنع رفض «تعذّر تقييم التطبيق».
2. **Notes:** اكتب بالإنجليزية سطرين:

```
This is a live e-commerce/delivery app for stores in Yemen (Arabic RTL).
A demo customer account is provided above. The app loads our production
website https://tawfir.giize.com inside a native shell with push
notifications, location permissions, and account deletion available in:
Account page → Danger Zone → Delete Account.
```

3. **Contact Information:** اسمك + بريدك + هاتف (يُستخدم فقط للمراجعة).

### 8-هـ) الإصدار والتقديم

- بعد تعبئة كل شيء: زر **Add for Review** → **Submit for Review**.
- **مدة المراجعة المعتادة:** 24–48 ساعة (أحيانًا أقل).
- ستصلك النتيجة على البريد، وتظهر الحالة في App Store Connect.

---

## 9) لقطات الشاشة

- الجهّز نفسك بـ **3 إلى 5 لقطات** على الأقل (ننصح بـ 5: الرئيسية، متجر/منتجات، تفاصيل منتج، السلة، تتبع الطلب).
- **المقاسات المطلوبة (iPhone):** ارفع مجموعة واحدة بمقاس **6.9 بوصة: 1320×2868** أو **6.7 بوصة: 1290×2796** — وApp Store Connect يولّد البقية تلقائيًا (تطبيقنا iPhone-Only فلا لقطات iPad مطلوبة).
- أعد استخدام منهجية `دليل_اللقطات_الشاشة` الموجودة عندك إن شئت، أو التقط لقطات من متصفح بعرض آيفون ثم غيّر أبعادها.
- **صورة العرض (App Preview / فيديو):** اختيارية — تجاهلها في النسخة الأولى.

---

## 10) ملصقات الخصوصية

في **App Store Connect → App Privacy** أجب بصدق وفق الواقع (اقتراح مبني على وظائف توفير):

| السؤال | الجواب |
|---|---|
| هل تجمع التطبيق بيانات؟ | **نعم** |
| **Contact Info** (هاتف، اسم) — Purpose: App Functionality — Linked to identity: **Yes** — Used for tracking: **No** | ✅ |
| **Purchase History / طلبات** — Purpose: App Functionality — Linked: **Yes** — Tracking: **No** | ✅ |
| **Location (تقريبي)** — Purpose: App Functionality — Linked: **No** — Tracking: **No** | ✅ |
| **Device ID / معرّف الإشعارات** — Purpose: App Functionality (الإشعارات) — Linked: **No** — Tracking: **No** | ✅ |
| إعلانات/تتبّع عبر شركات أخرى؟ | **لا** — لا يوجد تتبع إطلاقًا |

**التصنيف العمري:** املأ الاستبيان بصدق → النتيجة المتوقعة **4+** (تسوق، بلا محتوى مستخدم عام، بلا مقامرة/إعلانات بالغة). عند سؤال «هل يعرض التطبيق محتوى مستخدم غير مراقب؟» أجب **لا**.

---

## 11) مخاطر الرفض

راجعنا قواعد Apple الحرجة وعالجناها **داخل الكود مسبقًا**:

| القاعدة | الخطر | كيف عالجناه |
|---|---|---|
| **5.1.1(v) حذف الحساب** | أي تطبيق فيه تسجيل حساب ويُقدَّم بعد 2022 بلا حذف حساب = **رفض مؤكد** | ✅ ميزة كاملة اليوم: صفحة حسابي → منطقة الخطر → حذف نهائي بتأكيد مزدوج (واجهة عربية + API مع تكييف البيانات) |
| **2.3.10 أوصاف الأذونات** | استدعاء الموقع/الكاميرا بلا وصف في Info.plist = **انهيار فوري** للتطبيق ثم رفض | ✅ أوصاف عربية كاملة حُقن في Info.plist آليًا |
| **سياسة الخصوصية** | رفض إن لم توجد | ✅ `https://tawfir.giize.com/privacy` موجود ويعمل |
| **التصدير/التشفير** | أسئلة سنوية معقدة | ✅ `ITSAppUsesNonExemptEncryption=false` (HTTPS قياسي فقط) |
| **4.2 الحد الأدنى من الوظائف** | تطبيقات WebView «فارغة» تُرفض | ✅ توفير غني: تسوق، سلة، طلبات، تتبع، إشعارات، تقييمات... |
| **حساب مراجعة** | «تعذّر التقييم» إن لم يستطع المراجع الدخول | ⚠️ **عليك:** أدخل حسابًا تجريبيًا في §8-د |
| **Sign in with Apple** | تُطلب إن وُجد دخول بطرف ثالث (جوجل/فيسبوك) | ✅ دخولنا بريد/هاتف فقط — القاعدة لا تنطبق |
| **المدفوعات 3.1.1** | الدفع داخل التطبيق للسلع الرقمية | ✅ توفير يبيع سلعًا واقعية (الدفع عند الاستلام) — مسموح دائمًا |

---

## 12) بعد القبول

### رفع تحديث (نسخة جديدة)

1. غيّر رقم الإصدار في GitHub عند تشغيل البناء: مثل `version_name=1.3.0`, `version_code=2` (**يجب أن يزيد رقم البناء دائمًا** — وإلا رفض Apple للرفع).
2. (اختياري) حدّث `version` في `package.json` ليبقى متزامنًا مع الواجهة.
3. شغّل الـWorkflow بـ upload_testflight ✅.
4. في App Store Connect → ستظهر النسخة الجديدة في TestFlight → **App Store → iOS 1.3.0 → (نسخة جديدة من هذه النسخة ممكن أيضًا)** — انقل البناء من TestFlight إلى النسخة ثم Add for Review.

### الرد إن رُفض التطبيق (لا تقلق — يحدث للجميع)

- اقرأ **Reason** في Resolution Center — Apple تكتب السبب بدقة وبالإنجليزية.
- أصلح، أعد البناء، وأعد التقديم مع رد مهذب يشرح ما تغيّر.
- الردود تكتب بالإنجليزية، ولا حاجة لمكالمة.

---

## 13) استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| «أسرار Apple ناقصة جزئيًا» | الأسرار **الأربعة** مطلوبة معًا — راجع §3-هـ |
| فشل خطوة التوقيع: «no signing certificate» أو بروفايل | تأكد أن مفتاح API بصلاحية **App Manager**، وأن الـBundle ID مسجّل §3-ج بـ Push Notifications. أعد التشغيل — Xcode ينشئ الشهادة تلقائيًا |
| «You have reached the maximum number of certificates» | فريقك يملك شهادتي توزيع سابقين (حد Apple): developer.apple.com → Certificates → أبطِل القديم غير المستخدم ثم أعد التشغيل |
| فشل رفع TestFlight: «build number already exists» | رقم البناء مرفوع سابقًا — زد `version_code` وأعد التشغيل |
| فشل resolvePackageDependencies | عطل شبكي مؤقت غالبًا — أعد التشغيل. إن تكرر: راجع سجل الخطوة لعنوان url المسبب |
| الإشعارات لا تصل لآيفون | 90% من الحالات: مفتاح APNs غير مرفوع في Firebase (§4) — أو الإذن مرفوض من المستخدم (الإعدادات → توفير → الإشعارات) |
| الروابط العميقة لا تعمل | (أ) `apple-team-id.txt` غير موجود في جذر المستودع أو قيمته خاطئة §5 — (ب) انتظر كاش Apple حتى 24 ساعة — (ج) الروابط تُختبر من خارج التطبيق (Safari/الرسائل) وليس من داخله |
| «Minimum SDK» أو أخطاء Xcode أخرى | انسخ آخر 30 سطرًا من سجل خطوة الأرشفة وأرسلها لي |
| التطبيق يفتح شاشة بيضاء على آيفون | أول إطلاق يحتاج إنترنت (تحميل الموقع الحي أول مرة) — بعدها يعمل أوفلاين. تأكد أن `https://tawfir.giize.com` يعمل من الشبكة المستخدمة |

---

## 14) أسئلة شائعة

**س: هل أحتاج Mac فعلًا في أي خطوة؟**
ج: لا ✅ — التسجيل، المفاتيح، البناء، الرفع، كل شيء من المتصفح + GitHub Actions. (لولا لمرة: لو أردت يومًا فتح المشروع في Xcode فستحتاج Mac — لكن ليس لإصدار التطبيق.)

**س: لماذا 99$ سنويًا بينما Google Play 25$ مرة واحدة؟**
ج: سياسة Apple — الاشتراك سنوي إلزامي لكل الحسابات، ويشمل TestFlight وعددًا غير محدود من التحديثات.

**س: هل يظهر اسمي الشخصي كمطوّر؟**
ج: في حساب Individual: نعم يظهر اسمك في صفحة التطبيق (متطلب قانوني). حساب Organization (299$ + DUNS) يُظهر اسم الشركة — غير ضروري للبداية.

**س: كم يستغرق من لحظة الدفع حتى رؤية تطبيقي في المتجر؟**
ج: الاشتراك: حتى يومين · تجهيز الأسرار: 15 دقيقة · أول بناء: ~35 دقيقة · معالجة TestFlight: ~15 دقيقة · المراجعة: 24–48 ساعة. **الإجمالي الواقعي: 2–4 أيام.**

**س: هل أستطيع رفع تطبيق المالك (توفير مالك) بنفس الحساب؟**
ج: نعم — نفس الحساب يكفي، وسير العمل يدعم `app=owner` جاهزًا. ستحتاج تسجيل Bundle ID إضافي §3-ج (`com.tawfir.ye.owner`) وإنشاء تطبيق جديد في App Store Connect، ولتشغيل إشعاراته: سجّل تطبيق iOS ثانيًا في Firebase بنفس الـBundle ID ونزّل `GoogleService-Info-owner.plist` وضعه في جذر المستودع (السكربت يلتقطه تلقائيًا لنسخة المالك).

**س: ما الفرق بين IPA و APK؟**
ج: نظير AAB/APK لأندرويد — لكن iOS لا يسمح بالتثبيت المباشر من الملف (هذا سبب احتياج TestFlight/Matجر). لا يوجد «تثبيت من مصادر غير معروفة» في آيفون إلا عبر أدوات جانبية للطور التجريبي.

**س: هل تُقبل المراجعة باللغة العربية فقط؟**
ج: نعم — التطبيقات العربية تُراجع طبيعيًا. فقط Notes للمراجع اكتبها بالإنجليزية (§8-د).

---

## ✅ قائمة تحقق نهائية قبل أول تشغيل

- [ ] حساب Apple Developer مفعّل (§3-أ)
- [ ] `APPLE_TEAM_ID` مسجّل في GitHub Secrets (§3-هـ)
- [ ] `APPLE_AUTH_KEY_ID` + `APPLE_AUTH_ISSUER_ID` + `APPLE_AUTH_KEY_P8_B64` (§3-هـ)
- [ ] Bundle ID `com.tawfir.ye.app` مسجّل مع Push Notifications (§3-ج)
- [ ] التطبيق منشأ في App Store Connect (§3-و)
- [ ] مفتاح APNs مرفوع في Firebase (§4) — **للإشعارات**
- [ ] `apple-team-id.txt` في جذر المستودع (§5) — **للروابط العميقة**
- [ ] `GoogleService-Info.plist` في جذر المستودع (موجود من هذه الجولة ✅)
- [ ] حساب عميل تجريبي جاهز للمراجعة (§8-د)

**بعد كل ذلك: Actions → Build iOS IPA — توفير → Run workflow 🚀**
