# 🔔 تقرير نظام الإشعارات الصوتية — توفير (الجولة 8)

> **أداة المشرف الأساسية لاستبدال الأصوات الاختبارية بالنغمات النهائية.**
> كل صوت في المنصة = ملف واحد في `public/sounds/` باسم النوع حرفياً — **استبداله لا يحتاج أي كود**.

---

## 1) الخريطة الكاملة — 18 صوتاً

### أصوات الإشعارات (15) — `public/sounds/notifications/`

| type | الملف (المسار) | النغمة الاختبارية الحالية | الدور المستمع | مقترح الاستبدال (فكرة النغمة النهائية) |
|---|---|---|---|---|
| `order_new` | `/sounds/notifications/order_new.mp3` | نبضتان حادتان متقطعتان (880→1175Hz، مربّعة، 0.31s) — **أعلى أولوية (100 للمالك)** | المالك (عاجل) + الأدمن | رنين جرس مطعم قصير حازم «ترِنغ ترِنغ» — يجب أن يقطع الصمت فوراً |
| `order_confirmed` | `/sounds/notifications/order_confirmed.mp3` | ثنائية صاعدة (523→784Hz، 0.42s) | العميل | نغمة تأكيد واحدة ودّية «دينغ-دونغ» صاعدة |
| `order_preparing` | `/sounds/notifications/order_preparing.mp3` | ثنائية محايدة هادئة (494→587Hz، مثلثية، 0.39s) | العميل | همسة إيقاعية خفيفة (طقطقة مقلاة رمزية) |
| `order_out_for_delivery` | `/sounds/notifications/order_out_for_delivery.mp3` | ثلاثية صاعدة سريعة (587→740→880Hz، 0.44s) | العميل | نغمة «في الطريق» متحركة — توحي بالحركة (سقوط خفيف أو هورن قصير) |
| `order_delivered` | `/sounds/notifications/order_delivered.mp3` | أربيجيو دو الكبير الاحتفالي (C5-E5-G5-C6، 0.68s) | العميل | نغمة نجاح احتفالية قصيرة (تصفيق خفيف أو «تا-دا») |
| `order_cancelled` | `/sounds/notifications/order_cancelled.mp3` | هبوط حزين (523→415→330Hz، 0.57s) | العميل + المالك | نغمة هابطة مكتومة قصيرة — إحساس «أُلغي» بلا إخافة |
| `membership_new_request` | `/sounds/notifications/membership_new_request.mp3` | جرس إخطار (659→988Hz، 0.50s) | الأدمن | نغمة «بريد وارد» رسمية خفيفة |
| `membership_approved` | `/sounds/notifications/membership_approved.mp3` | فانفار صاعد رباعي (G4-C5-E5-G5، 0.68s) | العميل | نغمة إنجاز فخمة قصيرة — «تمت الموافقة» |
| `membership_rejected` | `/sounds/notifications/membership_rejected.mp3` | هبوط مينور (A4-F4-C4، 0.71s) | العميل | نغمة اعتذار هابطة ناعمة — ليست قاسية |
| `membership_expiring` | `/sounds/notifications/membership_expiring.mp3` | 3 نبضات تحذير متوترة (740Hz مثلثية، 0.47s) | العميل | نبضتا تنبيه «انتبه» (منبه ساعة لطيف) |
| `special_offer_new` | `/sounds/notifications/special_offer_new.mp3` | انزلاق صاعد متوهج + لمعة (440→880→1046Hz، 0.50s) | الكل | نغمة حماسية «عرض!» لامعة صاعدة |
| `special_offer_soldout` | `/sounds/notifications/special_offer_soldout.mp3` | هبوط مسطح (392→311Hz مثلثية، 0.50s) | الكل | نغمة «انتهى» هابطة قصيرة خفيفة الظل |
| `facility_approved` | `/sounds/notifications/facility_approved.mp3` | فانفار مزدوج (C5-C5-G5، 0.55s) | المالك | نغمة ترحيب رسمية بالغيّة — «منشأتك جاهزة» |
| `facility_rejected` | `/sounds/notifications/facility_rejected.mp3` | هبوط ثلاثي (415→311→247Hz، 0.63s) | المالك | نغمة رفض محايدة هابطة — لا قاسية |
| `owner_registered` | `/sounds/notifications/owner_registered.mp3` | ترحيب صاعد ناعم (E5-G5-C6، 0.55s) | الأدمن | نغمة «عضو جديد» لطيفة قصيرة |

### أصوات النظام (3) — `public/sounds/system/`

| type | الملف (المسار) | النغمة الاختبارية الحالية | متى تُسمع | مقترح الاستبدال |
|---|---|---|---|---|
| `success_action` | `/sounds/system/success_action.mp3` | نقرة صاعدة قصيرة (659→880Hz، 0.34s) | كل توست نجاح إجراء («تم إلغاء الطلب»، «تم تعليم الكل كمقروء»…) | «كِلك» نجاح واحدة خفيفة غير مزعجة — ستتكرر كثيراً! |
| `error_occurred` | `/sounds/system/error_occurred.mp3` | ازدواج منخفض مكتوم (233→185Hz مربّعة، 0.39s) | كل توست خطأ (variant destructive — فشل دخول/حفظ/شبكة) | «بوم» خطأ مكتوم قصير — واضح بلا إزعاج |
| `notification_open` | `/sounds/system/notification_open.mp3` | نقرة ناعمة واحدة (523Hz، 0.34s) | فتح صفحة /notifications (مرة واحدة لكل فتح) | نقرة/بوب فتح خفيفة جداً |

> **ملاحظتان:**
> 1. النغمات الحالية **مؤقتة للتطوير والاختبار فقط** (توليد تركيبي عبر `scripts/generate-sounds.mjs`) — النغمات النهائية **مسؤولية المشرف**.
> 2. نوعا إشعار لا يملكان ملفات صوت (سلوك مقصود): `membership_received` و`special_offer_ending` — يظهر توستهما بلا صوت. لإضافة صوت لهما مستقبلاً: ضع الملف باسم النوع + أضفه لخريطة الأنواع في `src/lib/sound-service.ts` (هذه إضافة نوع جديد — تلمس كوداً، خلافاً للاستبدال).

---

## 2) كيف تستبدل صوتاً — خطوة بخطوة (بلا أي كود)

1. **حمّل نغمة MP3** من مصدرك المفضل (انظر §4) — أو استخدم ملفك الخاص.
2. **سمِّ الملف باسم النوع حرفياً** — مثلاً لصوت «طلب جديد»: `order_new.mp3`
   (انسخ الاسم من عمود type في الجدول أعلاه — حساس لحالة الأحرف).
3. **ضع الملف في المجلد الصحيح**:
   - أصوات الإشعارات → `public/sounds/notifications/`
   - أصوات النظام → `public/sounds/system/`
   (اكتب فوق الملف القديم — لا تحذفه ولا تنشئ اسماً بديلاً).
4. **حدّث الصفحة** (F5 / اسحب للتحديث في التطبيق) — الصوت الجديد يشتغل فوراً.
5. **تسمّع قبل الاعتماد**: الإعدادات → قسم «الأصوات» → زر ▶ «تجربة» أمام كل صوت.

**مُتحقَّق فعلياً (بالخطوات نفسها على السيرفر):** استُبدل `order_new.mp3` بمحتوى مختلف (نسخ `success_action.mp3` فوقه) → تحديث الصفحة → المتصفح خدم الملف الجديد (4119→4432 بايت، md5 تغيّر، المدة تغيّرت 0.275→0.29s) وزر التجربة شغّله → ثم أُعيد الأصلي وتأكدت عودته (md5 مطابق). لا Service Worker يعترض الأصوات (طلب `audio` يمرر مباشرة) — الاستبدال فوري بعد التحديث.

---

## 3) قيود ملف الصوت

| القيد | القيمة | السبب |
|---|---|---|
| الصيغة | **MP3** فقط | توافق كل المتصفحات + أندرويد WebView |
| الحجم | **أقل من 100KB** لكل ملف | الـ18 ملفاً تُحمَّل مسبقاً بعد أول تفاعل (~110KB حالياً للكل) |
| المدة | **0.5 – 1.5 ثانية** | أصوات الإشعارات يجب أن تكون لحظية (2-3 نغمات داخل المدة) |
| التسمية | **حرفياً = نوع الإشعار** | هذه هي آلية «الاستبدال بلا كود» كلها |

---

## 4) مصادر نغمات مجانية (تجارياً)

| المصدر | الرابط | ملاحظات |
|---|---|---|
| Pixabay Sound Effects | `pixabay.com/sound-effects` | مجاني تجارياً بلا نسبة — فلترة حسب المدة |
| Mixkit | `mixkit.co` | مكتبة مجانية تجارية — قسم Sound Effects |

**نصيحة اختيار:** `order_new` هو الصوت الأهم (يوقظ المالك) — اختره أولاً وجرّبه في أحلك بيئة ضوضاء. الأصوات المتكررة (`success_action`) يجب أن تكون خفيفة جداً كي لا تُتعب.

---

## 5) سلوك النظام (كما بُني ومُتحقَّق فعلياً)

| السلوك | الحالة |
|---|---|
| الكاش المسبق | الـ18 ملفاً تُحمَّل بعد أول تفاعل (سياسة autoplay) — «لا تأخير أول إشعار» ✓ |
| المقدمة فقط | التطبيق بالخلفية → **صفر أصوات** (الإشعار يظهر كتوست عند العودة) ✓ |
| صوت واحد في كل مرة | إشعاران متتاليان → **صوت واحد** (الثاني توست فقط) ✓ |
| الأولويات | الأعلى يقطع الأدنى: `order_new` للمالك = 100 (لا يُسكت أبداً) ✓ |
| اهتزاز الجهاز | طلب جديد للمالك → `navigator.vibrate` إن كان مدعوماً ✓ |
| الإعدادات | `tawfir_sound_enabled` (افتراضي: مفعّل) + `tawfir_sound_volume` (0-1، افتراضي 0.7) في localStorage ✓ |
| منع التكرار | نفس الإشعار لا يُصوَّت مرتين بين WebSocket والاستطلاع الاحتياطي (بالمعرّف) ✓ |
| الاستطلاع الاحتياطي | سقوط WS → دورة كل 30s تكتشف الزيادة وتُصوِّت أحدث إشعار ✓ |
| مصادر التشغيل | WS الفوري + الاستطلاع + توستات الإجراءات (نجاح/خطأ) + فتح صفحة الإشعارات ✓ |

### أين تجد قسم «الأصوات»؟ (البوابات الثلاث)

- **المشرف**: `/admin/settings` → بطاقة «الأصوات» (في الأسفل)
- **المالك**: `/owner/settings` → بطاقة «الأصوات» (في الأسفل)
- **العميل**: `/account` → بطاقة «الأصوات» (في الأسفل)

كل قسم يعرض: سويتش التشغيل + منزلق المستوى (0-100%) + **زر ▶ تجربة لكل صوت من الـ18** — هذه هي أداتك للتسمّع قبل الاستبدال.

> زر «تجربة» يتجاوز الإيقاف عمداً (نية صريحة للتسمّع) ويحترم مستوى الصوت دائماً.

---

## 6) سجل التحقق الفعلي (كل البنود مُختبرة بالنقر/الشبكة)

| # | البند المطلوب | النتيجة | الدليل |
|---|---|---|---|
| 1 | 18 ملفاً بالتسمية الحرفية | ✅ | `ls` = 15+3 ملفات + ffprobe أثبت مدداً 0.31-0.71s + GET 200 لكل مسار |
| 2 | الاستبدال بلا كود | ✅ | تبديل order_new.mp3 → تحديث → 4432 بايت/مدة جديدة → زر التجربة شغّله → أُعيد الأصلي (md5 مطابق) |
| 3 | العميل: 3 حالات بأصوات مختلفة | ✅ | الطلب #40: confirmed→preparing→out_for_delivery = 3 أحداث بأنواع مختلفة (role: customer) |
| 4 | المالك: طلب جديد لحظي | ✅ | طلب #40 عبر API → حدث order_new خلال ثانيتين (priority: 100, role: owner) + توست |
| 5 | زر التجربة يشغّل كل ملف | ✅ | نقر الأزرار الـ18 دفعة واحدة → 18 حدثاً بالأنواع الصحيحة + 36 طلب mp3 في الشبكة (18 تحميل مسبق + 18 تجربة) |
| 6 | الإيقاف يعمل | ✅ | سويتش الإيقاف في /account → إشعار جديد → **صفر** أحداث؛ إعادة التفعيل → عاد الصوت |
| 7 | المستوى يخفض فعلاً | ✅ | 0.2 و0.6 من المنزلق (لوحة المفاتيح) → الحدث يحمل volume: 0.2/0.6 ويُطبَّق على العنصر |
| 8 | الخلفية → لا صوت | ✅ | visibilityState=hidden → إشعار → صفر أحداث؛ العودة → الصوت عاد |
| 9 | إشعاران متتاليان → صوت واحد | ✅ | تأكيد الطلبين #41+#42 بالتوازي (200/200) → **حدث واحد** order_confirmed |
| 10 | الاستطلاع الاحتياطي | ✅ | قطع WS على مستوى الشبكة → تسليم #41 → خلال دورة الـ30s صوّت order_delivered |
| 11 | أصوات توست الإجراءات | ✅ | «تعليم الكل كمقروء» → success_action / دخول خاطئ → error_occurred / إلغاء طلب → order_cancelled قاطعةً success_action (أولوية أعلى — سلوك مقصود) |
| 12 | فتح /notifications | ✅ | تنقل SPA من جرس الإشعارات → حدث notification_open واحد |
| 13 | BLOCKERS.md محدّث | ✅ | أُغلق عائقا approve/reject وseed (أُصلحا بالجولة الختامية للباك إند) — انظر تقرير_توفير §8 |
| 14 | lint + tsc | ✅ | 0 أخطاء (3 تحذيرات موروثة) + tsc نظيف + كونسول بلا أخطاء |
| 15 | 360px | ✅ | صفحات الأصوات الثلاث بلا تجاوز أفقي (أُصلح تلميح الاستبدال: نص flex بلا التفاف → span + min-w-0) |

**الطلبات المستخدمة في الاختبار** (كلها على حساب `customer@tawfir.local` بمنشأة المالك 17): #40 (دورة كاملة حتى delivered)، #41 (delivered)، #42 (out_for_delivery)، #43 و#44 (أُلغيا لاختبار زر الإلغاء + الصوت).

---

## 7) ملفات الجولة 8

### Added (3)
- `public/sounds/notifications/*.mp3` — 15 صوت إشعارات (مولّدة تركيبياً — مؤقتة)
- `public/sounds/system/*.mp3` — 3 أصوات نظام (مولّدة تركيبياً — مؤقتة)
- `src/lib/sound-service.ts` — الخدمة: الكاش/الإعدادات/الحراسات/الأولويات/الأدوار/منع التكرار
- `src/components/shared/SoundSettingsCard.tsx` — قسم «الأصوات» المشترك (سويتش + منزلق + 18 زر تجربة)
- `scripts/generate-sounds.mjs` — مولّد النغمات الاختبارية (PCM → ffmpeg MP3)

### Modified (8)
- `src/components/shared/NotificationsProvider.tsx` — صوت كل إشعار WS (بالنوع + الدور + المعرّف) + toast بلا صوت مكرر
- `src/hooks/useUnreadCount.ts` — مسار الصوت الاحتياطي (زيادة العدّاد → صوت أحدث إشعار) + كاشف الدور
- `src/hooks/use-toast.ts` — صوت مركزي للتوستات: نجاح/خطأ بالكشف التلقائي + خيار sound صريح
- `src/app/(public)/notifications/NotificationsContent.tsx` — notification_open مرة واحدة عند الفتح
- `src/app/providers.tsx` — SoundService.init() (التحميل المسبق بعد أول تفاعل)
- `src/app/(admin)/admin/settings/page.tsx` + `src/app/(owner)/owner/settings/page.tsx` + `src/app/(public)/account/page.tsx` — تركيب SoundSettingsCard (الحساب في فروعه الثلاثة)
- `BLOCKERS.md` — إغلاق العائقين المتقادمين + ترويسة «بعد الجولة 8»
